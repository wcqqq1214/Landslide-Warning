# 图件合同

用途：向导师呈现同一四点在完整训练段与 293 日条件预测段的真实结果。Python/matplotlib，沿用已有任务后端。采用用户参考图的四面板布局和线/区间表达，重新绘制本实验数据，不复刻 QRF 模型或参考数值。

核心证据：训练拟合较近并不保证独立预测优于连续原 B+；BRES 比 DIRECT 的均值好，但本次仍未超过 B+。两张训练/预测图分别给两臂全部四点，四面板承担空间分点的检验作用，不删除表现差的点。第三张完整 293 日均值对照图同时展示五方法及实测，用于避免完整训练历史的纵轴范围掩盖预测差异。

字段：原日期→横轴；保存位移均值/实测→纵轴 mm；所有系列共同减去原始第一日 y0，仅为累计位移展示零点，不在训练/预测分界平移。全部 1461 日、全部 293 日、四点、三个种子均保留；排除 0 行。两臂主线为三个种子等权平均，色带为冻结高斯尺度的 80%/95% 逐日边际区间；训练段不虚构区间。均值对照图只比较均值，区间在前两图和 CSV 中完整保留。

导出：240×170 mm 的 PNG（300 dpi）与可编辑文字 SVG；中文字体 Arial Unicode MS。按用户要求不制作 PDF，因此不运行依赖 PDF 的字体/碰撞审计，也不声称其通过。使用最终 Matplotlib 文字/数据线几何、1.5 pt 面板对齐、实际 SVG/PNG 数值/字体检查及逐面板目视替代当前交付核验。

审阅重点：不以 100% 覆盖掩盖宽区间；不截尾，不事后选轮数/种子；训练拟合、给定未来驱动、无位移反馈及探索性评价均明确标注。主曲线、阴影和图中完整 293 日 RMSE 必须对上已核验 CSV/数组。
