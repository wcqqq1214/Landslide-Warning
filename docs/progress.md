# 藕塘科研进度

更新日期：2026-09-11。本页只记录当前状态与最近完成工作；历史见[文档导航](README.md)。

## 当前状态

**只限定 ConvLSTM/PINN；v2.1 概率 PINN 方案已完成，尚未实现或训练。**
最新已完成实验 v2.0 未达标并已停止，继续保留 B+ 基线；整体研究目标尚未完成。
范围保持藕塘 ATU1、ATU5、MJ3、MJ1 的位移概率预测，不开展预警或新案例。
撤回此前非神经状态空间模型建议；本次没有采用其他模型家族。

[v2.0 结果](ootang_convlstm_direct_results.v2.0.md)：直接多步 ConvLSTM 加神经尺度头，
原两窗各完整 180 日、三种子等权混合。严格均值改善 5/8，但预测平均 RMSE 为
64.4117/22.1677 mm，P0 为 58.0122/22.2448；CRPS 为 47.2449/26.9716 mm，
P0 为 42.0113/25.1140。两窗完整概率判据失败，不能以改善个数代替稳定四点效果。

## 本次完成与核验

- [v2.1 方案](ootang_probability_pinn_plan.v2.1.md)及[配置](../config/ootang_probability_pinn.v2_1.json)
  已冻结提交 `1ebb98a`。候选为受限广义力偏差的降阶状态 PINN，力偏差末层采用变分贝叶斯近似；
  数据、物理约束与最终预测使用同一条件状态，均值和区间来自同一混合分布。
- 实际核对导师 ZIP 的模型/求解器源码、旧 PINN 结果和概率 PINN 原文；核对 20 份既有输入、
  两个 ZIP 成员、保存轨迹与观测映射，以及参数量/更新数/预算一致性。配置记录 21 项来源哈希（含新方案）。
- 原外层 432/612、完整 180 日和效果判据保持。新增未来无标签物理配点协议，训练仍不得使用未来位移；
  已给定未来驱动和历史窗口反复暴露的限制保持，不能称新盲测。
- 用既有数组检查固定背景给出的宽松位移下界，未直接排除该结构；这不证明力偏差幅度足够或方法有效。
  来源、复算警告及限制见方案第 7 节。本次没有新增模型前向、梯度、训练、力学积分或效果结果。
- 本次方案阶段记账 30 分钟（08:31:12 UTC 起，包含核对与提交）；后续实现、运行、核验和收尾
  最多 90 分钟，共享候选 120 分钟上限。方案阶段如超过 30 分钟，超出部分扣减后续额度。

## 后续边界

按用户具体执行请求进入 v2.1 实现与首模型可行性检验。首个固定模型的全部既定抽样须满足原力学容差，
任一失败就停止整个候选，不继续其余模型；通过后最多完成六个固定模型，再检查全部均值和概率判据。
不能以降低容差、改输出路径、筛种子/抽样或追加版本延长该尝试。方案完成不等于效果验证或导师验收。
原切分、阈值、模型记录及全部负结果保持；原始日值 as-of 未知、当前 h 预处理等限制见冻结来源。

## 历史查证与清理

- [路线复盘](ootang_route_review_2026-09-11.md)保留此前八轮、16 个变体的失败证据；不改写旧表冒充包含 v2.0。
- v2.0 方案、实现、实验产物、停止记录分别提交 `e49ac92`、`b3ba6c7`、`453762f`、`aece1c9`；
  7 项相关检查及 15 个模型重载通过，但效果失败，剩余预算不转入 v2.1。
- v1.26 九个未运行计划/配置/草稿文件已删除（`cb08f62`），原字节可从 `7fc5f29` 恢复；不恢复为活动入口。
- 入口精简提交 `672a69d`；清理前完整进度可用 `git show cb08f62:docs/progress.md` 查证，不复制回当前待办。
