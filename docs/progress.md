# 藕塘科研进度

更新日期：2026-09-13（北京时间）。当前结果与历史入口见[文档导航](README.md)。

用户已要求“按照这个 plan，一步步进行”，本次[冻结可信度审查](ootang_rolling_frozen_validation_report.v1.0.md)开始执行，独立 60 分钟窗口为 2026-09-13 09:59:11—10:59:11 UTC。按计划固定五对象，新增模型／拟合／物理调用预算为 0；原八小时与 C19 停止状态保持。先准备来源核对工具，随后按 A—D 分步记录与 commit；未决的 as-of 和独立评价资料不阻塞其他审查。

A 步骤已于 10:02:22 UTC 完成：299 项产物、191 项来源哈希通过，五对象两阶段十份预测及三配置冻结，C18 模型／统计回执和 manifest 匹配。工具提交 `0e6c9e7`；本步零数组加载、模型训练或物理调用，原数值／图件核验仅复用。接着检查尺度对应、指定时间边界和现有日值说明，不重复跑完整统计。

本次用户要求根据 6pro 对 `1b8fc12820eff42e7133e806597489152e794082` 的审查意见输出下一步计划，已形成[冻结模型与可信度验证计划 v1.0](ootang_rolling_frozen_validation_plan.v1.0.md)。固定 C18_PHYS、C18_CORE、C16_FAST_PHYS、DRIFT1、B_ANCHOR，分开判断整体滚动收益、B+ 增量和新增方差价值；重点审查 C8 误差尺度与 C16 修正均值的对应关系、信息矩阵假设及实际观测可用时点。冻结更新规则，在线系数仍按原算法使用已成熟监督；没有未暴露数据时完成历史探索审查，不重新包装盲测，也不以补采资料为前置条件。本步只核读源码／配置、保存 CSV 和审查引用并写文档，未运行模型、数组复算或新训练。拟议后续一次 60 分钟审查尚未启用，不恢复原八小时、不启动 C19。6pro 报告远端已有原快照；本次 SSH 查询连接关闭，旧“未 push”仅保留为形成时状态，本次未 push。

本轮原八小时总截止已到（2026-09-13 05:27:52 UTC）。C18 训练外计算、模型核验、统计核验及图件检查已完成；六次复盘与当前入口备份 `c3cc211` 在 05:28:12 UTC 前后结束，文档/备份收尾约超出 20 秒，实际超时不隐去。本段只补计时说明，不追加实验或重置预算。C18 是数值门槛通过的探索候选；后期已暴露、方差近似与物理增量仍有未决限制，正式独立有效性和原293日目标不标记完成。

## 当前结论

**当前：18 个候选、六次三候选复盘完成。C18 信息矩阵方差近似在开发/后期均通过原 27 项数值门槛，独立模型/评分、112 项统计及六图核验完成。** 开发/后期平均 RMSE 0.5740/1.3931、CRPS 0.3508/0.6937 mm、90% 覆盖89.34%/86.27%；均值继承 C16，CRPS 相对 C16 略退步，区间评分改善。保留为探索候选，不称新盲测或完整贝叶斯后验，物理增量仍不稳定；原 C4 迁移失败及293日任务不改判。原总截止05:27:52 UTC保持，停止新增C19。结果 `db351d0`，源码/原件/核验 `4139854`/`ca1992e`/`fc99763`，56项相关合成检查；每步本地提交，未push。 [C18完整记录](ootang_rolling_probability_candidate.c18.md)。
[冻结计划](ootang_rolling_probability_plan.v3.0.md) / [配置](../config/ootang_rolling_probability.v3_0.json)。
授权约 8 小时：2026-09-12 21:27:52—2026-09-13 05:27:52 UTC，准备、训练、修正、核验和提交都计入。
同一原始数据、四点不变；主评价为每天更新历史观测后的未来 30 日端点，同时报告 1—30 日全部步长。
B+ 同样接收当前位移，加入近期残差速度、位移保持和速度外推对照；不是用新信息击败未更新 B+ 后归功神经模型。
每三次候选复盘，共五次定期复盘及 C16/C17 收尾；原六/九候选等形成时安排分版保留，所有工作计入同一预算，不重启旧版本。
原 293 日固定起点目标和负结果单独保留，滚动协议尚非导师已验收要求。

实施步骤：[v3.0 实施记录](ootang_rolling_probability_implementation.v3.0.md)。因果数据接口、物理教师边界、同信息对照、ConvLSTM 与正则回归概率模型均已实现；至 C6 累计 21 项相关检查和 lint 通过。

以下按步骤保留形成时记录；中间步骤的“下一步”和累计计数不覆盖上述当前结论。

物理准备已完成：5 个冻结教师扩展到 1168 日驱动，共 10 次前向、0 次标定；既有重叠输出差 0，短前缀核验最大差 1.14e-13 mm。矩阵运算警告如实保留。

C1 首轮开发已完成并失败：[滚动结果记录](ootang_rolling_probability_results.v3.0.md)。30 日平均 RMSE：C1 9.5789、B_ANCHOR 10.0224、DRIFT14 1.6744 mm；均值/概率整体门槛未过，不能进入后期迁移。共 1,350 次更新、86.81 秒；首个启动路径异常原件保留。

C2 平均 RMSE 1.6769 mm，未超过固定 DRIFT1 的 1.2779，且均值几乎等于初始组合；24/27 项通过，不构成学习均值成功。C3 正则回归 FULL 为 1.1105 mm、CRPS 0.5570，首次超过最强固定速度对照，但平均覆盖 84.22% 和 MJ3 76.08% 未过，25/27 项通过。DATA 消融更好，尚无物理输入额外贡献的证据。三轮均已独立重载/复算；C3 原件 `bcc8b47`、核验 `51063bd`。

[第一次三候选复盘](ootang_rolling_route_review_2026-09-13.md)决定停止 C1/C2，冻结 C3 原分布；第四候选只检验对模型与所有对照统一使用的因果覆盖反馈，不重选均值或放宽门槛。下一阶段仍为开发，未读取本轮后期迁移标签。累计 2,850 次神经更新、1,440 个正则求解、48 次尺度优化；物理仍为 10 次前向、0 次标定，未 push。

C4 零新拟合，开发平均 RMSE 1.1105、合并 1.3918、CRPS 0.5958 mm，覆盖 86.74%；27/27 通过。合并 RMSE 仍差于 DRIFT1，DATA 消融更好；反馈提高覆盖但相对 C3 的 CRPS/区间评分退步，限制保留。[迁移选择锁定](ootang_rolling_transfer_decision.v3.3.md)固定 C4_FULL、DATA 消融、DRIFT1 对照、alpha=0.001 及完整配置，仅使用一次后期迁移选择。

后期结果已完成：平均 RMSE 1.7539（B_ANCHOR 5.5060、DRIFT1 4.0617）、CRPS 0.8205 mm，但平均覆盖 80.40%、ATU1 79.17%、ATU5 70.83% 未过，24/27 项通过。DATA 平均 RMSE 1.7305，不支持物理额外收益。独立核验与配对成块/非重叠统计完成，六张完整曲线已目视检查；没有删尾段或事后重选。当前停止按后期标签修改候选，保留均值正结果与概率缺口，不声称完整目标成功。实现、效果与用户/导师验收分列，后者没有据本轮自动完成。

累计 4/6 主候选、2,850 次神经更新、1,680 个正则求解、56 次尺度优化（1,015 次目标调用、840 次迭代）、22 次物理前向、0 次物理标定。总上限仍为 05:27:52 UTC，当前没有运行中的训练；并非额度耗尽。不再根据已用的后期评价选模，剩余时间不作为开启诊断链的理由。代码、模型、原始输出、核验和图件分步提交，未 push。

2026-09-12 23:17:12 UTC 收尾核对：距总起点约 1 小时 49 分钟，同一 8 小时上限尚未耗尽。六个正式结果/分析目录的 362 项产物哈希、五次运行完成状态、核心报告本地链接以及 B_RAW 概率不适用列均通过；至简报提交 `63cd944` 共 30 次本轮分步备份。工作树仅余用户原有的未跟踪 Vajont Excel。没有合格完整效果结论，未将研究目标标记为已达成。

自主目标继续核对后，新增[C5 开发对照](ootang_rolling_probability_candidate.c5.md)：只检验用历史前缀模型的预测误差训练尺度，保留均值与反馈规则。原六候选/同一八小时上限不变，用户原授权涵盖此内部研究；没有重新开放后期选模。C4 仍为唯一后期候选且整体未过，C5 只能形成开发证据，不能宣称完整跨期目标达成。上一轮已完成工作的结论与提交不变。

C5 已完成并独立核验，开发 FULL 的 CRPS 15.6544、90% 宽度 218.6903 mm，大幅差于 IN_SAMPLE，20/27 项通过；停止原 C5。历史较早辅助模型有巨大外推误差，均值与原对照已核对不变。最后一个[C6 开发候选](ootang_rolling_probability_candidate.c6.md)统一只用最近已完成折训练尺度，不按点选折、不改参数/门槛、不进入后期。完成后按约定进行第二次路线复盘，不新增第七候选。

C6 已完成：23:45:44—23:45:51 UTC，6.4130 秒，16 次尺度优化、330 次目标调用、295 次迭代，全部收敛；零新均值拟合、神经更新和物理调用。[尺度对照结果](ootang_rolling_probability_scale_study.v3.4.md)显示开发 FULL 的 CRPS 0.5874、区间评分 6.4493 mm、覆盖 86.89%，27/27 通过；均值仍为 1.1105 mm。相对 C4 的概率评分小幅改善，但内部阶段两臂概率评分均退步，不能称稳定成功，也没有运行后期。六个候选已用满，正在完成第二次复盘与收尾。

C6 原件 `295f399`，独立核验通过：116 项产物、74 项来源、556 个起点锁和 49,440 个评分单元；主模型 1,112 个起点集合重载与全部反馈尺度差 0。四个辅助模型及 302 个历史查询的时间边界、独立系数/预测、尺度 NLL 已核对，未重训或重跑优化器。位移上限 1168 行，原数值容差不变，最新相关检查共 21 项通过。实现通过与效果未完成分别保留。

第二次路线复盘 `671034e` 已完成，逐项核对用户目标；C6 核验提交 `8f0d999`。当前累计六个主候选、一次后期选择、2,850 次神经更新、2,160 个正则求解、104 次尺度优化（2,111 次目标调用、1,805 次迭代）、22 次物理前向、0 次物理标定。结束本轮候选搜索的依据是尺度收益跨期不稳定、六候选上限和已使用的后期选择；总时间仍未耗尽，不将研究目标标记为完成。报告和导航同步本状态，保留全部原始证据与原长窗结论。

2026-09-13 继续目标时修正停止判断：六候选是助手自定安排，不能代替用户约八小时自主任务的完成。旧六轮、原一次迁移和全部负结果冻结，C7 另开明确标记的探索性校准对照；以已有误差分布形状为具体假设，原 27 项阈值与四点完整日期不变。本步只固定方案/配置及来源哈希，未运行或重新拟合。

C7 方案/配置提交 `0463d96`；经验分布与同池高斯对照实现完成，26 项相关检查和 lint 通过。对历史阶段末端没有保存的未到期预测不作补造，两臂统一等待当前阶段误差成熟。完整均值/raw_sigma 复用旧数组，没有新拟合；即将按固定方案执行两段探索性评价，后期已暴露限制保持。

C7 已完成一次运行（00:22:54—00:22:59 UTC，4.7141 秒），零新拟合/优化/神经或物理调用。经验分布 FULL 的开发/后期探索 CRPS 为 0.5239/0.8236，覆盖 81.56%/76.89%，分别 25/27、24/27，未解决覆盖。同池高斯对照开发概率评分较差，但后期覆盖更高，局部评分收益不构成整体成功。所有均值保持 C4，C7 停止调参，接着完成独立分布及因果时序核验。累计 7 个主要候选，原总截止和旧 C4 一次迁移失败保持。

C7 原件 `51bba25`，独立复算实现 `7e6be88`；00:33:10 UTC 核验通过，50 项产物、47 项来源、669 个预测锁、79,477,200 个池值和 95,040 个评分单元已核对。所有原始均值/尺度不变、全部误差池精确重建，CSV 差最大 4.98e-10，原容差未改；零新拟合或物理调用。覆盖不足不是此次评分或时间边界错误，不能继续靠更换区间形状宣称解决。

C7 独立核验提交 `4f9258f`。基于“每日新观测目前只更新输入和区间、未更新均值映射”的源码事实，C8 单独预定在线回归系数更新；不先断言参数漂移或收益。两阶段共计划 153,600 次成熟监督样本追加/小矩阵求解，原始尺度、全部静态对照和 27 项标准保持。配置/来源先固定，尚未运行；总截止不变。

C8 方案提交 `41b6c03`；在线回归实现与三项新增检查通过，验证递增解等于独立增广批量解、未成熟/重复/错步长目标拒绝及冻结标准化。保存逐日起点系数、初始化与最终充分统计量、已发特征和完整校准记录；原模块与静态对照不改。累计相关合成检查 29 项，新增 lint 通过；准备一次两段执行，不增加预算。

C8 已完成一次两段运行（00:47:09—00:47:20 UTC，10.9576 秒），153,600 次在线点位/步长系数更新，神经、尺度优化和物理调用为 0。FULL 开发平均 RMSE 1.1105→1.0781、CRPS 0.5958→0.5737，覆盖 87.25%，27/27；后期探索平均 RMSE 1.7539→1.7519、CRPS 0.8205→0.8198，覆盖仍 80.40%，24/27。没有解决覆盖问题，原静态对照和原始尺度完全保留；接着独立核验批量解和成熟目标时序。累计 8 个候选，原截止不变。

C8 原件 `821c48c`，独立核验器 `7fa4ea6`；00:57:37 UTC 核验通过：77 项产物、63 项来源、669 个锁、153,600 次原系数更新、55,440 个评分单元。600 次增广 SVD 和全部批量正规方程验证，系数差最大 5.74e-12、均值差最大 1.14e-12 mm，特征与校准差 0；原静态分布/尺度精确保持，旧容差不变。没有新的训练或物理调用，效果结论保持。

C8 核验提交 `307ea68`。从冻结训练数组/参数完成按步长原始尺度核对，原模型 30 日拟合覆盖显著低于短步长，完整四点/30 步与来源保存。C9 据此预定每点每步长两个尺度系数，共 480 次优化，保持 C8 在线均值和原校准规则；初值、边界、优化容差沿用旧值，尚未执行。第九候选后再次复盘，总截止不变。

C9 方案提交 `79f36ef`；逐步长尺度入口与两项新增检查完成，新增 lint 通过，累计相关合成检查 31 项。首次新增合成检查的整数/log-exp 精确比较存在 8.88e-16 舍入差，已按浮点表示修正该检查，生产公式和旧数值容差未改。均值、初始化监督与旧对照冻结，准备一次 480 头优化和两阶段因果重放。

C9 一次运行完成（01:15:31—01:15:34 UTC，2.5305 秒），480 次尺度优化、7,922 次目标调用、6,506 次迭代，全部保存收敛标志为通过；新增均值、神经和物理调用为 0。FULL 开发/后期覆盖从 C8 的 87.25%/80.40% 到 88.04%/82.58%，但 CRPS 为 0.5861/0.8676，两阶段均退步；开发 27/27，后期 25/27，平均与 ATU5 覆盖失败。均值完全保留，准备独立核验；第九轮完成后复盘，不据局部覆盖增加自动开新候选。

C9 原件提交 `bd3232a`，独立尺度/复数步长梯度与评分核验器准备完成；格式、lint、语法检查通过，尚未把实际数组核验标记为通过。原始模型/评分文件不改，核验不追加优化或物理调用。

C9 核验器 `7808d3b`；01:20:15 UTC 实际核验通过，75 项产物、58 项来源、669 个锁、480 个头和 63,360 个评分单元。均值/旧分布精确保持，原始尺度差 3.55e-15 mm、校准差 0，独立 NLL/复数步长梯度差最大 8.44e-15/1.67e-14，CSV 差最大 5.00e-10；零重训和物理调用。拟合段 30 日原始覆盖确实改善到 88.16%/89.32%，但预测未完整通过，停止 C9 并进行第三次复盘。

C9 独立核验提交 `1f151b6`；第三次 C7—C9 路线复盘完成，明确停止仅调整经验分布/尺度的分支。累计九候选、2,850 次神经更新、2,160 个初始/辅助 Ridge 求解、153,600 次在线更新、584 次尺度优化（10,033 次调用、8,311 次迭代）、22 次物理前向。下一项可检验差异是在线监督的有限记忆，尚未开跑；不把自定九候选安排变成用户授权结束，原 05:27:52 UTC 截止保持。

第三次复盘及当前入口已同步九轮结果。C10 单独固定指数遗忘在线监督，一种衰减尺度，不搜索参数；配置引用 C8 不可变产物与已通过核验，预计另计 153,600 次在线点位更新，零初始拟合/尺度优化/神经/物理调用。这里只完成事前方案，尚未运行，不把结构合理性当成已验证收益。

C10 指数遗忘实现完成；复用的在线类保持默认 lambda=1，新增一条保留固定正则矩阵的减权路径。三项新增加权 SVD/无效监督/默认行为检查与原三项在线检查通过，累计相关合成检查 34 项。新入口冻结全部 C8 对照与原始尺度，读取已核验的因果特征；将一次运行原固定配置，尚无效果结论。

C10 一次运行完成：01:39:29—01:39:39 UTC，9.3270 秒，153,600 次在线点位求解；零新神经/尺度优化/物理调用。FULL 均值和概率均差于原 C8，DATA 也退步；停止当前遗忘配置，不调整衰减速度。完整原件保存，独立加权方程及评分核验继续；累计在线更新 307,200，原总截止保持。

C10 原始结果已分步提交；独立显式加权方程核验器准备完成，核对固定正则、每起点成熟权重及系数，并用增广 SVD、原始特征和独立评分交叉检查。实现 `159dc36` 的默认 C8 分支与原快照保持，核验器语法/lint 通过，准备真实产物只读复核。

C10 原件 `cd0005b`、核验器 `ee94f77`；01:45:38 UTC 独立核验通过，76 项产物、59 项来源、669 个锁、153,600 次原更新和 63,360 个评分单元；600 次加权增广 SVD。系数/权重/均值差最大 2.91e-12/2.84e-13/1.02e-12 mm（仅均值项单位 mm），特征/校准差 0；旧分布与原尺度精确不变，原容差保持，零新训练/物理。减权假设在此配置未获支持，停止参数搜索；三次路线复盘补记当前结果，完整自主目标保持未完成。

C10 后先进行不读取新位移标签的 h30 回归几何筛查，来源和公式见 `results/ootang_rolling_v3/20260913/c11_geometry_screen.json`。已有 C8 信息矩阵下，后期 FULL 的 ATU1/ATU5 最大杠杆量仅 0.001964/0.001543，直接采用同方差 `sqrt(1+ell)` 因子只产生约千分之一的最大尺度变化，因此不优先启动这一直接修正。它不是经校准的贝叶斯后验，也不排除一般贝叶斯模型；零模型拟合、物理调用和新预测评分。下一项待固定问题转为 B+ 对较大偏差概率的参照价值，尚未运行新候选。

C11 已事前固定双尺度同中心混合，主候选 PHYS、对照 TREND，共用 C8 DATA 原均值/尺度。采用 DATA 是全四点新方案的明确设计选择，原有均值收益不归功混合，也不改判旧 C4/C8 主候选。固定最近 90 条成熟监督、零初始权重、CRPS 二次式封闭解；没有主观尾部概率下限、宽度倍率或参数搜索，尚未运行。

C11 双尺度混合入口及四项新增概率检查通过，累计相关合成检查 38 项。CDF 积分/独立数值最优解和未来目标保护均核对；每个起点锁定完整混合参数再释放标签，最终真实分位数只由锁定参数计算。原 C8 均值/核心尺度和全部对照保持；准备按唯一固定方案运行，未预先宣布概率改善。

C11 一次运行完成（02:07:39—02:07:43 UTC，3.4412 秒），153,600 次混合权重成熟更新，零新均值/尺度/神经/物理拟合。PHYS 开发/后期覆盖仅略升至 87.32%/81.34%，CRPS 和区间评分均差于同均值 C8 DATA；后期平均/ATU5 覆盖失败。配对 TREND 后期覆盖相同，概率评分略差；当前不能称新增物理分量有实用收益。原件保存，真实混合 CDF、成熟权重与评分继续独立核验。

C11 独立核验器已准备：高斯分量能量公式、h30 子集 CDF 积分、所有上下界真实 CDF 概率、成熟系数池/权重和旧核心/对照不变；不导入生产混合学习器。接着只读核验实际数组，未重跑模型或改变概率标准。

C11 实现 `48af1ce`、原件 `2c97930`、独立核验器 `dadac87`，实际核验通过：68 项产物、53 项来源、669 个锁、153,600 次成熟权重更新、63,360 个评分单元、921,600 个 CDF 值、184 次 h30 独立积分。权重/核心/分布核对通过，积分 CRPS 差最大 9.52e-14 mm，真实 CDF 概率差最大 1.99e-12；原容差保持，零新训练/物理调用。停止 C11 CRPS 组合，不能把物理权重非零当作方法成功。

C11 独立核验提交 `1dc25a1`。C12 独立方案/配置已固定，同一核心、物理/趋势参照和成熟时序，只比较训练目标；使用最近 90 条预测的区间评分选择事前 101 点权重网格，预计 153,600 次权重更新和 15,513,600 个损失值，尚未运行。每个旧 C11 对照保留真实混合评分，避免按矩尺度误用高斯。

C12 方案 `b132ace`；有限网格区间评分学习器实现完成，四项新增独立求根/成熟池/旧混合评分检查及原四项混合检查通过，累计相关合成检查 42 项，lint 通过。完整批次先验证再更新，保存最终平均风险网格。准备执行唯一固定配置，尚无效果结论。

C12 实现 `1acd0ab`；一次运行 02:36:11—02:37:06 UTC，54.9049 秒，153,600 次成熟权重更新、15,513,600 个网格损失值。开发覆盖增加但后期仍 81.34%，27/27、25/27，CRPS/区间评分差于核心和趋势参照；停止该目标配置。准备独立核验，累计混合权重更新 307,200，其他训练与物理计数不增。

C12 独立核验器已准备：用 erf 形式的混合 CDF 重算全部网格风险和成熟最优权重，另用分量能量公式与 CDF 积分核对 CRPS。旧 C11 分布逐字段保持并按真实分布评分，网格风险计数单独核对；不导入生产学习器。lint 通过，实际核验尚待执行。

C12 原件 `93476ab`、核验器 `9a125fc`；02:39:57 UTC 独立核验通过：73 项产物、58 项来源、669 个锁、153,600 次成熟权重、15,513,600 个网格损失值、71,280 个评分单元。已发权重差 0，风险网格差最大 2.13e-13 mm，368 个 CDF 积分与 1,843,200 个 CDF 值通过，旧分布精确保持。效果失败与核验通过分开，零新训练/物理；第四次复盘接着完成。

第四次 C10—C12 路线复盘完成：指出近期多轮投入仍偏向概率层的小改动，停止遗忘参数和固定核心宽分量/评分切换分支。十二轮、四次复盘、42 项合成检查及适用独立核验完成；完整效果与额外物理价值未完成。原截止尚未到，暂无冻结 C13，不自动枚举模型；下一项必须说明新的可学习关系及证据。

第四次复盘 `0ac04f6` 后同步当前入口、简报和实现导航至十二轮。明确答复方法范围：Ridge/在线风险学习为机器学习，ConvLSTM 为深度学习；类别符合不能替代概率覆盖和额外物理收益。原始结果不改，暂无新冻结候选，工作树无关表保持，未 push。

第四次复盘补充 C8 h30 全四点误差方向与时间可用性核对，描述表及复算源码保存。误差不是稳定单侧偏移；相邻预测误差不能在当前 h30 起点使用，实际最近可用的是前 30 个起点的误差。零训练/物理，后续只能在线学习已兑现误差关系，不能按全段相关符号预设修正。

误差描述证据提交 `8798a35`；C13 方案/配置事前固定。统一使用 C8 DATA 核心，比较核心误差加 B+、核心误差加 DRIFT14、仅核心误差三个在线 Ridge 头；全部系数从 0 开始，只用合法成熟监督，概率尺度固定不变。尚未执行，不将样本相关性当作预测收益。

C13 方案 `7501fdc`，成熟误差特征和在线 Ridge 头已实现，四项新增独立 SVD/时间边界/完整回放检查通过，累计 46 项；原宽度和所有对照精确保持。新系数从 0 开始，只用实际兑现的目标，准备一次两阶段三臂运行，原截止不变。

C13 实现 `65b156d`；02:59:33—02:59:37 UTC 一次运行 3.7519 秒、219,240 次成熟点位求解，零新尺度/神经/物理。PHYS 开发/后期平均 RMSE 1.3527/14.3433，三个误差头均退步，停止当前配置，完整原件与宽度不变记录保留；接着核验来源、特征、系数及尺度换算。

C13 独立核验器已准备，重建全部成熟误差、批量正规方程与增广 SVD、原尺度/对照和所有评分，另核对逐日输入及预测锁；不导入生产学习器，lint 通过，准备实际只读核验，不重新训练。

C13 原件 `49b5d19`、核验器 `2416913`；03:05:23 UTC 独立核验通过：80 项产物、57 项来源、669 个锁、219,240 次成熟更新与 2,160 次 SVD。特征差 0，系数/均值差最大 1.24e-14/2.27e-13 mm（仅均值单位 mm），评分差不超过原舍入容差，原尺度/旧分布精确保持。当前配置失败得到确认，不重跑；其时间变化的标准化关系不等同于直接毫米误差关系。

C13 核验 `fa62660`；尺度还原核对仅使用保存 sigma/预测，不读标签，原公式含当前/过去尺度比，完整四点和源码另存。C14 据此事前固定首份合法预测尺度作为阶段内单位常数；改变单位约定而不改变概率宽度，不断言全部失败根因已证明，尚未运行。原 C13 结果保持。

C14 方案 `b13d7b7`；固定误差单位分支已实现，保存第一起点常数，旧 C13 默认路径保持。两项新增固定尺度来源/扰动不变及毫米 Ridge 等价式检查与原四项检查通过，累计 48 项。概率 sigma 不改，准备一次三臂执行，尚无效果结论。

C14 实现 `3504e98`；03:10:37—03:10:41 UTC 一次运行 4.0958 秒，219,240 次成熟点位求解。PHYS 开发/后期平均 RMSE 0.6190/1.8356，25/27、24/27，仍差于核心。CORE 只有开发小幅收益，后期退步；完整三臂与固定宽度保留，停止该单位约定，独立核验继续。

C14 独立核验器已准备，核对首起点常数、固定单位特征/目标/还原、全部在线头与旧 C13 控制分布；默认 C13 路径保持，lint 通过，接着进行实际只读核验，零新训练。

C14 原件 `b0f4240`、核验器 `d34339e`；03:15:25 UTC 独立核验通过：88 项产物、63 项来源、669 个锁、720 个固定常数、219,240 次成熟更新、2,160 次 SVD 和 79,200 个评分单元。特征差 0，系数/SVD/均值差保持原容差，旧分布与核心宽度精确不变；效果失败与核验通过分开。C13/C14 共计 438,480 次误差头求解，累计在线 Ridge 求解 745,680，原其他训练/物理计数不变。

C13/C14 结果与停止状态同步到当前入口、简报、实现及第四次复盘补记；原 C8 核心均值优势保留，概率与物理额外价值未完成。下一项是检查低维回归如何利用其他点当前已知变化，尚无冻结 C15；不是重新启动旧 ConvLSTM 或假定空间学习有效。

C15 依据当前 Ridge 逐点特征/系数的源码限制事前固定跨点输入；复用 C8 原训练/已发六特征和标签，追加邻点十二项实测历史变化。它不是宣称此前未做空间学习，也不预告邻点有收益；完整比较及第五次复盘均预定，尚未执行。

C15 方案 `ef10a84`；跨点回归与原尺度/校准规则实现完成，三项新增点位/物理通道隔离、初始 SVD/重载/合成邻点作用及在线批量解检查通过，累计 51 项，lint 通过。模型仍为监督机器学习，准备一次固定两阶段执行，不先宣称空间输入有收益。

以下为此前阶段的冻结记录：

**最新核对：历史预测残差学习已经试过，本轮不新增模型实验，也不重复送6pro。**
用户要求按前述想法尝试或判断是否先送审后，完成[已有训练任务核对](ootang_residual_training_review_2026-09-13.md)。
v1.5已用纯物理OOF监督，v1.19加入起点历史，v2.0直接多步，v2.4联合均值/尺度；
此前“把拟合残差换成预测残差”不足以作为新方向，撤回其新实验优先级。
核对两配置63项来源条目、8张训练查询、4组教师参数与前缀哈希、输出锁顺序及原CSV均值；
零新训练、神经求值和物理调用，位移解析仅至612日。旧教师的未收敛标志、当前h预处理及180日范围保留。
293/376日外推监督尚未被上述查询覆盖，但仅凭未做过不能支持优先启动；当前没有有依据的新候选。
用户已明确现有论文数据固定且为真实值，本轮按原发布数据使用，不要求更换或补采，不再把上游材料作为当前前置条件；
Wang等藕塘数据来源及既有处理链限制保持，不能由用户使用约定改写成新完成的仪器级来源验证。
本次完成即停，只在出现具体新假设且本地无法裁定时考虑定向6pro论证；不改最终293日任务或历史负结果。

**最新状态：用户授权的加性GP v2配对实验、只读复核和报告已完成，ADD效果失败并停止，保留B+。**
[结果报告](ootang_bplus_additive_gp_results.v2.md)依据[冻结计划](ootang_bplus_additive_gp_plan.v2.md)及
[配置](../config/ootang_bplus_additive_gp.v2.json)，完整762日拟合/376日开发，未触碰最终293日标签。
预测四点平均RMSE：ADD **41.7343**、TIME_ONLY **40.7863**、B+ **41.3684 mm**；
ADD平均MAE **33.2878 mm**也高于B+和TIME_ONLY。CRPS **25.2531 mm**、90%区间评分 **229.6315 mm**
优于e0，但对TIME_ONLY的CRPS改善不足1%；14项聚合门槛6项通过，逐点完整保护0/4通过。
不将局部概率改善写成整体成功，也不把TIME_ONLY改称成功候选。

一次8组拟合，360次目标调用、270次迭代，正式运行2026-09-12 18:49:42.889706—18:49:56.084855 UTC，13.1950秒。
独立预算18:37:17—20:37:17 UTC，累计收尾时刻见结果报告；不继承旧预算，剩余时间不转移。
49项来源、全部8组先锁定后评分、重载、独立矩阵、40/20/22760/512行评分复算通过。
首次只读核验的log参数元数据比较相差1.11e-16，修正为按原log/exp表示重建核再完全相等比较；
训练核、配置、模型、预测及数值容差未改，原异常保留，零重训。合成检查原11项，补回归后12项通过。
矩阵乘法警告保留，输出有限并通过独立固定容差复核，未查明底层成因；没有扩边界或更改噪声下界。
配置/授权 `fe07480`、实现 `f73f3d4`、唯一原件 `112fd17`、只读修正 `e332787`、复核图件 `327e521`。
用户执行要求已采纳整份v2计划；本轮完成即停，不重启、换核、自动进入最终293日或重复送审，未push。

**此前工作：用户授权的 B+–GP 单次开发验证已完成并停止；整体未达标，保留 B+。**
[结果报告](ootang_bplus_gp_results.v1.md)依据[原计划](ootang_bplus_gp_plan.v1.md)和用户确认的
[执行补充](ootang_bplus_gp_execution_addendum.v1.1.md)，不回写旧计划或旧负结果。
完整376日四点平均 RMSE **41.3684→41.1976 mm**，MAE **33.2651→32.8855 mm**，
CRPS **28.1156→25.1766 mm**，90%区间评分 **388.4387→263.5318 mm**；
六项平均改善通过，四点均值均不退步，但 **MJ3区间评分/覆盖、MJ1 CRPS/区间评分/覆盖退步**，
只有ATU1、ATU5通过逐点保护，未满足整体条件。不能把这次写成“全部指标都没有改善”，也不能宣布效果达标。

一次4点GP拟合，138次目标调用、108次迭代，正式运行4.3896秒；原B+未重拟合，物理调用0。
12项合成检查、4模型重载、独立矩阵和144项旧对照复算均通过；保存24行指标、12行聚合、13656行点日表及两幅全窗曲线。
矩阵乘法警告保留；所有操作数/结果有限，独立逐项求和通过原容差，未查明底层警告成因，不声称已修复运行库。
配置冻结 `d7c3ea2`、实现 `13eec6d`、原始结果 `cdae749`、验证与图件 `e2307a9`；
本轮没有新增push请求，未推送。

用户本次明确选择GP并确认“采用这套事前固定的补充方案”，授权该机器学习候选，不改称深度学习或PINN。
预算起止2026-09-12 17:32:19—19:32:19 UTC；实际正式运行17:53:37—17:53:41 UTC。
截至17:59:24 UTC收尾记录累计27.09分钟，之后仅文档核对/提交；剩余预算不转为新实验。
严格保留792/376窗口、四点和原标签读取顺序，未读取最终293日标签；不自动重启、调参或再送6pro。

**v2.4 联合概率 ConvLSTM 已完成一次配对实验及复算，未达标并停止；保留 B+。**
完整报告：[v2.4 结果](ootang_convlstm_joint_results.v2.4.md)。两个完整 180 日预测窗的四点平均 RMSE
为 **58.1709 / 43.5275 mm**（P0 **58.0122 / 22.2448**），CRPS 为 **42.1278 / 31.7638 mm**
（P0 **42.0113 / 25.1140**）；拟合平均 RMSE、MAE 也均变差，旧四项严格均值改善 0/8。
JOINT 只在第一窗优于配对 DETACHED，第二窗明显退步，不支持当前联合梯度配置的稳定增益。

用户要求“根据这个计划，一步步进行”后，按[冻结计划](ootang_convlstm_joint_plan.v2.4.md)、
[执行配置](../config/ootang_convlstm_joint.v2_4.json)完成两窗、两臂、三种子固定 e100。
12 组模型重载与 DETACHED 对旧 DIRECT 的均值复现最大差均为 0；没有重拟合 B+。
唯一训练入口完成全部更新后在 P0 单分量读取处报错；原失败记录保留，只读评分修复后复算通过，
新增训练和物理调用均为 0。全过程受 **2026-09-12 15:51:38—16:51:38 UTC** 原 60 分钟时限约束。
研究效果未达标，没有新的效果接受记录，不启动最终 293 日或自动派生新版本。

**此前 v2.3 概率 PINN 的开发均值、概率和神经近似检查均失败，停止状态不变。**
[v2.3 原结果](ootang_probability_pinn_results.v2.3.md)及最终 293 日复核独立保留，不与本次两个旧窗混排。

**此前交付：[两页阶段简报 PDF](../output/pdf/ootang_bplus_process_report_20260912.pdf)。**
按用户要求参照原 `process_report` 版式，汇总已有结果、问题及下一步讨论，不含图件。
措辞已按后续要求改为两人讨论结果的自然口吻，简报不再强调汇报对象的身份。

**此前的方法决策：三篇以内原始论文的方法筛选已完成，当时推荐进入实验的候选为 0 个。**
[决策页](ootang_method_selection_2026-09-12.md)对照旧路线，附与导师讨论短期滚动任务的沟通稿。
建议尚未获导师确认；293 日任务、四点范围与 ConvLSTM/PINN 限定保持，该次筛选没有训练。

**此前收尾：用户要求的冻结 3×2 均值/尺度交叉评分已完成，由本地核对结案，不再送 6pro 重复审核。**
固定 B+ 均值换用保存尺度，开发平均 CRPS 28.1156→26.9647、90% 覆盖率 36.24%→40.23%，
但宽度增加、MJ3 CRPS 变差，均值没有改善；不升级为新主模型或新训练候选。
完整六组合、逐点反例、状态差与审查处理见 [v2.3 结果第 5 节](ootang_probability_pinn_results.v2.3.md)。

| 完整 376 日开发预测指标 | 固定参照 | v2.3 神经主输出 |
| --- | ---: | ---: |
| 四点平均 MAE / mm | B+ 33.2651 | 177.5373 |
| 四点平均 RMSE / mm | B+ 41.3684 | 190.1728 |
| 合并 RMSE / mm | B+ 41.6045 | 200.0195 |
| CRPS / mm | v1.1-e0 28.1156 | 166.6350 |
| 90% 覆盖率 | v1.1-e0 36.24% | 0.00% |

v2.3 训练 792 日，开发预测 2018-09-01—2019-09-11，共 376 日；与最终 293 日是不同窗口。
三种子各 300 次，固定 e300，共 900 次更新。正式 worker 08:44:21—08:47:17 UTC，175.47 秒。
同背景原 C 对照的开发平均 MAE 为 41.1096 mm，也差于 B+；它只作诊断，没有替换神经主输出。
神经/物理解平均绝对差四点为 167.0935/195.8916/123.7453/58.9807 mm，不能称物理近似通过。
拟合段 MAE 9.5813→34.1867 mm；失败并非只有末端，不能用导师尾段容忍改判。

## v2.3 交付与核验

- 授权与[执行配置](../config/ootang_probability_pinn.v2_3.json)：`e735f7f`；用户原话“继续下一步，我批准启动新实验”。
- 完整接口与有界运行实现：`003156d`，包括[数据/重放](../code/physics_guided_pinn/creep_workflow.py)和[运行入口](../code/physics_guided_pinn/run_creep.py)。
  八项相关测试和 Ruff 通过。仅用训练前缀作归一化与标签损失，预测锁定后才作开发评分。
- 原 1168 日完整重放对开发 e0 均值最大差 0；74688 子步记忆重建通过原容差，模型重载四类输出差均为 0。
  初检的一个原 C 容差内负滑移保留，修正仅限对照接口，神经路径仍非负，没有裁剪或放宽容差。
- [只读复算工具](../scripts/verify_ootang_creep_pinn.py)：`7f0ed6d`；复算 53 项清单、388 行指标、18688 行点日表、
  原始神经残差、观测映射和九项验收。指标差不超过 5.68e−14，均值差不超过 2.28e−13 mm。
- [运行产物](../results/ootang_probability_pinn_v2_3/20260912_development/execution.json)、数组、权重、原 C 记录、
  图表、源快照、[验证记录](../results/ootang_probability_pinn_v2_3/20260912_verification.json)及原始日志：`4aad0c9`。
  全部三种子和完整日期保留；没有新案例、预警标签、历史模型重训或 B+ 重标定。

- 6pro 诊断核对及用户后续“补齐……你来决定一下”：采纳停止与初始化解释，
  撤回把“保留 B+ 再学习修正”本身当作新路线的建议；旧 M1/M2 已包含相应思路。
- [冻结交叉评分脚本](../scripts/audit_ootang_creep_cross_scores.py)及三项针对性检查：`6cfbd80`；
  [评分/条件差值/状态差及来源记录](../results/ootang_probability_pinn_v2_3/20260912_cross_scores/audit.json)：`7c9eb3d`。
  原 53 项产物核验通过，366 行原分数最大差 5.68e−14，完整六组合和三种子保留。
  仅读取数组及冻结评分模块，没有新的优化更新、物理求解或最终 293 日训练。

## 方法筛选交付（2026-09-12）

- 授权：用户“进行下一步吧”，承接此前“最多三篇原始论文、对照旧路线、交付一页方法决策”的建议。
  本次是方法研究与文档工作，不继承旧训练额度，也没有新实验预算。
- 输入：项目快照 `68b36c4ce0f6961dc8b24042a80a9f169f29fd43`；旧 M1/M2 源码、v2.0/v2.1 方案、
  v2.3 冻结交叉评分和最终 293 日复核。外部核读 Yang B-PINNs v1、Linka 动力系统 B-PINN v2、Zhou Phys-LSTM。
- 方法：按可学习关系、物理接入、预测时可用信息、均值与概率证据对照；没有训练、改动数据切分或重选模型。
  论文原始链接、具体章节及读取限制列在[决策页](ootang_method_selection_2026-09-12.md)；未复现作者实验。
- 输出与结论：没有足够依据推荐新的长窗实验；建议讨论获得新位移观测后的短期滚动预测。
  这会改变预测信息条件，须另定任务及同信息条件的 B+ 对照；不能自动采用论文的 5 步或改写旧长窗结果。
  决策不等于证明原目标不可能；概率局部收益不能替代均值目标，现有结果仍未获导师验收。
- 核验：新增决策引用的四项汇总数值及 MJ3 反例与冻结 CSV 一致，98 处本地链接有效，`git diff --check` 通过。
  工作于 11:41:28 UTC 开始，截至 11:53:27 UTC 核验完成；没有模型运行或作者实验复现，文档正确不代表方法效果通过。
- 本轮完成后停止方法筛选，不追加论文、训练、诊断或同材料重复送审。沟通稿已交付，没有向导师发送消息。

## 阶段简报交付（2026-09-12）

- 用户要求输出与原 `process_report` 风格相近的简短 PDF。本次仅读取 `bd71024` 对应的
  293 日复核、v2.0、v2.3 及冻结交叉评分的四份 CSV 和结果记录；没有训练或修改切分、选模及结论。
- 输出为两页 A4 纯文字与表格报告；[可编辑源码及编译说明](../paper/README.md)一并保留。
  最终 293 日、两个 180 日及 376 日开发窗分别说明，不直接混排；局部概率改善不替代均值目标。
- 核验：PDF 中 17 处数值语句或表格行与原 CSV 一致，日期长度及比较方向一致；两页均已渲染检查，
  中文提取正常、字体全部嵌入，无溢出或缺字。研究目标仍未完成，后续与导师讨论，尚未发送或获验收。

## v2.4 候选计划（2026-09-12）

- 本次授权仅先交付计划，承接用户明确指定的联合均值/尺度概率 ConvLSTM；不自动执行旧实验。
- 核对旧 M1/M2、v2.0 与 v2.3 训练源码；DeepAR HTML 原文相关方法部分仅支持分布参数联合优化，
  不提供当前四点长窗必然改善的证据。旧 v2.0 尺度使用更早预测误差，因此另设同数据、同初始化的梯度隔离对照。
- [计划](ootang_convlstm_joint_plan.v2.4.md)写明两块均值 MSE、OOF 查询 NLL、唯一 detach 开关，
  固定 e100、两窗三种子和候选/对照；两臂共 1200 次协调迭代，均值与尺度各最多 1200 次更新。
- 来源核对：旧配置 12 项输入哈希一致，两个训练查询的块数/权重与旧 CSV 一致，引用的 RMSE/CRPS 已与原指标核对。
  本次没有训练、模型前向或新物理求解。实现与效果仍未验证，最终 293 日协议及旧负结果保持。

## 预算与停止点

**v2.4 当前执行已停止。** 用户在计划交付后要求“根据这个计划，一步步进行”，
另启本次 60 分钟总时限 **2026-09-12 15:51:38—16:51:38 UTC**；没有沿用旧额度。
配置及授权 `5ff6439`，实现及 17 项相关检查 `293c5d8`，唯一运行原件 `157c467`，
P0 单分量只读评分修复 `ba1e645`，评分及复算产物 `73aad6f`。
正式入口 16:12:59.687—16:14:00.811 UTC，61.12 秒，完成 1200 次协调迭代、均值/尺度各 1200 次更新；
在评分阶段退出失败，错误日志和锁定数组均保留。只读修复/复算 16:17:22.963—16:17:25.384 UTC，
2.42 秒，核验 12 组模型、64 行指标和 21504 行点日表；原训练与固定判据的 AST 一致。
截至收尾记录 16:27:00 UTC，已用约 **35.38 分钟**；最终提交继续计入同一上限。
只读命令复核通过，报告 172 项表格数值/判定与保存结果一致，105 处本地链接有效；两张曲线已目视检查。
本轮不重启训练，剩余时间不转用于下一轮；全窗、三种子与失败证据完整保留。

旧准备额度的累计扣减保留，不因新授权冲销：

| 步骤 | 记账分钟 | 旧额度剩余分钟 |
| --- | ---: | ---: |
| v2.1 原准备（120 分钟起） | 30 | 90 |
| v2.2 修订 | 10 | 80 |
| 首次审查核对与补审材料 | 15 | 65 |
| 只读 293 日核验 | 10 | 55 |
| 293 日实现、导出、报告与提交 | 20 | 35 |
| v2.3 候选与来源核对 | 30 | 5 |
| v2.3 组件实现与检查 | 5 | 0 |

用户随后明确批准新实验，本轮采用此前提出的 **60 分钟总时限：08:24:03—09:24:03 UTC**。
截至收尾记录 08:59:55 UTC，已用约 **35.87 分钟**，收尾提交仍计入同一上限。
训练于 08:47:17 UTC 前完成并停止；剩余时间不用于额外实验，配置不会因失败或版本号重新计时。

11:02:32 UTC 开始的交叉评分收尾由用户本次请求授权，属于已保存结果的核验和记录；
核验/评分计算于 11:08:21.958598—11:08:24.035685 UTC 完成，约 2.08 秒。
没有沿用或延长旧训练窗口，完成后停止，不把局部概率收益转为自动续跑授权。

## 保持的研究边界

- 用户明确导师没有指定尾段日期，“如果尾段预测不准确就先不管”。不再索要截点，不据胜负删日期；
  本次采用完整窗 MAE 和概率门槛，属于候选执行标准，不是导师已确认的验收，不改判旧结果。
- 范围仍为藕塘 MJ3/MJ1/ATU5/ATU1 四点位移概率预测，此前神经候选限定 ConvLSTM/PINN；用户另行授权的GP实验现已停止，保留B+。
- 已有最终 [293 日复核](ootang_293day_prediction_audit_2026-09-11.md)结论不变：M1/M2 均为 e0，
  没有神经均值增益；四点平均 RMSE 9.1242 mm、合并 RMSE 9.4506 mm，M0 概率指标不适用。
- v2.3 的负结果不能推断全部 PINN 无效，也没有机制可辨识性或因果验证。原始日值 as-of 未知、
  既有 h 预处理、给定未来驱动和历史窗口已暴露的限制保持；边际区间不保证联合覆盖。
- 本轮不再自动追加诊断、调权或训练；原 v2.1 与常数力探针继续暂缓。后续取舍须以已交付失败证据为依据。

## 历史查证

- [v2.3 形成时方案](ootang_probability_pinn_plan.v2.3.md)：`c7561db`；入口同步 `226224c`；组件与记录 `2383c6a`、`dbf035e`。
- [293 日复核](ootang_293day_prediction_audit_2026-09-11.md)实现、数据、报告：`785a2b4`、`963ecb2`、`2b446ee`。
- [v2.0 结果](ootang_convlstm_direct_results.v2.0.md)按原两窗判据失败；方案、实现、数据、停止记录：
  `e49ac92`、`b3ba6c7`、`453762f`、`aece1c9`。不与本次 376 日或最终 293 日直接排名。
- [路线复盘](ootang_route_review_2026-09-11.md)保留此前八轮、16 个变体的失败证据。
- v2.1 方案 `1ebb98a`、入口 `d48c454`；v2.2 修订 `e78490d`、入口 `e48d3e1`；补审材料 `ba51304`、`a871680`。
  第二次补审未附数组，本地 293 日复核已找到并核验；原件读取失败不等于未推送。
- v1.26 九个未运行文件已删除，原字节可从 `7fc5f29` 追溯；不恢复为活动入口。
  更早进度可用 `git show dbf035e:docs/progress.md` 查证。


2026-09-13 最终证据收尾：C16 核验 `64d86d5`、C17 核验 `4203bfc`；C17 307,200 次自身校准点位更新单列，均值/raw_sigma 与 C8 恒等对照保持，后期覆盖和区间评分未解决。最终归并方案 `23e8548`、分析器 `3f89430`、独立统计复算器 `4bae0bc`、结果 `840b7e5`、日志补全 `3b981d2`。六图 22 面板已目视，字体最低 7 pt、对齐和碰撞检查通过；112 项区间与完整点日表独立复算通过。原目录/源码/配置不覆盖，未 push。

累计 17 主候选、神经更新 2,850、初始/辅助 Ridge 解 2,640、在线 Ridge 点位解 1,128,960、尺度优化 600（10,270 次调用、8,517 次迭代）、混合权重更新 307,200，C17 校准更新另外 307,200；物理前向 22、标定 0，54 项相关合成检查。完整科学效果和用户/导师验收未成立；停止新候选，整理阶段的完成不等于目标已达成，原 05:27:52 UTC 总截止保持。


05:02:03 UTC 最后交付审计通过，证据快照 `d867fc5`：24 份顶层清单、1,526 个清单成员、1,675 个文件的 Git 字节与哈希、182 个本地链接及全部 29 条主记录的独立核验状态通过。审计脚本及记录保存于最终报告入口。距原起点约 7 小时 34 分钟，原八小时额度尚有约 26 分钟；本次停止新增候选是基于连续效果和后期暴露证据，不虚称预算耗尽。短步长误差反馈的均值正结果保留，概率覆盖与物理额外收益未解决，完整目标不标记完成。没有新训练、物理调用或 push。


原八小时末段新增 C16 回归头信息矩阵的具体证据，修订助手自定的十七轮停止安排，单独冻结 C18；旧校准参数搜索仍停止。C18 05:17:50—05:17:56 一次计算5.9533秒，独立模型核验05:21:57、统计核验05:24:31完成。六图22面板已目视，全部QA通过。新增228,960次信息矩阵点位追加单列，零新均值、概率优化、神经、校准或物理调用，原所有训练累计保持；相关合成检查56项。第六次C16—C18复盘决定保留数值通过的探索候选，正式独立有效性与物理增量不作完成声明，停止新C19。
