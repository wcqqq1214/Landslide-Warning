# 藕塘科研进度

> 更新日期：2026-09-11
>
> 本页只记录当前可执行状态、关键结果、限制与下一步，不是导师报告。完整旧记录可从 Git
> 基线 `6f499cd` 恢复；机器产物、配置和源码始终优先于本页叙述。

## 四点 B+ 概率位移预测：v1.5 样本来源对照完成（2026-09-11）

- 按固定 [学习方案](ootang_bplus_sample_learning_plan.v1.5.md)，复用同一 M1 ConvLSTM，
  比较 IN 拟合误差与 OOF 过去外推误差。均值截止 342/432，随后 90/180 日估计每点常尺度；
  两组标签日期、同日权重和标准化相同，物理教师/均值/尺度按时间隔离，外层评分在预测锁后。
- 两窗×两策略×三种子各 100 轮全部完成，1,200/1,200 次更新，约 248.87 秒。
  无新物理 nfev/前向、无尺度网络或 M2/PINN 训练，无补训/选种子/按外层选 checkpoint。
- 两窗 B+ 平均预测 RMSE 为 58.0122/22.2448 mm；IN 为 62.6232/22.9473，
  OOF 为 58.1161/42.1026。严格四项同时改善 IN 为 2/8（432 日 MJ3、MJ1），OOF 为 0/8。
  OOF 相对 IN 第一窗较好、第二窗明显较差；各组都未稳定改善四点，旧各版负结果保持。
- 预测 CRPS：P0 42.0113/25.1141、IN 46.5537/24.8784、OOF 45.0977/31.7852 mm。
  OOF 的 432 日 ATU1/ATU5 90% 覆盖只有 5.00%/7.78%；612 日 ATU1 覆盖 100% 却使
  均值 RMSE 从 4.264 增至 39.506 mm。P0 是本版另设概率参照，原 M0 概率 NA 不变。
- 物理教师及时间变化之间存在尺度迁移问题：612 日 P0 的 ATU5 由旧窗估 sigma≈140.064 mm，
  下一窗 B+ RMSE 仅 5.022 mm，90% 宽度仍 460.770 mm；未分离教师更换与时间变化的作用。
- 15 项相关测试、Ruff/格式检查通过；12 checkpoint 回放、尺度、分位数、CRPS/误差/验收
  复算通过，179,304 数值最大差约 2.952157e-9，776 个保护文件及 24 源码/测试哈希保持。
  另核对 112 行逐种子指标、14 行修正诊断；八个图面板已查看。见 [v1.5 学习结果](ootang_bplus_sample_learning_results.v1.5.md)。
- 分步提交：`c0a20fd` 方案、`8726a03` 实现/测试、`7141337` 结果；解释和导航另行提交。
  仅本地备份，无关 Vajont 表格未纳入。实施/数值验证完成，精度目标未实现，用户/导师接受另计。
- v1.5 按预算结束。下一步建议固定已存神经权重，另版设计“同一教师/重拟合教师”的同日
  配对回放，再决定受限增量与教师匹配的概率校准；旧教师全目标期需有限新前向，尚未运行。
  不自动加神经训练预算，不将当前小型方程模型改称经典 PINN。

## 四点 B+ 概率位移预测：v1.5 误差结构与样本盘点完成（2026-09-11）

- 后续方案已固定：[v1.5 样本来源对照](ootang_bplus_sample_learning_plan.v1.5.md)。先复用同一
  ConvLSTM 对比 IN/OOF，仅改变教师与误差样本来源；两组标签日期、权重、scaler 和优化相同。
  均值截止 342/432，分别有 90/180 个不同标签日期；随后 90/180 日估计每点常尺度。
  固定 2 窗×2 策略×3 种子×100 轮，共 1,200 次更新、20 分钟，无新物理拟合或 M2/PINN。
  概率尺度从旧教师迁移至重拟合教师，不承诺覆盖保证。此条是执行前记录，尚未实施或训练。

- 用户要求“根据你的建议，一步步推进”。先冻结 v1.5 诊断协议，复用前 792 日输入和
  v1.4 B 的 8 条既有曲线；0 新标定、0 物理前向、0 神经训练，运行约 16.74 秒。
- own-training J0 教师为 252B、342A、432B、612B。MJ3 平均预测偏差依次约
  −44.191、−38.984、+9.369、+50.593 mm；误差方向不能直接跨窗沿用。
  612B 的 ATU1/ATU5 RMSE 约 4.264/5.022 mm，而 MJ3 为 61.046 mm，四点需求不同。
- 16 个所选点窗中，12 个事后去常数/线性趋势可移除 >90% 原误差平方和；它使用预测实测，
  只是误差结构描述，不能称为新模型改善。局部速率和驱动相关方向也会变化，不作因果解释。
- 432/612 截止内完整外推窗口含 180/540 窗口日、180/360 个不同日期；前者纳入截断窗口
  后变为 270 窗口日，不同日期仍为 180。点日、重复日期和时间相关都不能冒充独立样本。
- 8 项相关测试、Ruff 和格式检查通过。全部 32 行逐点指标、5,760 行日误差、128 行相关、
  4 组盘点独立复算，最大数值差 4.547474e-13；753 个受保护文件和 6 份本版源码/测试哈希
  保持；图已查看，封存索引后只读复核通过。见 [v1.5 诊断结果](ootang_bplus_error_structure_results.v1.5.md)。
- 分步提交：`c9862fd` 协议、`7d190af` 实现/测试、`a72ae01` 结果；解释与导航另行提交。
  仅本地备份，无关 Vajont 表格未纳入。实施和算术验证完成，模型有效性/用户接受另计。
- 下一步：固定同一 ConvLSTM 的样本来源对照。均值训练、物理教师和尺度期需一起按时间隔离，
  优先复用现有 342/432 日截止；旧诊断中的末 60 日只是盘点候选。新学习方案另行登记，
  当前尚无本版神经训练；整个研究目标仍在进行中。

## 四点 B+ 概率位移预测：v1.4 两项有限对照完成（2026-09-11）

- 用户在 v1.4 方案提交后授权“继续进行下一步”。按冻结方案实现独立
  `code/physics_guided_optimization_selection/`，运行 A 同前缀 J1 续算、B 固定 J0 内部选模；
  只解析前 792 日，无新增神经网络、概率区间、预警或新案例。
- A：432/612 日分别以 v1.3 B / v1.2 B 为锚点，各续算一次 800 nfev，J1 分别由
  16.367884→16.160747、32.444117→31.497799，下降 1.2655%/2.9168%。均保留续算参数，
  均未达 gtol=1e-7。A 不评价外推，不能据此称预测精度改善或参数收敛。
- B：252/342 日 A/B 四条新链共 10,870 nfev；最终三条预算停止、一条 ftol，0/4 达 gtol。
  复用原 432/612 日配方参数；T 两窗选 B，V 两窗选 A。训练平均 RMSE 分别
  7.1230→9.3051、7.1930→9.9538 mm；预测分别 58.0122→25.4663、22.2448→63.3987 mm。
- 432 日四点预测 RMSE/MAE 均改善，但训练 RMSE 均变差；612 日四点两段误差均变差。
  严格四项同时改善为 0/4、0/4，合计 **0/8**。三个可用内部窗口均偏好 A，也没有保证
  下一外层时段优胜。432 日只有一个内部窗口，612 日三个窗口重叠，不视为独立重复。
- A+B 合计新 nfev **12,470/14,800**、优化/Jacobian 原物理前向 **615,655** 次，
  监控拟合耗时约 **20.1 分钟**，无超时、重试或增加预算。独立检查调用另计。
- 核验：运行前后各 25 项相关测试通过，使用已有 unittest；Ruff 与格式检查通过。
  A/B 分别复算 10/16 条轨迹、333,440/568,064 个子步，保存曲线再前向差均为 0 mm；
  B 的原/独立力学最大差为 2.273737e-13 mm，611 个历史/原件文件、19 份科学/测试源码
  哈希保持。8 条 B 参数来源、锁定顺序、选择与逐点指标独立复算；两幅可读图已查看。
- 产物：[v1.4 实施与结果](ootang_bplus_optimization_selection_results.v1.4.md)，以及
  `results/ootang_bplus_v1_4/20260911_optimization/` 与 `20260911_selection/`。
  分步 commit：`7a6db5d` 实现/测试、`a2299c9` A 结果、`46b4a27` B 结果；本文档另行提交。
  仅本地备份，未 push，无关 Vajont 表格未纳入。
- 当前结论：实施和数值验证完成，**稳定四点提升仍未实现**，用户/导师接受状态待定。
  按固定停止规则结束本版；后续训练内误差结构与概率修正目标须另版设计，未新增训练预算。

## 四点 B+ 概率位移预测：v1.4 检查与方案确定（2026-09-10）

- 用户要求“按照你说的，检查并确定方案”。本轮只检查现有参数、代码和指标，未修改旧科学
  源码，未调用优化器或训练神经网络。依据见 [检查记录](ootang_bplus_optimization_review.v1.4.md)。
- 8 个既有参数、2,158 次物理前向：重复计算差均为 0，目标及 v1.3 optimality 复算相符。
  612 日旧 B 的 J1=32.444117，优于新拟合最佳 41.801142，说明新目标尚未被充分求解。
- 局部差分检查：432 日 C1 A/B 分别有 4/1 列在 h/10 对照下差异超过 10%；612 日各候选没有
  该类列。612 日 C1 B 的 O1_up 塑性位移全训练段为 0，四个相关参数局部导数列为 0；其中
  log_eta 在末阶段仍变化了边界跨度的 55.41%。不能把这种参数变化当成物性学习或全局不可辨识证明。
- 单窗口选模回放：432 日拟合后内部 180 日会选 A，但 A 在 612 日完整训练后的外推 RMSE
  为 63.3987，劣于原训练选择 B 的 22.2448 mm；时序选模不能预先承诺有效。
- 已确定 [v1.4 方案](ootang_bplus_optimization_selection_plan.v1.4.md) 与配置：A 只从同前缀
  最好已知 J1 参数各续算 800，合计 1,600 nfev；B 固定 J0，新拟合 252/342 日各 A/B，
  合计 13,200 nfev，并复用原 432/612 日配方结果比较训练选择和内部预测选择。
- B 的 432 日外层只有 {252} 一个内层，612 日外层有 {252,342,432} 三个内层，各预测 180 日；
  明确重叠日期、按配方重选和层级隔离。总新上限 14,800 nfev、30 分钟硬超时，不自动加预算。
- 检查与计划日期/预算核对完成，525 个原件/历史文件和 13 份科学源码哈希保持；新证据保存为
  `results/ootang_bplus_v1_4/20260910_optimization_review/`，检查产物已 commit `0efb635`。
  方案、检查说明和导航随后单独提交；无关 Vajont 表格仍未纳入。
- 当前状态：检查完成、方案确定；新模块、续算、内部选模对照及任何神经训练**均未执行**。
  本次没有“修复成功”或新的预测效果结论，旧各版负结果保持。

## 四点 B+ 概率位移预测：v1.3 增量目标对照完成（2026-09-10）

- 用户授权在 v1.2 后“进行下一步”。按预先提交的 v1.3 方案，在原位移/末日目标之外加入
  固定权重 1 的 30 日净位移增量项；54 参数、方程、初值、差分/容差、64 子步均保持。
- 只解析前 792 日；432/612 日各从干净 A/B 起点执行四阶段，同预算 C0 复用 v1.2 continued。
  全部参数先锁定，再对各 180 日预测评分；没有新增概率/神经训练，也未延伸原开发/最终段。
- 四条链均完成，约 8.5 分钟，实际新 nfev 13,086/13,200；优化物理前向 623,036 次，
  另有 16 次优化末态复核。最终 1 条 xtol、3 条预算耗尽，0/4 达到 optimality≤1e-7。
- 按各自训练目标选择：432 日 C0/C1 均为 B，训练四点平均 RMSE 7.1230→7.0022，预测
  58.0122→68.8448 mm；612 日从 C0 B 换为 C1 A，训练 7.1930→9.1095，预测
  22.2448→56.4485 mm。只有 432 日 ATU5 同时改善两段位移 RMSE/MAE，合计 **1/8**。
- 同起点配对均已保留。612 日旧 B 按新 J1 复算为 32.444117，优于 C1 最佳 41.801142；
  新优化没有找到优于已知参数的新目标值。432 日 B 的 J1 改善而预测变差，不能仅凭训练目标
  宣布外推改善，也不能由本轮未收敛的有限结果推广为全部增量目标无效。
- 验证：运行前 30 项相关测试通过，运行后新增模块 7 项测试全部通过；12 条轨迹、538,368
  子步通过约束，最大复现差 2.2737e-13 mm，短/长前缀差 0。两幅图已查看。
  427 个原件/历史文件、13 份科学源码、方案和配置哈希保持；11/11 项解释检查完成。
- 产物：[结果记录](ootang_bplus_increment_results.v1.3.md)，及
  `results/ootang_bplus_v1_3/20260910_increment/` 中的参数、预测、状态、指标、日志与完整性索引。
- 分步 commit：`cba8a19` 方案、`fa909b3` 实现/测试、`65e8075` 结果/核验；本条总结与导航另行提交。
  只做本地备份，没有 push；无关 Vajont 表格未纳入。
- 结论：实施与数值验证完成，**稳定四点提升未实现**，用户/导师验收待定。下一步建议先检查
  标定优化稳定性，以已知可行解作基准，并设计训练内部滚动验证的参数选择规则，再决定神经
  增量残差路线；该后续建议尚未执行，不自动增加优化预算或 ConvLSTM/PINN 训练。

## 四点 B+ 概率位移预测：v1.3 增量目标方案已固定（2026-09-10）

- 用户授权“进行下一步”，并要求每完成一个可验证步骤 commit。已固定
  [v1.3 计划](ootang_bplus_increment_plan.v1.3.md) 和执行配置。
- 唯一改动为增加训练期 30 日净位移增量项，按有效日期平均与位移项等权；432/612 日训练、
  各 180 日预测、A/B 起点及每起点 3300 nfev 沿用 v1.2，最大新预算 13,200 nfev。
- 控制组复用经哈希核验的 v1.2 `continued` 结果；新组从干净起点重新拟合。暂不新增神经网络。
- 本条记录对应方案提交时：当时仅完成方案，实施、运行、效果与数值验证尚待完成。

## 四点 B+ 概率位移预测：v1.2 前缀诊断完成（2026-09-10）

- 用户在既有检查点及物理增长速度诊断后授权“进行下一步”。已固定
  [v1.2 方案](ootang_bplus_diagnostics_plan.v1.2.md)，作为 v1.1 之后的独立探索诊断。
- 仅读取前 792 日：432/612 日两个滚动标定起点，各外推 180 日；792 日只检查已有参数续算。
  两个干净 A/B 起点、固定三阶段及一次 800 nfev 续算，最大新预算合计 14,800 nfev。
- 新增独立 `code/physics_guided_diagnostics/`；每折优化器只接收该折训练数组，先锁参数再评分。
  数值管道只解析前 792 日；原件/旧产物的完整性检查读取完整文件字节。
- 执行：三个独立本地进程，约 18.9 分钟；实际新增 nfev 14,457，优化目标/Jacobian 物理前向
  697,007 次，另有 18 次优化末态复核。没有新增神经网络训练。
- 收敛：六次续算的目标改善约 0.10%—1.20%，5 次预算耗尽、1 次 xtol；6/6 均未达到既定
  optimality≤1e-7。612 日 B 的最大参数变化占该参数存储边界跨度 14.44%，不能声称参数稳定可辨识。
- 按预定训练目标均选 B：432 日窗口训练 RMSE 7.1882→7.1230，预测 57.3802→58.0122 mm；
  612 日窗口训练 7.2249→7.1930，预测 23.1308→22.2448 mm；后者仅 ATU1 的预测 RMSE 改善。
  792 日 B 训练 RMSE 12.8324→12.8197，未额外评价原开发/最终预测段。
- 起点诊断：续算后 432 日 A/B 预测 RMSE 为 25.4663/58.0122，612 日为 63.3987/22.2448 mm。
  外推优劣随窗口改变，不能凭训练拟合或追溯挑选某一初始点宣布新方法更好。
- 验证：6 项测试通过；12 条候选、561,408 个子步通过约束，最大复现差 2.2737e-13 mm，
  最大短/长前缀差 0；两幅图逐幅查看。323 个原件/旧产物哈希、科学源码与执行前方案均保持一致。
- 产物：[结果记录](ootang_bplus_diagnostics_results.v1.2.md)，及
  `results/ootang_bplus_v1_2/20260910_diagnostics/` 的优化日志、预测、内部状态、指标和来源快照。
- 结论：本版实施和数值核对完成，**未得到稳定改善四点外推的参考模型**；用户/导师验收待定。
  下一步建议另版有限检验“位移 + 30 日增量”目标，并研究训练内部时序验证的教师选择规则；
  神经网络的增量残差方向尚属待检验假设，不自动补训或改写 v1.1/v1.2。

## 四点 B+ 概率位移预测：v1.1 实施与有限实验完成（2026-09-10）

- 输入：用户 `/goal` 授权按 v1.1 开发；实际核对导师 PDF 第 3、6–11 页及 ZIP 最终交付源码。
  使用同一藕塘物化日序列、四个指定测点；没有新案例、预警或预测期位移反馈。
- 实施：新增独立 `code/physics_guided/` 与 v1.1 配置；完成 B+ 平台接口、前缀标定、M1/M2、
  冻结尺度头、三种子共同选模、最终零轮重训与概率评分。源码/配置快照和完整 checkpoint 保留。
- 物理复现：全 1461 日位移最大差 `6.8212e-13 mm`。开发 A/B 各完成三阶段标定，按第三阶段
  训练目标选择 B（53.4709416，A 为 141.8918384）；B 三阶段均达到评估上限，不称收敛最优。
  优化目标/Jacobian 调用 214,374 次，连同初态/末态检查共 214,378 次前向，约 752 秒。
- 时间与方法：内部训练/开发为 792/376 日，最终训练/历史预测为 1168/293 日；首 30 日预热后
  训练共同分母为 762/1138 日。M2 日内 64 子步、完整历史反向、0.5–2 倍率均保持计划约束。
- 开发结果：两路线各三个种子完成均值 200 轮、尺度 100 轮，均选择 **e_mu=0、e_sigma=0**。
  四点平均 RMSE 41.3684 mm、混合 CRPS 28.1156 mm；已训练 checkpoint 均未胜过零轮。
  M2 只因参数数更少成为同分候选；**保留 M0 为基线**，不称 M2 更有效。
- 最终结果：按锁定零轮配置重新初始化并使用最终训练统计，三组均值数值等同 B+。最终训练
  四点平均/合并 RMSE 为 8.5046/8.8296 mm，历史预测为 9.1242/9.4506 mm；M1/M2 逐点
  均值验收均为 **0/4**。没有依赖最终成绩补训、拉宽区间或改选种子。
- 概率结果：尺度停留在约 8.8296 mm 的常尺度初始化；90% 区间宽度 29.0469 mm，ATU1、
  ATU5、MJ3、MJ1 覆盖率依次为 93.86%、100%、72.70%、100%。平均覆盖 91.64% 不能掩盖
  MJ3 欠覆盖；不称学得时变不确定性。M0 概率指标为 NA。
- 核对：冻结复现、两候选逐子步约束、完整 792 日四方向梯度、M1 分块/M2 独立展开、CRPS
  积分和混合 CDF/指标复算通过；全仓库 268 项测试通过。导出层另外核对失败状态与放大图纵轴；
  预测和指标 CSV 的哈希不变。最新 18 项定点测试和四张图件的逐张目视检查均通过。
- 产物：`results/ootang_bplus_v1_1/20260910_implementation/`，含 94,644 行预测、2,196 行指标、
  204 个 checkpoint、选择表、来源包、日志和四点图；
  [实施与结果记录](ootang_bplus_probabilistic_implementation.v1_1.md) 提供复现命令与结论边界。
- 结论与验收：开发和固定实验已完成，**未得到比 B+ 更好的学习模型**；不能宣称满足导师的
  精度提升目标。历史回测、单案例、参数可辨识性和概率覆盖局限保留，用户/导师验收单独待定。
- 下一步：v1.1 预算结束。后续可讨论前缀参考迁移与残差外推诊断，但新实验需另记版本，不能
  在当前历史段上继续试到达标或覆盖负结果。

## 四点 B+ 概率位移预测：协作说明精简（2026-09-10）

- 用户授权落实 AGENTS 审查建议：合并重复协作、阶段及恢复规则，保留导师六项目标、四个 `@` 引用、科研底线与 Git 约定。
- 明确只读审查只报告发现；仅在任务授权编辑时更新研究记录。文献主张核对原始来源，项目数值与效果结论核对数据和实验产物。
- ZIP 内部文件定位与平台说明集中到 v1.1 第 3.1 节，AGENTS 保留资料入口。此次仅调整文档组织，未改变实验设计、原始资料或模型代码。
- 核对：AGENTS 从 87 行精简至 56 行；四个引用、六项目标和五条 Git 约定与修改前一致，v1.1 第 3.1 节之外全文不变。引用及迁移路径检查通过。

## 四点 B+ 概率位移预测：导师资料与目标固化（2026-09-10）

- 输入：用户要求把导师两份 PDF、ZIP 和六项研究目标写入 `AGENTS.md`，供当前阶段及上下文恢复使用。
- 输出：增加三个原件的 `@` 引用、用途与完整交付目录、导师目标和恢复阅读顺序；沿用 v1.1 为执行入口，区分导师要求、用户已同意的实现选择和待实验验证的目标。
- 核对：三个原件均存在；两份 PDF 内容相同，SHA-256 为 `9b2785eb9c1795afe66a380b0fa972c0c6370953a1febd33fcb68315bbe9c367`；ZIP 为 `57f3e7c68f004a47875c59ee93d38c384900b5263f111341fdd4aee36fdcc966`。已核对 ZIP 内阅读与完整交付入口，避免误用历史副本。
- 状态：仅更新资料入口与研究记录，原件未改动；本轮未解压、运行物理代码或训练模型。下一步仍是数据/评分接口与冻结 B+ 复现。

## 四点 B+ 概率位移预测：执行规格补全（2026-09-10）

- 输入：用户要求根据参考资料和导师意见补齐 plan review 的七项缺口；复核 ZIP 中最终
  `physical_model.py`、`physical_solver.c` 和标定脚本，沿用已核验的论文与数据证据。
- 输出：[实验计划 v1.1](ootang_bplus_probabilistic_experiment_plan.v1.1.md)，替代 v1 成为
  `AGENTS.md` 引用的当前入口；v1 及前期讨论保留为历史记录。
- 补全：固定 M1 的 30 日窗口语义和 20 通道、M2 的 38 维输入/日级修正/完整梯度、统一
  预热计分、开发 B+ 两起点三阶段标定、均值/尺度独立训练与选轮数、混合预测排序、M0
  概率指标 NA、数值验收与独立产物合同。
- 分工默认：导师原 B+ 保持冻结，我方负责开发段辅助标定的执行准备与计算；这是本次计划
  补全的责任安排，不冒充导师已确认或已提供结果。后续若明确由导师交付前缀版本，按同协议接入。
- 状态：仅修订执行计划及引用，未修改模型代码、已完成实验的配置/划分/指标/结论，未执行
  新标定或训练。计划中的初值、网络和容差是计算默认，不是实测物性或效果证据。
- 核对：来源代码、特征维度、日期分母、54 参数两组初值边界、概率公式及文档链接均通过；
  高斯混合 CRPS 与数值积分相符。独立复核后明确区分物理 A/B 标定选择与神经开发选模的
  标签隔离规则。下一步从数据/评分接口和冻结 B+ 复现开始，执行状态另行记录。

## 四点 B+ 概率位移预测：阶段计划归档（2026-09-10）

- 输入：用户同意 M0 冻结 B+、M1 轻量 ConvLSTM 概率残差、M2 方程嵌入小型概率模型的
  有限比较，并要求将刚完成的计划保存为 Markdown，在 `AGENTS.md` 中用 `@` 引用。
- 输出：[实验计划 v1](ootang_bplus_probabilistic_experiment_plan.v1.md) 已作为本阶段工作
  入口；`AGENTS.md` 增加仅在当前阶段适用的引用与使用规则。前期网络讨论保留为依据。
- 计划：内部 792 天训练与 376 天开发预测，最终 1168 天训练与 293 天历史预测；规定
  两条学习路线、三个种子、名义 90% 主区间、验收及停止条件。此处是计划，不是新增结果。
- 分工：开发用 B+ 独立副本重标定仍保留为默认建议及待讨论责任，没有将其写成导师已确认。
- 核对范围：计划内容、日期天数、`@` 路径、Markdown 链接与文档差异；本次不涉及训练、
  物理重标定、模型代码或既有实验产物变更。
- 下一步：按阶段计划准备数据/评分接口和冻结 B+ 复现，执行时记录实际输入、配置及验证状态。

## 四点 B+ 概率位移预测：方法讨论（2026-09-10）

- 输入：用户本轮转述的导师意见、两份内容相同的 B+ PDF、`section2d_v4.zip`、现有
  ConvLSTM 代码和上一轮只读资料审查。当前范围收窄为藕塘四点位移概率预测，暂不开展
  预警或新案例；下面旧实验记录保留原日期和结论，不作为本轮执行主线。
- 已明确：MJ3、MJ1、ATU5、ATU1；沿用 B+ 前 1168 天训练、后 293 天连续预测协议；
  预测期给定实际降雨/库水位且无实测位移反馈；优先讨论小型方程约束模型，90% 主区间，
  同报 80%/95%。物理重新标定的责任由用户留待讨论。
- 核对：现有 ConvLSTM 是 8 点 IDW 网格、过去 7 日到下一日增量、P10/P50/P90 输出，
  预测期每日以真实前一日位移重新锚定。因此保留 ConvLSTM 也需要新版本的数据表示、
  连续预测循环及均值/分布头，旧误差不能直接比较 B+ 连续预测 RMSE。
- 结论：导师允许 ConvLSTM、推荐 PINN，并未指定必须保留 ConvLSTM；网络结构与物理
  约束方式应分开选择。上一轮的小型蠕变修正模型是代理建议，不是导师已指定方案。
- 输出：[网络选择讨论 v1](ootang_bplus_model_route_discussion.v1.md)，记录三种物理结合
  层次、ConvLSTM 适配要求、独立准备步骤与尚未锁定的设计。未新增训练或物理预测结果。
- 验证边界：现有 B+ 参数使用完整训练段，内部再切 ML 开发段不能获得完整混合模型的
  独立验证；后 293 天也已用于历史开发判断。原日序列加工与物理参数可辨识性限制保留。
- 下一步：先准备冻结 B+ 复现及编码器/动力修正的接口，再确定最终网络和时间开发方案。
  尚未修改原模型、原配置、原划分和原结果；本次仅更新研究讨论记录，检查文档差异与链接。

## 阶段提交与协作规则更新（2026-09-05）

- 输入：用户明确说明阶段报告已提交，先前加入 `AGENTS.md` 的导师要求已过期；此次仅修改
  `AGENTS.md` 和项目文件，不修改任何 Skill。
- 修改：删除 `AGENTS.md` 的导师专属约束，补充任务范围、已有授权复用、澄清与批准区分、
  局部阻塞处理、完整交付及相称验证规则；同步项目导航、报告说明和阶段结果入口。
- 状态：旧导师意见保留为历史快照，旧固定路线、里程碑和 Vajont 启动门禁不再是当前指令。
  下文按日期保留的研究记录描述当时实施情况，不自动成为新的任务或待办。
- 证据边界：报告提交状态来自用户说明，不等同于导师验收、论文接收或模型有效性认可。
  未修改数据、源码、冻结配置、指标、模型、实验结果或报告正文；未重训或启动新案例。
- 验证：检查文档差异、旧约束引用和 Markdown 相对链接；本次无实验时间划分或数值变更，
  无需运行训练或代码测试。
- 后续：按用户后续任务确定工作范围，既有结果仍按原版本和证据局限解释。

## 现有实验结论

藕塘的 ConvLSTM–NGBoost–SHAP 自动流程已经完整跑通：8 个测点概率位移预测、预测区间
评价、四项逐点预警指标、H=7 自动未来状态标签、五分类 site NGBoost、NGBoost SHAP、
测点级与滑坡体级逐时五色输出均已有版本化产物。ConvLSTM 主体结构未被替换。

流程完整不等于模型有效。当前 ConvLSTM 的跨时期点预测和区间质量不稳定；固定 NGBoost、
memory 与 residual 三种分类方案均未超过严格因果的 lag-7 状态持续基线。因此当前状态是
“可复算的探索性科研原型”，所有预警结果均为 `formal_warning_output=false`。

## 已实现的方法流程

```text
多源监测数据与逐点运动学特征
  → ConvLSTM 全 8 点 P10/P50/P90 位移预测
  → PICP、区间宽度和覆盖偏差评价
  → 区间、速度、严格加速度、改进切线角四项指标
  → H=7 多测点未来变形代理标签
  → 五分类 site NGBoost
  → NGBoost SHAP
  → 测点级与滑坡体级逐时预警
```

SHAP 解释五分类 site NGBoost 的期望有序等级，作用类似毕业论文中的 LightGBM+SHAP；
它不是 ConvLSTM-SHAP，也不能单独证明物理因果主控因素。

## 三种时间口径

| 口径 | 数量 | 含义 |
| --- | ---: | --- |
| 原始物化序列 | 1,461 日 | 藕塘完整历史输入 |
| NGBoost 模型可用时间 | 861 日 | 2018-02-21—2020-06-30；逐时概率与 SHAP 的模型时间域 |
| v4 透明规则基线 | 514 日 | 2019-02-03—2020-06-30；规则基线自身的有效窗口 |

“所有时刻”必须注明具体分母。NGBoost 另输出 `861×8=6,888` 条测点诊断；v4 输出
`514×8=4,112` 条测点记录。暖启动、缺失输入和未成熟未来真值不能静默删行。

## 已完成的五阶段运行

统一入口的运行记录为 `figures/pipeline/ootang_advisor_demo_run.json`，五阶段合同全部通过，
总耗时约 35.7 秒；该运行未重训 ConvLSTM，也未读取 Vajont。

| 阶段 | 主要输出 | 状态 |
| --- | --- | --- |
| 1. v4 四指标透明基线 | 514 日测点级与 site 双轴时间线 | completed |
| 2. H=7 初始自动标签 | 未来状态标签诊断 | completed，首方案失败结果保留 |
| 3. ECDF challenger | fold-1-only 五级固定边界 | completed，开发期标签门禁通过 |
| 4. 五分类 NGBoost | 概率、颜色、基线比较与分类 SHAP | completed，模型结果为负 |
| 5. 证据包汇总 | 既有图表与 CSV 索引 | completed，不是导师报告 |

相关提交：`a3c956d` 增加藕塘证据包，`6f499cd` 统一当前科研状态与术语。

## 工程精简记录（2026-08-31）

已退役与当前论文证据链无关的 live/prequential/epoch/replay/trusted-time 工程支路，包括其
配置、非默认流水线阶段、专属源码、测试和回顾性 prequential 产物；同时删除四个无生产
消费者的旧 operational 配置。该变更不修改 ConvLSTM、四指标、H=7 自动标签、五分类
NGBoost、分类 SHAP、现有模型或当前实验结果，也不读取 Vajont。旧功能可从 Git 基线
`c9c6917` 恢复。

第一轮精简后统一入口登记 19 个科研阶段，`config/` 保留 17 个版本化文件。第二轮在用户
明确授权且保留 `paper/` 的前提下，进一步退役旧独立回归 SHAP、interval-proxy → auto-V0
→ V5 gate/display、未启用的 ConvLSTM inner-validation/capacity、operational-v2 配置、旧逐日
日期兼容 API 及无消费者产物；统一入口现为 11 个阶段，`config/` 为 10 个版本化文件。

本轮没有修改当前 ConvLSTM 主模型、rolling/seed 诊断、H=7 标签、五分类 NGBoost、分类
SHAP、v4、memory/residual 负结果或 advisor 包，也未读取 Vajont。为保持用户指定保留的
`paper/` 可构建，只留下其直接嵌入的 4 张历史 PNG，并在产物索引中标明它们不是当前证据。
退役执行链可从 Git 基线 `b13eb8b` 恢复；本地另清理约 203 MB 可再生缓存和已删除功能的
孤立虚拟环境。

本轮跟踪差异删除 111 个文件、约 106.5 MiB；大部分行数来自旧预测 CSV，不代表源码规模。
同时修正 v4 PDF 的忽略规则，使 advisor 包实际依赖的 all-station diagnostic 与 full timeline
两份 PDF 能纳入版本控制；其 SHA-256 与既有 advisor manifest 一致。验证包括 256 项全量
`unittest`、Ruff、Python 编译检查、11 个阶段的 80 个现存输出合同，以及保留 `paper/` 的
17 页 XeLaTeX 构建，均通过；未重训模型或重跑科研实验。

清理后复审另修正两项 P3 维护残余：切线角 frame 测试现在只拒绝重复/乱序时间戳，不再把
合法不规则间隔误写成应拒绝；协议辅助模块删除无消费者的 inner/capacity 目录常量。冻结
配置中的两个 `rerun_now=false` 历史决策字段原样保留，50 项相关定点测试与 Ruff 通过。

## 方法实现状态

- 速度按真实相邻时间差计算：`v_i=(U_i-U_{i-1})/(t_i-t_{i-1})`，不再用 30 日位移差。
- 加速度按 `a_i=(v_i-v_{i-1})/(t_i-t_{i-1})` 计算；首个速度和前两个加速度为暖启动。
- ConvLSTM 对 MJ9、MJ1、MJ3、ATU1–ATU5 全部 8 点输出训练、校准、预测段及
  P10/P50/P90；P10–P90 是 80% 预测区间，不称参数置信区间。
- 四项指标全部进入 32 维 site NGBoost 输入，不分主指标和副指标。
- H=7 标签只由未来位移率和未来正速度 Q90 构造；边界只在 fold 1 拟合并固定外推。
- NGBoost 输出 green、blue、yellow、orange、red 五级概率；v4 只作透明诊断基线。
- site 主模型同时使用 8 点；空间拓扑按 O1/O2/O3 分区，不由单点极值代表整个滑坡体。

## 关键结果

### ConvLSTM 三折五种子

| fold | RMSE mean±SD | persistence RMSE | RMSE skill | PICP | width |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 1.970±0.418 | 0.245 | -7.036 | 0.387 | 2.959 |
| 2 | 0.356±0.085 | 0.120 | -1.970 | 0.956 | 1.320 |
| 3 | 0.328±0.008 | 0.340 | 0.036 | 0.754 | 0.476 |

目标覆盖率为 0.80。fold 1 欠覆盖、fold 2 过覆盖、fold 3 轻度欠覆盖；第三折虽有小幅
正 skill，但日增量明显强平滑，不能据此宣称稳定捕捉变形过程。

### 自动标签与 NGBoost

- fold 1 的 site 五级各 56 日；fold 2 为 `138/66/39/21/16`。
- 固定 NGBoost 在 fold 2 的 accuracy、fixed-five macro-F1、ordinal MAE、log-loss、
  Brier 为 `0.3536/0.2871/0.7429/3.3358/0.9960`。
- 共同 273 日上，lag-7 persistence 为 `0.8022/0.6722/0.2234`；固定 NGBoost、memory
  和 residual 均明显落后，后两者已拒绝，停止在已暴露评价折上继续搜索变体。

### NGBoost SHAP 与多点基线

- 当前 SHAP 前四项依赖为 ATU2 切线角 `0.5440`、ATU1 切线角 `0.4815`、ATU5 速度
  `0.2094`、ATU2 速度 `0.1799`；这是失败模型的依赖描述，不是因果主控结论。
- v4 的 514 日中，106 日满足至少 2 点、2 区的跨区确认，8 日仅保留局部 blue 关注且
  整体轴为 green；另有 400 日存在 yellow 及以上局部候选，但未获跨区佐证。
- v4 加速度等级计数为 `4012/98/2/0/0`；A0 阈值是 fit-only 的项目操作化，尚无现场验证。

## 证据入口

- 方法与结果底稿：`docs/ootang_manuscript_methods_results_draft.md`
- 阶段证据边界：`docs/ootang_stage_results_package.md`
- 自动标签与分类协议：`docs/ootang_ngboost_auto_state_experiment_plan.md`
- SHAP 对象与样本：`docs/ngboost_shap_protocol.md`
- 全 8 点预测：`figures/convlstm/forecast_all_stations.png`
- NGBoost 概率、逐时预警与 SHAP：`figures/ngboost_auto_state_classifier_v1/`
- v4 多点透明基线：`figures/warning_operational_draft_v4/`
- 五阶段机器运行记录：`figures/pipeline/ootang_advisor_demo_run.json`

## 导师阶段汇报重写（2026-08-31）

- 输入：导师当前修改意见、`AGENTS.md` 固定主线、现有藕塘方法与结果底稿，以及已经生成的
  ConvLSTM、NGBoost 和 SHAP 产物。本次没有重训模型，也没有读取或运行 Vajont。
- 方法与时间范围：报告统一为 ConvLSTM–NGBoost–SHAP 路线，明确区分 1,461 日原始序列、
  861 日模型输出、其中 840 日成熟 H=7 标签，以及 514 日多测点规则对照；分类结果仍按
  既定时间折评价，没有根据报告呈现效果重新调参或改标签。
- 输出：重写 `paper/process_report.tex`，同步更新流程图、四指标热图和 `paper/README.md`，
  生成当前 13 页 `paper/build/process_report.pdf`。报告以图为主，包括 8 个测点全时段概率预测、
  区间评价、五分类比较、逐时结果、NGBoost SHAP 和全部测点四指标结果。
- 表达：正文和流程图只直接陈述当前方法、结果与限制，删除第三人称的汇报说明、请示性措辞
  和后续工作安排。
- 结论：藕塘流程已经接通，但结果不能写成模型已经有效。ConvLSTM 跨时期表现不稳定，
  NGBoost 在当前评价段明显落后于 lag-7 状态持续基线；报告如实保留这两个负结果。
- 局限与下一步：H=7 仍是代理标签，不是真实灾害等级；目前缺少原始 GNSS 复核材料和现场
  事件真值。先请导师确认自动标签定义及四指标、多测点综合方式，再决定是否整理最终藕塘
  展示包并进入 Vajont 外部验证。
- 核对：报告图件脚本可复现；XeLaTeX 构建通过；13 页逐页检查未发现文字裁切、表格越界、
  图片重叠或缺字。
- 版式调整（2026-09-01）：删除首页题目、姓名和日期，取消全部页面的页眉；正文图表与底部
  页码保留，未改动方法、实验结果或科研结论。
- 图件调整（2026-09-01）：流程总览改为 Draw.io 可编辑源文件，并由 Draw.io 导出报告 PNG；
  只调整视觉层级和连线路径，不修改技术路线、实验数字或结论边界。
- 流程图可读性重构（2026-09-01）：在 Draw.io 中将总览压缩为 5 个主流程框，并把预测区间
  评价、H=7 自动代理标签和 NGBoost SHAP 分列为 3 个虚线支路；删除框内运行日志式细节，
  放大字号并以 200% 浅色 PNG 重新导出。14 页 A4 报告重新构建并检查首页，文字和连线均可读。
  本次未修改模型、数据、时间划分、标签、阈值或结论，也未读取或运行 Vajont。
- 表达收口（2026-09-01）：进一步区分 NGBoost 的 861 日模型输出与透明规则的 514 日诊断
  窗口，补充 32 维四指标输入、273 日共同样本、时间轴前两行含义和 SHAP 解释量定义；
  图内统一使用“滑坡体级 NGBoost”和中文标签。此次只调整报告叙述与图件标签，没有重训、
  改标签、改阈值或改变实验结论；同时删除未引用的 Draw.io SVG 导出物和报告脚本中的无用
  颜色常量，保留可编辑 `.drawio` 源文件与报告使用的 PNG。
- 第 6 部分口径核正（2026-09-01）：明确 514 日来自校准段与测试段，图中“测点融合”是
  各测点内部的四指标最高等级候选，不是跨测点综合；将旧有“114 日均获跨区确认”修正为
  106 日跨区确认、8 日局部 blue 且整体 green、400 日局部 yellow 及以上但未获跨区佐证。
  同时补明区间位置采用单侧偏差诊断，400 日不能解释为 400 次异常或预警事件。本次未重训、
  未改阈值、未改标签，也未读取或运行 Vajont。
- 位移图分页（2026-09-01）：最终从既有 `forecast_predictions.csv` 免训练重绘为 8 张单测点图，
  每点一页；上栏显示全时段累计位移与预测区间，下栏显示测试段日增量。图内不再显示 O1/O2/O3，
  但多测点预警的空间分区逻辑保持不变。持续性基线仍保留在定量评价中。本次未重训或改动结果。
- 位移预测泛化核查（2026-09-01）：当前任务使用过去 7 日预测下一日，并在每个测试时刻以真实
  `U(t-1)` 重新锚定累计位移，因此上栏曲线重合不能作为拟合效果好的证据。当前种子测试总体
  RMSE 为 0.338 mm，持续性基线为 0.340 mm；测试段日增量相关系数为 -0.137，预测增量标准差
  仅为实测的 0.164。三折五种子中前两折均劣于持续性基线，第三折仅小幅优于基线且仍明显
  平滑。现有证据支持“训练期动态未稳定迁移到测试期、跨时期泛化不稳定”，但没有逐轮验证损失，
  不能把原因单独归结为网络容量过拟合；时间分布漂移和累计序列强自相关同样可能参与。本次核查
  未新增调参或训练。
- 位移图诊断重构（2026-09-01）：沿用既有 `forecast_predictions.csv` 免训练重绘 8 个测点，
  每点依次展示全时段累计位移、测试段日增量与 P10–P90 区间、测试段残差与对应区间带。
  本次只改图件，不改数据、模型、时间划分、指标或结论，也未读取或运行 Vajont。
- 论文图件收口（2026-09-01）：输入沿用版本化的 SHAP importance、site/station prediction、
  ConvLSTM 五种子汇总和透明规则逐时 CSV。NGBoost SHAP 改为 8 点 × 4 指标的完整 32 项热图；
  逐时图完整保留 861 日、三个时间折及每折末端共 21 日未成熟标签；验证图补明跨种子 SD；
  四指标图明确校准/测试边界及测点内融合。图件、图注和哈希清单已同步，13 页 PDF 通过构建
  与目视检查。结论边界不变：SHAP 只表示模型依赖，逐时颜色仍是自动代理状态或模型输出。
  本次不重训、不改数值、不读取或运行 Vajont；后续若继续改版式，仍从上述 CSV 免训练重绘。
- README 一致性审计（2026-09-01）：核对仓库全部 4 个 README 及其链接，补入当前
  `process_report`、完整 32 项 SHAP、861/840/21 时间口径和免训练重绘说明；删除“历史图仍由
  当前报告引用”的过时表述，并修正 861 日全部为 OOF 的错误说法。未改方法、数据或实验结论，
  未读取或运行 Vajont。
- 论文式润色（2026-09-01）：以旧 `paper/report.pdf` 作为反面样例，将当前报告改为
  “简短读图提示—图表—结果与边界”的论文叙事，并把四项指标移至 NGBoost 之前。8 个测点图注
  补入各自 PICP，正文直接写明 ConvLSTM 跨时期不稳定及 NGBoost 未超过 lag-7 持续基线；逐时图
  仅从既有 CSV 加高重绘，改善 861 日和 8 个测点标签的可读性。最终 14 页 PDF 通过 XeLaTeX
  构建和逐页目视检查；未重训、未改数据划分、标签、阈值或实验结论，也未读取或运行 Vajont。

## 外部方法文献审计（2026-09-01）

- 输入：当前 `docs/`、`paper/`、`literature/` 与相关 Git 历史；未读取或运行 Vajont。
- 文献范围按用户确认收窄：论文参考文献只保留实际支撑当前路线、方法、公式或评价原则的
  来源，不因某个方案曾经试验过就把其论文写入当前主线。
- Killick et al. (2012)、Truong et al. (2020) 和 Cheng et al. (2020) 只对应已经失败并由
  ECDF v2 取代的 v1“变点 + KMeans”标签路线；Gibbs and Candès (NeurIPS 2021) 只是尚未
  实施的候选方向。它们只留在内部实验史中并标明退役/未实施，不进入当前论文参考文献和
  本轮 PDF 下载范围。
- 区间审查另引用 Gneiting et al. (2007) 与 Gneiting and Raftery (2007) 作为校准、锐度和
  interval score 的评价依据。当前代码确实实施了逐测点 split-conformal 对称扩张，Romano
  et al. (NeurIPS 2019) 可作为相近方法来源，但必须写成项目适配而非完整复现。
- 当前方法来源已补齐并核验，登记见 `docs/current_method_reference_register.md`。本轮仅下载
  7 篇正文 PDF，未下载 SI：ConvLSTM、NGBoost、SHAP、CQR、校准与锐度、proper scoring
  rules，以及一篇分位数回归权威综述；所有 PDF 均通过文件类型、文本、页数与首页抽查，
  并记录 SHA-256。Koenker and Bassett (1978) 与 Shepard (1968) 未找到可核验的合法开放
  正文，保留 DOI、出版社记录和 `metadata_only` 状态，不使用来源不明的镜像。
- Deng et al. (2021) 只有在正文实际用它论证“自动运动状态分类”方向时才保留，不能把它
  写成当前 ECDF、`H=7` 或空间综合规则的直接出处。
- 数据血缘审查引用 Yang et al. (2023, 2024) 核对藕塘监测频率、分区和点位语义；它们不是
  当前模型所借鉴的算法，不纳入方法文献批次。Wang et al. (2025) 本地 PDF 已存在。
- 归档采用双层策略：Git 中记录题名、DOI/arXiv、对当前方法的作用、正文对应位置、合法来源
  URL、本地文件名和校验值；对最终实际引用的方法/评价来源另保留可核查全文。PDF 继续放在
  已忽略的 `literature/`，不提交大文件或可能受版权限制的全文。若没有合法 PDF，则记录可访问
  的出版社/作者全文链接和核验状态，不以未读的元数据代替原文核查。

## 当前限制与下一步

1. 不将自动 H=7 标签写成真实灾害等级，不将 SHAP 写成物理因果证明。
2. 不依据已经暴露的 fold 2/3 继续调参、改标签或筛选模型以掩盖负结果。
3. 用户已确认阶段报告提交；报告生成与核对记录仍按原日期保留，不再作为待提交任务。
4. 若后续开展模型效能研究，应将探索性开发与独立确认性评价分开，另行登记设计和版本，
   不覆盖已报告结果或把已暴露评价数据重新称为未见测试。
5. 旧导师要求及其 Vajont 阶段门禁已退役。后续路线由当前用户任务决定；本次仅更新文档，
   未启动新案例，也未授权依据新案例结果回调既有实验。
