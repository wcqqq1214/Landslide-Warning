# 教师更新配对图件合同

## Material Passport

Python/matplotlib，沿用本任务既有后端和中文科研报告工作流。图件回答“更新历史教师是否产生跨窗稳定收益”；标题与解释以冻结结果表为准，不预设成功。仅PNG300dpi/SVG交付，无PDF报告；如使用同源临时PDF进行字体/碰撞检查，它仅作工具内部QA载体，不纳入交付。

| 图 | 证据角色 | 面板及数据 |
| --- | --- | --- |
| teacher_policy_comparison | 完整均值比较、配对种子效应、逐点失败、概率覆盖边界 | a三个293日窗六方法RMSE；b更新减旧缓存的同种子和集成RMSE差；c最终四点RMSE；d最终四点90%覆盖 |
| cached_final | 旧缓存教师训练的四点完整走势和区间 | 四点2016—2020累计位移，训练段实测与B+，预测段两组均值/本组三种子/80%与95%边际区间 |
| refresh_final | 更新教师训练的四点完整走势和区间 | 与上一图共同坐标范围，突出更新教师GRU与自身原校准区间 |

每幅240×170mm，2×2 quantitative grid，面板a—d。三种子是算法重复，日期/窗口重叠，不画独立重复置信区间、不作显著性标记。灰色物理/简单对照，蓝绿旧缓存组，橙色更新组；相同方法跨图颜色固定。

字段映射：phase_summary的origin/method/rmse映射完整窗口；seed_summary映射同种子配对；metrics_by_point映射逐点；最终means/seeds/sigmas数组与1461行观测映射曲线。累计位移统一减首日实测y0；MAE/RMSE不因平移改变。区间直接mu±z*sigma，无平滑、截尾或缩窄，四点全部保留。历史无保存的神经独立重建，不补画拟合曲线。

图件为既有绘图风格的结构适配，数据和计算另按当前合同实现；原图和旧脚本不改。四点图两组使用共同纵轴，必须容纳全部观测/均值/三个种子/两组95%边界；另保存图形数据、每条线和带的来源表达、SVG实际顶点、字体和1.5pt对齐回执。注释放在绘图区下方，避免与预测曲线/网格碰撞。每张图与每个面板逐一目视；数值核验和目视分别记录。
