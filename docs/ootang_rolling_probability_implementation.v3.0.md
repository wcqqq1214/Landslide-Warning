# v3.0 滚动概率预测实施与核验记录

## Material Passport

2026-09-13；来源：`a24f3e9` 冻结方案与本轮新增代码。状态：数据、模型和流程已实现；合成核验通过，实际效果尚未评估。不是结果报告。

## 已完成的实现步骤

代码位于 `code/rolling_probability/`：

- `data.py`：逐日释放位移；训练查询记录每个物理教师的标定前缀、起点与目标末行。早期四教师对上前次审计哈希，保留原优化器状态。必要的物理轨迹扩展使用冻结参数从零历史重放，不重新标定。
- `models.py`：25 个历史通道、8 个未来条件通道，16 隐通道的卷积 LSTM 编码器和直接高斯解码器，8,370 个参数。四点跨点卷积并不等于联合概率分布；当前只评价逐点边际区间。预测为 B+ 当前观测锚定输出加学习修正，网络初始化严格等于该锚定均值。
- `scoring.py`：Gaussian CRPS、区间评分与成熟误差校准。概率校准使用原始模型标准差计算误差比值；只有目标已经观测到才可用于下一起点，不循环使用已校准标准差。末端不足 30 日时只生成并评分合法步长。
- `run.py`：训练、内部选轮、开发、后期迁移分阶段；每起点锁定预测哈希后释放下一位移。模型、训练记录、所有步长/种子输出、来源快照和运行退出状态保留。全局截止和局部一小时上限都有效；实际执行成功与效果门槛分开。

为保持数组键与数学对照一致，内部 CSV 的 `B_ANCHOR`、`B_TREND14`、`PERSIST`、`DRIFT14` 表示本轮附加高斯分布的对照，呈现时写为 `…_G`；它们不是旧 M0 的概率成绩。`B_RAW` 的概率字段为空，NPZ 中其尺度只是未参与评分的占位数。原始和校准后的候选尺度均保存。

## 开跑前检查

命令：

```sh
.venv/bin/python -m unittest discover -s tests -p 'test_rolling_probability.py' -v
.venv/bin/ruff check code/rolling_probability tests/test_rolling_probability.py
```

10 项合成检查通过：起点/目标索引、观测锚定、未来位移扰动不改变先前特征与实际流式预测、教师前缀隔离、训练目标/标准化边界、固定残差速度解析对照、成熟误差拒绝未来数据、CRPS 独立 CDF 数值积分及梯度、区间评分惩罚、零修正与模型重载。

初版风格检查出现多语句同行，已统一格式；流式 CSV 的未关闭文件提示已修复为末行释放时关闭。正式训练尚未开始，这些不是模型效果失败或修改科学门槛。

下一步：先保存冻结参数重放与来源核对产物，再运行 C1 的内部选择和开发比较。新结果追加到独立结果报告；如果进入第二个候选，将事先单独记录假设与配置。

## 冻结物理轨迹扩展已完成

保存位置：`results/ootang_rolling_v3/20260913/physics_development/`。
5 个前缀各完整重放一次，再做对应前缀的短轨迹核验，共 10 次物理前向、0 次标定。
全部既有重叠位移与冻结输出最大差为 0 mm；短前缀与完整重放最大差 1.14e-13 mm，低于事前 1e-8 mm 容差。
仅解析首日位移作为原点，未来驱动按给定条件读取至 1168 日；没有解析开发位移用于此步骤。
早期四教师与既有来源审计一致，原 342/432/612 日优化器未收敛标志继续保留。
原物理包的矩阵乘法仍出现除零/溢出/无效浮点警告；输出有限、重叠复现和因果前缀检查通过。
这与历史平台记录一致，不声称底层库问题已修复；本步骤没有改动原求解器或放宽容差。

## C1 首次启动的工程异常

2026-09-12 21:50:20 UTC，首次 `develop` 在 0.004 秒内退出，来源快照对相对 `--pool` 路径调用 `relative_to`，与绝对仓库根不兼容。
错误位置在任何训练/标签读取之前，训练更新和神经前向均为 0；不是模型效果结果。
`c1_direct/status.json` 与 `c1_direct_launcher.log` 原样保留。修复将统一路径解析为绝对路径，另用新目录启动，原计划与总截止不变。

相对/绝对输入路径已统一解析，后期引用的开发结果路径也采用相同处理。新增真实来源快照回归检查后，共 11 项相关检查通过，原科学配置未改。

## 独立复算工具

`scripts/verify_ootang_rolling.py` 已实现：核对不可变来源和产物，逐项重建每日起点锁与观测释放链；从保存的原始预测重新计算滑动成熟误差尺度，以独立 erf/NormalDist 公式重算概率与区间评分。再使用运行时保存的源码重载全部检查点、全部起点及三种子输出，不重训或调用物理求解器。
独立 CRPS 的零误差标准正态解析值核验、语法和 lint 已通过；完整 C1 复算接着执行。

首次只读核验在事件链处发现核验器的 JSON 重建问题：内部检查点评分原用整数键排序，读取 JSON 后变成字符串键，重排会改变哈希。现改为核对原始记录字节，全部原事件链可复核；没有改动运行记录、预测、评分、训练源码或科学容差。原核验失败日志保留，使用新核验目录继续。

## C2 组合头实现

按候选事前方案增加七分量 softmax 均值头及固定速度/初始组合对照；原 C1 使用默认分支，原源码快照和输出保持。新增合成核验检查初始组合、凸组合边界、权重梯度非零，以及新增对照不改变原特征/五个对照。12 项相关检查和 lint 通过。独立复算器按运行快照恢复相应组合头，数值容差与原校准规则未改。

C2 完整独立核验已通过，详细计数与零差重载记录见 v3.0 结果页及 c2_verification。新增固定速度对照没有改动原五个对照的预测或尺度。当前两个候选的实现可靠，但效果都没有通过工作标准。

## C3 概率正则回归实现

`ridge.py` 实现六特征 FULL 与四特征 DATA 两臂、无截距 RMS 缩放、按点/步长的正则求解和按点的两参数条件尺度拟合。预测仍走同一逐日起点与成熟误差校准流程；不重新计算物理轨迹。新增独立增广矩阵 SVD 对照检查正则解、解析尺度梯度差分、参数序列化重载与 FULL/DATA 维度隔离，合计 14 项相关合成检查通过。新模型是确定性机器学习，`seed0` 仅作共同输出接口标识，不代表进行了随机重复训练。
