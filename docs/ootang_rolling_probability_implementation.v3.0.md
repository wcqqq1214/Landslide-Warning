# v3.0 滚动概率预测实施与核验记录

## Material Passport

2026-09-13；来源：`a24f3e9` 冻结方案与本轮新增代码。当前 C1—C6 实现、相关合成检查及独立重载/评分核验均已完成；效果见[主结果](ootang_rolling_probability_results.v3.0.md)和[尺度对照](ootang_rolling_probability_scale_study.v3.4.md)，完整效果目标尚未达成。本页按实现提交顺序保留形成时状态，后文的“尚未运行”不是当前待办。

## 已完成的实现步骤

代码位于 `code/rolling_probability/`：

- `data.py`：逐日释放位移；训练查询记录每个物理教师的标定前缀、起点与目标末行。早期四教师对上前次审计哈希，保留原优化器状态。必要的物理轨迹扩展使用冻结参数从零历史重放，不重新标定。
- `models.py`：25 个历史通道、8 个未来条件通道，16 隐通道的卷积 LSTM 编码器和直接高斯解码器，8,370 个参数。四点跨点卷积并不等于联合概率分布；当前只评价逐点边际区间。预测为 B+ 当前观测锚定输出加学习修正，网络初始化严格等于该锚定均值。
- `scoring.py`：Gaussian CRPS、区间评分与成熟误差校准。概率校准使用原始模型标准差计算误差比值；只有目标已经观测到才可用于下一起点，不循环使用已校准标准差。末端不足 30 日时只生成并评分合法步长。
- `run.py`：训练、内部选轮、开发、后期迁移分阶段；每起点锁定预测哈希后释放下一位移。模型、训练记录、所有步长/种子输出、来源快照和运行退出状态保留。全局截止和局部一小时上限都有效；实际执行成功与效果门槛分开。

为保持数组键与数学对照一致，内部 CSV 的 `B_ANCHOR`、`B_TREND14`、`PERSIST`、`DRIFT14` 表示本轮附加高斯分布的对照，呈现时写为 `…_G`；它们不是旧 M0 的概率成绩。`B_RAW` 的概率字段为空，NPZ 中其尺度只是未参与评分的占位数。原始和校准后的候选尺度均保存。

## 开跑前检查

命令：

```sh
.venv/bin/python -m unittest discover -s tests -p 'test_rolling_probability.py' -v
.venv/bin/ruff check code/rolling_probability tests/test_rolling_probability.py
```

10 项合成检查通过：起点/目标索引、观测锚定、未来位移扰动不改变先前特征与实际流式预测、教师前缀隔离、训练目标/标准化边界、固定残差速度解析对照、成熟误差拒绝未来数据、CRPS 独立 CDF 数值积分及梯度、区间评分惩罚、零修正与模型重载。

初版风格检查出现多语句同行，已统一格式；流式 CSV 的未关闭文件提示已修复为末行释放时关闭。正式训练尚未开始，这些不是模型效果失败或修改科学门槛。

下一步：先保存冻结参数重放与来源核对产物，再运行 C1 的内部选择和开发比较。新结果追加到独立结果报告；如果进入第二个候选，将事先单独记录假设与配置。

## 冻结物理轨迹扩展已完成

保存位置：`results/ootang_rolling_v3/20260913/physics_development/`。
5 个前缀各完整重放一次，再做对应前缀的短轨迹核验，共 10 次物理前向、0 次标定。
全部既有重叠位移与冻结输出最大差为 0 mm；短前缀与完整重放最大差 1.14e-13 mm，低于事前 1e-8 mm 容差。
仅解析首日位移作为原点，未来驱动按给定条件读取至 1168 日；没有解析开发位移用于此步骤。
早期四教师与既有来源审计一致，原 342/432/612 日优化器未收敛标志继续保留。
原物理包的矩阵乘法仍出现除零/溢出/无效浮点警告；输出有限、重叠复现和因果前缀检查通过。
这与历史平台记录一致，不声称底层库问题已修复；本步骤没有改动原求解器或放宽容差。

## C1 首次启动的工程异常

2026-09-12 21:50:20 UTC，首次 `develop` 在 0.004 秒内退出，来源快照对相对 `--pool` 路径调用 `relative_to`，与绝对仓库根不兼容。
错误位置在任何训练/标签读取之前，训练更新和神经前向均为 0；不是模型效果结果。
`c1_direct/status.json` 与 `c1_direct_launcher.log` 原样保留。修复将统一路径解析为绝对路径，另用新目录启动，原计划与总截止不变。

相对/绝对输入路径已统一解析，后期引用的开发结果路径也采用相同处理。新增真实来源快照回归检查后，共 11 项相关检查通过，原科学配置未改。

## 独立复算工具

`scripts/verify_ootang_rolling.py` 已实现：核对不可变来源和产物，逐项重建每日起点锁与观测释放链；从保存的原始预测重新计算滑动成熟误差尺度，以独立 erf/NormalDist 公式重算概率与区间评分。再使用运行时保存的源码重载全部检查点、全部起点及三种子输出，不重训或调用物理求解器。
独立 CRPS 的零误差标准正态解析值核验、语法和 lint 已通过；完整 C1 复算接着执行。

首次只读核验在事件链处发现核验器的 JSON 重建问题：内部检查点评分原用整数键排序，读取 JSON 后变成字符串键，重排会改变哈希。现改为核对原始记录字节，全部原事件链可复核；没有改动运行记录、预测、评分、训练源码或科学容差。原核验失败日志保留，使用新核验目录继续。

## C2 组合头实现

按候选事前方案增加七分量 softmax 均值头及固定速度/初始组合对照；原 C1 使用默认分支，原源码快照和输出保持。新增合成核验检查初始组合、凸组合边界、权重梯度非零，以及新增对照不改变原特征/五个对照。12 项相关检查和 lint 通过。独立复算器按运行快照恢复相应组合头，数值容差与原校准规则未改。

C2 完整独立核验已通过，详细计数与零差重载记录见 v3.0 结果页及 c2_verification。新增固定速度对照没有改动原五个对照的预测或尺度。当前两个候选的实现可靠，但效果都没有通过工作标准。

## C3 概率正则回归实现

`ridge.py` 实现六特征 FULL 与四特征 DATA 两臂、无截距 RMS 缩放、按点/步长的正则求解和按点的两参数条件尺度拟合。预测仍走同一逐日起点与成熟误差校准流程；不重新计算物理轨迹。新增独立增广矩阵 SVD 对照检查正则解、解析尺度梯度差分、参数序列化重载与 FULL/DATA 维度隔离，合计 14 项相关合成检查通过。新模型是确定性机器学习，`seed0` 仅作共同输出接口标识，不代表进行了随机重复训练。

独立复算器已补齐 C3 支持：从冻结 JSON 重载确定性模型，重新核对各训练前缀与 RMS 缩放；每个正则解用独立增广 SVD 复算，完整起点重建均值/原始尺度，并独立核对所有固定速度对照公式。不会重跑方差优化器或训练模型。

## C4 因果覆盖反馈实现

按 C4 事前方案给成熟误差校准器增加可选反馈状态，默认分支保持原 RMS 规则。每个已兑现误差必须提供当时发出的 sigma，拒绝未来目标和重复反馈；记录每个起点的对数尺度、最终状态、更新次数和边界命中。原均值与原始尺度不进入反馈更新。

开发入口新增冻结模型复用：核对 C3 产物清单全部哈希，复制 792 日模型/尺度/选模记录，重新逐日发出预测，最后精确比较全部原始输出。没有重新训练或选 alpha。独立复算器自行重建此前已发 sigma 及延迟反馈，再核对最终状态与完整评分，不把保存反馈值当作复算输入。

17 项相关合成检查及 lint 通过，包含反馈正负方向、实际发出区间、重复/未来目标拒绝、上下界与原 RMS 队列保持，以及顺序数据流中扰动未观测位移不能改变当前预测/区间。正式 C4 效果尚未运行；实现通过不预示覆盖达标。

## 后期物理准备

C4 开发与独立核验完成后，按迁移选择锁定准备六个前缀教师到 1461 日已知驱动。本次 12 次物理前向、0 次标定，累计 22 次前向；所有数组有限，既有重叠位移差 0，短前缀最大差 1.14e-13 mm。1168 日参数来自原交付包 `results/calibrated.json`，与旧最终物理曲线完全一致。只读取首行位移锚点及完整驱动，没有在准备步骤解析后期位移标签；原矩阵运算警告及未收敛历史标志保留。来源和 SHA-256 见 physics_transfer/provenance.json 与 source_provenance.json。

## 配对统计与图件工具

`scripts/analyze_ootang_rolling.py` 从冻结预测产生全步长、逐点、每 30 日一个起点的非重叠表，以及事前固定的 2,000 次循环移动块配对区间。四点与各模型共享抽样起点，平均点 RMSE 与合并 RMSE 独立保留。重新核对全部汇总与 27 项效果判断，B_RAW 不补概率成绩，确定性 seed0 不冒充随机重复。

工具同时绘制完整日期的 30 日预测/90% 区间、误差曲线与 1—30 日步长指标。解析标准正态 CRPS、平均/合并 RMSE 的已知例、恒定误差的成块重采样及连续块索引检查通过，lint 通过；没有新训练或物理调用。实际统计结果与图件需运行后另行核对。

## C5 开发尺度训练对照

按 C5 冻结方案新增 `crossfit_scale.py`：构造不跨历史折末端的查询，342/432 日辅助模型只拟合一次，612 日直接复用旧模型；内部/开发均值与基线尺度从 C3 复制，只有主候选的两个尺度参数用历史预测误差重估。保存同均值/原尺度的 IN_SAMPLE 对照，并在开发结束检查其全部分布与 C4 精确一致。

迁移入口对 `transfer_permitted=false` 在读取任何迁移标签之前拒绝执行。来源快照增加对复用内部模型和 C4 对照哈希的记录。20 项相关检查、lint 通过，包括历史折边界、未观测位移扰动、尺度拟合不改变均值、禁止迁移读取。正式 C5 未运行，旧 C4 结论及唯一后期选择保持。

C5 已完成一次运行，独立复算器增加辅助模型的增广 SVD 解核对、各历史折的起点/目标/物理教师边界、预测误差逐项重建及尺度 NLL/解析梯度独立计算。主模型均值与 C3、开发 IN_SAMPLE 全分布与 C4 逐项比较；不重跑优化器。语法/lint 通过，完整核验接着执行。

## C6 最近完整折

在 C5 基础上仅增加按结束日期取最近已完成折的规则，内部/开发分别使用 432→612、612→792。辅助模型从 C5 不可变产物复制，新增辅助拟合为 0；原 C5 默认仍使用全部合格折。独立复算器分别推导所需模型与查询日期，不依赖运行器保存的选择结果。新增乱序折列表、未完成折排除及无合格折检查，合计 21 项相关检查与 lint 通过。后期入口禁止状态保持，C6 尚未正式运行。

上述实现提交 `fb8c008` 后，C6 已完成一次开发运行 `295f399` 及独立核验 `8f0d999`。最近折、四个辅助模型、302 个历史查询、主均值不变、开发对照全分布不变、成熟误差反馈和全部评分均核对通过；未新拟合均值或运行后期。实现与核验收尾，不追加测试或重训来替代效果证据。

## C7—C9 追加探索实现与核验

C7 经验分布与同池高斯、C8 成熟监督在线 Ridge、C9 分步长尺度均已完成一次运行及独立核验。实现与方法细节分别见 [C7](ootang_rolling_probability_extension.v3.6.md)、[C8](ootang_rolling_probability_candidate.c8.md)、[C9](ootang_rolling_probability_candidate.c9.md)。原源码快照与来源不改，新入口不会打开 C5/C6 禁止的迁移流程；新增后期明确为暴露后的探索性复核。

新增核验分别使用 CDF 积分、独立批量/SVD 解、独立 NLL 复数步长导数；三轮原始预测锁、成熟目标和评分均通过。至 C9 累计相关合成检查 31 项，完整数值仍未过，不能用实现通过代替效果。当前决定见[第三次复盘](ootang_rolling_route_review_2026-09-13.md)。

## C10 指数遗忘对照

`OnlineRidge` 新增默认 1 的遗忘因子；原 C8 默认分支保持，C10 只降低已经到期监督的旧权重，并保留固定正则矩阵。新增三项独立加权 SVD/无效监督/默认行为检查，累计相关合成检查 34 项。新入口复用 C8 已核验特征和原始尺度，全部原对照保持，源码快照分别保存。

一次 C10 运行与显式指数权重批量解、600 次加权 SVD、完整因果链/评分核验均已完成，详见[C10 记录](ootang_rolling_probability_candidate.c10.md)。新增 153,600 次在线求解，累计 307,200；没有新初始 Ridge/尺度/神经或物理拟合。效果差于 C8，停止该遗忘配置，不用核验通过替代效果。

## C11/C12 概率混合与权重学习

两轮均已完成并独立核验，详见 [C11](ootang_rolling_probability_candidate.c11.md)、[C12](ootang_rolling_probability_candidate.c12.md)。C11 的 CRPS 二次式封闭权重和 C12 的固定 101 点区间风险网格分别只读成熟监督，核心均值和组件尺度完全冻结，来源与逐日锁完整保留。80/90/95% 区间与 CRPS 使用真实混合分布；冻结 C11 对照也不误用其矩尺度作高斯评分。

新增合成检查累计 42 项。C12 独立重建 15,513,600 个网格损失值与 153,600 次点位权重，已发权重差 0；真实 CDF 与独立积分、全部原分布和评分通过。实现通过而效果未过，两个固定核心混合配置已停止，第四次路线复盘完成。源码/配置/旧产物保留，累计混合权重更新 307,200，不与 Ridge 系数更新混计；物理调用不增加。

## C13/C14 成熟误差均值头

`innovation.py` 只用同步长已经兑现的核心/参照误差学习未来核心偏差，零初始、固定 alpha=1、原概率宽度不变。C13 使用每次已发尺度，C14 独立配置固定首份合法尺度作误差单位；原源码快照和默认行为保留。四项时序/SVD/完整回放与两项固定单位检查通过，累计相关合成检查 48 项。

两轮共 438,480 次成熟点位求解；独立复算均通过，各核验 2,160 次 SVD，C14 另精确核对 720 个固定常数。所有输入、预测锁、完整均值/评分和原尺度/对照保持均核对；效果均未超过原核心，停止单位、惩罚和滞后阶搜索。实现可靠不等于误差记忆能产生收益。详见 [C13](ootang_rolling_probability_candidate.c13.md)、[C14](ootang_rolling_probability_candidate.c14.md)。
