# 历史预测残差训练核对：这条思路已被尝试

日期：2026-09-13（北京时间）。证据快照：`6c86d451287cfd72e669b778c637c04867b3de8a`。
使用 academic-research-suite 辅助解释；本次为已授权的本地来源/训练任务核对，不运行模型实验。

## 1. 决定

**不重跑“把拟合残差换成历史预测残差”这条泛化建议，也不为确认同一事实再送6pro。**
本地代码、查询表和保存结果足以确认：v1.5已经用纯物理OOF残差训练均值；
v1.19随后加入起点历史，v2.0直接多步，v2.4联合均值/尺度，仍未稳定同时改善均值与概率。
此前把这条思路作为潜在新方向，依据不足，应撤回其新实验优先级。

本轮找到的是旧实验覆盖范围与新任务的边界，没有找到足以启动新训练的具体收益依据。
**“293/376日这一组合还没跑”只代表未验证，不代表值得优先跑或会改善。**
保留B+、GP v1的局部正结果及ADD、ConvLSTM/PINN的失败记录；不把旧180日结果改判成293日结果。

## 2. 用户最新约束

用户明确资料固定、当前值是真实值，来自论文配套数据；不更换数据、不要求补采，
也不再把补齐原始观测链当成当前工作前置条件。
上轮核对已定位为Wang等（2025）的藕塘数据，Liu等煤矿论文不是本任务的数据来源；
[既有来源记录](ootang_data_lineage_expert_review.md)保留，不重复执行数据结构诊断。
这里落实用户的使用约定，不把它写成新完成的原始仪器处理链验证，也不回写历史来源限制。
本任务仍为藕塘同四点、B+物理引导、均值与概率共同改善，最终293日协议保持。

## 3. 已经做过的训练方式

| 版本 | 训练残差及实际区别 | 不能误称 |
| --- | --- | --- |
| [v1.5](ootang_bplus_sample_learning_results.v1.5.md) | IN/OOF同标签日期配对；OOF均值监督全部来自更早B+教师的外推误差；随后隔离尺度期 | 没有用过纯预测残差；但该版尚无后来的起点残差历史/lead表达 |
| [v1.19](ootang_bplus_origin_learning_results.v1.19.md) | anchor拟合块 + 起点B+教师的OOF块；30日历史残差/日差和lead；配对日期相同 | 全组都是纯OOF，或神经模型没见过这些训练目标 |
| [v1.21](ootang_bplus_balanced_origin_results.v1.21.md) | 同一来源/样本，逐步平衡拟合与OOF梯度 | 从未处理两个目标的尺度失衡 |
| [v2.0](ootang_convlstm_direct_results.v2.0.md) | 同一混合样本，ConvLSTM历史编码直接到每个未来步；尺度使用较早神经模型锁定后的预测误差 | 只学习B+拟合误差，或从未使用完整模型的历史预测误差训练尺度 |
| [v2.4](ootang_convlstm_joint_results.v2.4.md) | 同源均值块 + OOF行高斯NLL，JOINT/DETACHED配对；均值与尺度共同更新 | 首次提出联合概率训练；或OOF训练NLL等于独立校准 |
| GP v1 / ADD v2 | 使用792日前缀中排除30日预热后的B+标定残差；376日开发 | 把GP的训练方式概括成所有旧神经模型的训练方式 |

v1.5“纯OOF”只指均值监督使用的B+来源；共同预处理仍按该神经训练前缀确定，
不代表旧时点已经独立生成并封存了整套神经系统。v1.19后的OOF组还保留共同anchor块，
这两者须区分；仅建议去掉anchor也不是完全未经尝试的研究方向。

v2.0尺度训练更具体地使用：前缀432的尺度取342神经模型的[342,432)90日误差；
前缀612取432神经模型的[432,612)180日误差。早期均值锁定在后期观测读取之前。
这些误差来自完整均值模型，但尺度仍需迁移到更长前缀的重新训练模型，未证明跨模型校准稳定。

## 4. 样本和物理教师逐项核对

按独立日期枚举核对v1.19的`pairs_h.csv`、v2.0训练`queries_h.csv`及v2.4对应查询，共8张表。
v1.19的`queries_h.csv`实际是评价查询，不能与其训练`pairs_h.csv`混用；本次草稿读取纠正在回执中保留。

| 神经训练前缀h | anchor行 | OOF行 | OOF不同目标日 | OOF教师/起点 | 最大预测步 |
| --- | ---: | ---: | ---: | --- | ---: |
| 342 | 136 | 90 | 90 | 252 | 90 |
| 432 | 188 | 270 | 180 | 252、342 | 180 |
| 612 | 291 | 540 | 360 | 252、342、432 | 180 |

每个OOF行teacher=o、目标t≥o、历史观测截止o−1；重复目标日按原权重分摊，
两块权重分别为0.5。h=612的540行不是540个独立观测，更不是540个独立预测起点。

四个教师252/B、342/A、432/B、612/B的训练标签/驱动哈希与对应真实前缀相符；
保存轨迹的54维theta与来源参数逐值相同。A/B按各自训练J0选取，不使用未来预测胜负选教师。
参数前缀身份通过不等于全局优化收敛；原342/432/612记录的未成功优化标志保持，不据此自动重拟合。

输入构造仅使用`labels[:origin]`，未来残差通道置零；物理驱动仍是给定未来雨量/库水位。
当前h的共同scaler与神经权重可利用h内训练样本，因此不能把每条早期OOF查询冒称完整独立在线预测；
真正的外层冻结预测是[432,612)、[612,792)，预测期间不注入新位移。

已核对v2.0/v2.4两份配置共63项哈希条目、4组教师参数/训练前缀、
8张训练查询和输出锁日志。只读取前612日位移用于前缀哈希；未读最终293日标签，
没有神经前向、反向、优化器更新或物理求解。完整[机器回执](../results/ootang_residual_training_audit_20260913/audit.json)保留来源SHA和计算方式。

## 5. 同窗已有结果

均为完整180日、四点指标算术平均，单位mm；前缀432/612分别对应两个不同预测窗。
这些由原逐点CSV重新求平均，并非本次模型重载或重新训练结果。

| 方法 | 432窗RMSE | 612窗RMSE | 432窗CRPS | 612窗CRPS |
| --- | ---: | ---: | ---: | ---: |
| B+ / P0 | 58.0122 | 22.2448 | 42.0113 | 25.1140 |
| v1.5 纯物理OOF | 58.1161 | 42.1026 | 45.0977 | 31.7852 |
| v1.19 起点历史OOF | 59.6529 | 33.6124 | 43.5597 | 27.1666 |
| v1.21 梯度平衡OOF | 59.1106 | 26.9027 | 43.4738 | 27.3780 |
| v2.0 直接多步 | 64.4117 | 22.1677 | 47.2449 | 26.9716 |
| v2.4 联合概率 | 58.1709 | 43.5275 | 42.1278 | 31.7638 |
| v2.4 隔离梯度对照 | 64.4117 | 22.1677 | 48.9543 | 23.0039 |

部分窗口/点位和对照有局部收益，原结果与原逐点判据保持。
不能从上表推断所有残差学习无效，也不能把某个点/对照事后改称新主模型。
v1.22已做同lead、过去候选的相似输入残差迁移诊断，v1.23已核对教师与时期混杂；
这两项也不作为待重复启动的检查。其限制按原报告保留，不把相似性或局部梯度当因果证明。

## 6. 尚未覆盖的差异及是否值得启动

| 差异 | 当前判断 |
| --- | --- |
| 把现有180日网络直接拉长为293/376日 | 旧输入接口和lead固定为0—179，轨迹只保存到每教师后180日；这会产生未验证的步长迁移，并非修复遗漏的残差训练 |
| 重新构造293/376日的历史外推监督 | 确为上述旧查询未覆盖，但需要另定物理轨迹、历史起点、共同拟合目标和比较协议；目前无正证据表明拉长监督能解决旧180日不稳定收益 |
| 将纯OOF、起点历史与联合概率再次组合 | 精确组合可能不同，但各组成与相关失败已有记录；不能仅凭组合未跑就给出优先级 |
| 增加教师参数/重新检查输入相似性 | v1.22/v1.23已有证据，教师与时期绑定，有限起点不足以支持强因果解释；不作为自动追加诊断 |
| 因噪声下界/优化标志继续调参 | 没有独立开发收益依据，不自动扩边界或续算 |

因此，本轮推荐启动的新模型实验为**0个**。这是当前投入取舍，不是数学上证明原目标不可能。
停止理由是尚无充分的新实验依据，不是资料必须更换、等待6pro许可或旧实验预算不够。

## 7. 下一步与完成边界

本次本地核对完成即停，不把“还有未试组合”自动变成新实验。
不需要6pro重复确认代码里是否用过预测残差；此事已可本地裁定。
若后续提出一个具体、可能改变取舍的新假设而本地证据无法判断，
再请6pro围绕该假设定向论证，必须附本报告所列旧尝试，允许其结论仍为“不建议训练”。

现阶段可带现成结果讨论：在固定数据、四点和293日目标下，是否存在有独立依据、
尚未被旧方案覆盖的具体误差机制；如果没有，暂缓模型搜索。这里没有新增物理假设或训练计划。
数据和全部旧计划/源码/产物保持；本次仅新增核对回执与说明、同步当前入口，分步commit，不push。
