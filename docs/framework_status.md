# 研究框架覆盖与实现状态

> 更新日期：2026-08-08。状态分为“已实现”“部分实现”“未实现”。模型数值来自当前探索性时间留出结果，不等同于最终确认性评价；历史段落保留原始日期和快照语义。
>
> **状态更新（2026-07-18）**：本表的大部分内容仍是导师修改前的实现快照，不能作为本轮正式方法协议。四级动态 V0、V0 主判、NGBoost 和旧融合规则只说明遗留实现；当前独立 SHAP 已改用逐点速度增量 `ΔV`，但仍不构成正式五级预警。当前行动计划与导师指定论文优先。
>
> **数据血缘门禁（2026-07-28）**：当前输入为 Figshare 发布物化日序列，位移与 GWT 具有强自然月分段三次指纹，原始锚点、生成算法和未来信息使用状态未解决。以下数值均为物化序列内部结果，`data_gate=blocked`，不升级为正式日预测、`V0` 或预警证据。
>
> **原型初跑更新（2026-07-30）**：原始 GNSS 已确认无法取得。兼容字段 `data_gate=blocked` 仅指确认性证据和正式预警；当前另设 `prototype_run_gate=allowed`。`elev_m` 已作为静态高程通道进入 7 通道 ConvLSTM，三阶段最小链路已跑通；旧滚动/种子/早停/容量产物仍属于加入高程前的 6 通道历史版本。
>
> **fixed-120 诊断更新（2026-08-04）**：当前 7 通道版本已经按冻结协议完成三个扩展窗口折和五随机种子诊断；历史根目录的 6 通道文件仍保留为旧版本。7 通道早停和容量敏感性没有运行，且本轮结果只支持内部探索性诊断，不解除数据血缘、正式阈值或外部泛化门禁。

> **当前树同步（2026-08-13）**：默认入口为 `features → convlstm → ootang-operational-v4`。旧 30 日 `V0`、旧融合及 v1/v2/v3 运行入口已经从当前工作树移除，仅在 Git 历史中保留；v4 当前使用中性命名的双轴空间融合模块。独立解释改为 NGBoost 回归 + SHAP，不再保留旧同日 V0 分类支路。v4 仍是 prototype/non-formal，正式 NGBoost 仍因缺少独立五级结局标签而未实现；本更新不启动 Vajont，也不解除数据门禁。

历史工程验收：新增 `shap-stability` 后曾有十三阶段统一入口 13/13 通过、65/65 个产物哈希的记录（`figures/pipeline/latest_run.json`）；`figures/pipeline/shap_stability_run.json` 同样仅记录 2026-06-23 的历史单阶段运行。2026-07-18 的 `ΔV` 对齐 SHAP 已按单阶段脚本重算并写入新的溯源/测点分层产物，尚未将其表述为新的完整管线验收。工程通过只证明架构可端到端执行，不等同于阈值、模型或预警效果已获确认。

## 1. 指标与方法覆盖

| Framework 内容 | 状态 | 当前实现 | 主要缺口 |
| --- | --- | --- | --- |
| 库水位 | 已实现 | `RWL` 进入 SHAP、ConvLSTM 和 NGBoost；已纳入原始环境组消融 | 单变量及跨外部时期稳定性未验证 |
| 库水位速率 | 已实现 | `RWL_rate` 进入三个模型阶段 | 不同调度阶段稳定性未验证 |
| 降雨影响 | 已实现 | 日降雨及 7/15/30 日累计滞后进入候选集 | 缺事件级和窗口敏感性分析 |
| 地下水位 | 已实现 | GWT 滞后量及变化率进入 SHAP 候选集和对应组消融 | 单变量贡献不稳定，仍需物理证据核对 |
| 位移速率 | 已实现 | 8 测点速率进入 SHAP；NGBoost 使用聚合量 | ConvLSTM 未显式使用速率通道 |
| 旧位移加速度（已弃用） | 已在当前 SHAP 路径替换 | 当前独立 SHAP 使用 8 测点逐点速度及 `ΔV_i=v_i-v_{i-1}` 滞后量；旧 `disp_accel_lag*` 产物仅保留溯源价值 | 这只是解释输入对齐，不冻结 `ΔV≈0` 容差或正式预警规则 |
| 改进切线角 | 部分实现 | 已分开保存原文严格边界的原始角阶段等级，以及因果平滑和持续性工程等级；预设参数敏感性已完成 | 自动等速段对候选窗口不稳健，仍需专家复核和事件增益验证 |
| 旧多指标融合（已弃用） | 历史实现 | V0 主判、切线角升级复核、NGBoost 概率旁证 | 本轮须以四项并列、透明且非监督的规则融合重新定义并冻结 |
| SHAP 指标解释 | 已实现（内部探索性） | 独立 NGBoost 的模型/目标/样本边界、五折特征/组排名、方向、测点分层时间稳定性和五组删组消融均可审计 | 相关滞后量贡献可分配；不是 ConvLSTM-SHAP、因果结论或新时段/跨滑坡确认 |
| ConvLSTM 预测区间 | 部分实现 | 7 通道模型已输出原始 P10/P50/P90、conformal 区间、三个扩展窗口折和五种子诊断；日期块区间、内层早停和 2x2 容量/正则化诊断另有历史 6 通道产物 | 7 通道 fold 1/2 仍稳定不如持久性，fold 3 的小幅优势伴随方差强收缩；当前不启动 7 通道早停/容量，外部稳定性未验证 |
| 数据血缘 | 阻断 | 已核验 Figshare 文件一致性、48 个月自然月三次指纹、三个模型边界和预测日期键集 | 原始观测锚点、日值聚合/QC/插值、未来锚点使用及 MJ/ATU 映射未恢复 |
| 旧动态 V0 状态（已弃用） | 历史实现 | 8 测点旧 V0 四级标签、融合输出及 9 组预设敏感性分析；一级 V0 沿用毕业论文摘要的匀速变形段速率统计表述，已与 Chen（2024）高等级倍数来源区分 | 必须按当前行动计划与导师指定论文重新定义、冻结并重算；旧产物只保留溯源价值 |
| 旧 NGBoost 当日分类（非正式路径） | 历史实现 | 四分类概率模型已实现 | 不作为本轮正式融合；留出段无 orange/red，且未超过持续性基线 |
| 未来 onset 预警 | 部分实现 | 1/3/7 日标签、连续事件清单和样本盘点已输出 | 仅 3 个可预测事件，不足以支持稳定滚动调参 |
| 自适应在线预警 | 未实现 | 当前为离线等效计算 | 缺实时接入、滚动阈值和漂移检测 |

## 2. 当前探索性结果

### 2.1 ConvLSTM（8 测点）

本节下方直到容量诊断的数值为加入高程前的历史 6 通道记录，保留用于溯源，不能当作当前 7 通道结果。当前 7 通道 fixed-120 的直接证据如下。

#### 2.1.1 当前 7 通道 fixed-120（三折 × 五种子）

| fold | 持久性 RMSE (mm) | 模型 RMSE，均值 ± SD (mm) | 正 RMSE skill 种子 | 校准 coverage | 增量相关 | 增量标准差比 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 0.245 | 1.970 ± 0.418 | 0/5 | 0.387 | 0.202 | 6.933 |
| 2 | 0.120 | 0.356 ± 0.085 | 0/5 | 0.956 | 0.187 | 3.668 |
| 3 | 0.340 | 0.328 ± 0.008 | 5/5 | 0.754 | -0.041 | 0.156 |

fold 1/2 对所有种子均未超过持久性基线；fold 3 的小幅误差优势伴随约 84% 的增量方差收缩和接近零的平均相关，不能解释为动态触发捕捉。三折区间分别欠覆盖、过覆盖和轻度欠覆盖。完整结果、版本对照和表述边界见 [`ootang_convlstm_elevation_fixed120_review.md`](ootang_convlstm_elevation_fixed120_review.md)。

#### 2.1.2 历史 6 通道（单次 287 日留出及后续诊断）

| 指标 | ConvLSTM | 持久性基线 | 判断 |
| --- | ---: | ---: | --- |
| RMSE | 0.318 mm | 0.340 mm | 降低 6.5% |
| MAE | 0.159 mm | 0.181 mm | 降低 12.0% |
| P10-P90 覆盖率 | 73.4% -> 75.2% | 不适用 | 校准后仍低于名义 80% |
| 平均区间宽度 | 0.415 -> 0.432 mm | 不适用 | 校准扩张 0.017 mm |
| 平均 pinball loss | 0.063 -> 0.062 mm | 不适用 | 校准端点仅作诊断 |
| 80% interval score | 1.094 -> 1.068 mm | 不适用 | 校准后改善 2.4% |

仅在该最后留出折中，8 个测点的 RMSE 均低于持久性基线；MJ9/MJ1/MJ3 校准后覆盖率仍只有 47.4%/60.6%/55.4%，其余 ATU 测点为 83.3%-88.9%。

三个连续测试块的 RMSE 为 0.074、0.110 和 0.537 mm，校准后覆盖率为 91.3%、87.9% 和 46.1%。最后时段明显退化，说明固定校准量不能消除跨期漂移。R2/NSE 已输出，但两者在当前定义下相同，且累计位移强趋势使其接近 1，因此不作为主要性能依据。

14 日主块长下，模型相对持久性基线的 RMSE 差值为 -0.022 mm（95% CI -0.029 至 -0.012），MAE 差值为 -0.022 mm（-0.031 至 -0.011）。校准相对原始区间的 interval score 差值为 -0.026 mm（-0.041 至 -0.010），但距名义 80% 覆盖率的绝对偏差变化区间跨 0。7/30 日敏感性分析保持总体误差和 interval score 的方向，但 30 日覆盖率区间扩大到 61.9%-90.9%。这些是固定模型和 `qhat` 的条件性区间，不是未来漂移保证。

三个非重叠滚动折给出相反的稳健性证据：模型 RMSE 为 2.123、0.492、0.318 mm，持久性基线为 0.245、0.120、0.340 mm；逐测点 RMSE 优于基线的数量为 0/8、0/8、8/8。校准覆盖率为 48.8%、94.9%、75.2%，第二折虽然覆盖率升高，但 interval score 从 1.431 恶化至 1.756。模型只在最后一折显示增量价值，不能据此声称跨时期稳定优于基线。

固定协议五种子诊断进一步表明：折 1/2 的 RMSE 和 MAE 均为 0/5 种子超过基线，折 3 为 5/5。折 3 RMSE 为 0.323 +/- 0.008 mm，但预测日增量标准差仅为实际值的 0.164 +/- 0.022，相关性为 -0.048 +/- 0.220。初始化影响误差幅度，但不改变跨折方向；第三折点误差优势不能等同于稳定时序响应。

预注册内层早停后，三折 RMSE 均值降至 1.336、0.239、0.309 mm，相对固定 120 轮分别有 5/5、5/5、4/5 个种子改善；但折 1/2 仍为 0/5 超过持久性基线。第三折覆盖率达到 81.1%，代价是区间宽度由 0.471 增至 0.977 mm、interval score 由 1.037 恶化至 1.248 mm。训练轮数是可纠正的影响因素，但不是当前结构跨时期失效的完整原因。

2x2 有限容量/正则化诊断在折 1/2/3 分别选择 `h16_wd0`、`h08_wd0`、`h16_wd1e4`，第一/二名内层 loss 差值均小于所选配置种子 SD 的 5%。最终三折 RMSE/MAE 正 skill 种子数为 0/5、0/5；0/5、0/5；5/5、4/5，仅折 3 达到双指标多数正 skill。该结果触发预注册停止扩搜规则，不支持继续在当前已查看折上扩大搜索。

### 2.2 NGBoost 当日四级状态

- 全时段：green 1131、yellow 263、orange 30、red 7。
- 留出段：green 269、yellow 18、orange 0、red 0。
- Accuracy=0.958，完整四分类 macro-F1=0.432，多分类 Brier=0.088。
- Green/yellow recall=0.955/1.000；orange/red 因支持数为 0 而不可评价。

### 2.3 SHAP 遗留当日二分类

- NGBoost：AUC=0.994、PR-AUC=0.792、Brier=0.009、F1=0.766。
- 昨日状态持续性基线：AUC=1.000、PR-AUC=1.000、F1=0.958。
- 5 个扩展窗口测试折仅第 1/5 折同时含正负类；NGBoost AUC=0.915/0.993，持续性基线=0.999/1.000。
- 当前结果支持“能够识别持续状态”，不支持“已经实现未来提前预警”。

### 2.4 V0 与切线角敏感性

| 对象 | 预设组合 | 主要结果 | 判断 |
| --- | ---: | --- | --- |
| V0 | 9 | 相对默认等级一致率 90.1%-100%；黄色及以上 229-380 日；事件数 2-6 | 参数会实质改变状态和事件定义 |
| 切线角平滑/持续性 | 每个候选窗口 9 | 相对同窗口默认规则一致率 96.9%-100% | 工程平滑规则影响较小但非零 |
| 切线角候选窗口 | 15/30/60 日 | 15/60 日相对 30 日默认一致率约 80.6%；升级日约 1398-1399 日，30 日默认升级 1192 日 | 自动 `v_eq` 选择仍不稳健，8 测点融合整体更敏感 |

30 日默认组合产生 1192 个切线角升级日；15/60 日候选窗口产生约 1400 个升级日。敏感性分析不用于挑选结果较温和的 30 日窗口，而是表明参考等速阶段复核必须先于确认性切线角评价。

## 3. 尚缺的评价内容

| 评价对象 | 必需内容 | 当前状态 |
| --- | --- | --- |
| 位移点预测 | 按测点 MAE/RMSE、可选 R2/NSE、分时段误差 | 已实现；R2/NSE 因累计位移趋势仅作补充 |
| 预测区间 | 各分位数 pinball loss、覆盖率、宽度、interval score | 已实现仓库代码层面的日历后置校准和日期块 95% 区间；上游生成独立性与外部稳定性未验证 |
| 概率分类 | PR-AUC、Brier、校准曲线、校准截距/斜率 | PR-AUC/Brier 已有；校准检查未实现 |
| 四级分类 | 每类支持数、recall/F1、macro-F1、混淆矩阵 | 已实现，但 high-level 留出支持数为 0 |
| 事件级预警 | 事件召回率、漏报、误报事件、误报持续时间、首次命中提前量 | 未实现 |
| 不确定性 | 按日期连续块和事件重采样的 95% 置信区间 | 部分实现：ConvLSTM 日期块区间已完成；分类和事件区间未完成 |
| 泛化能力 | 多个未来测试期、新监测时段或跨滑坡验证 | ConvLSTM 三个内部滚动折已实现且显示不稳健；外部验证未实现 |
| 可解释性 | 模型/目标/样本溯源、测点/时间折 SHAP 稳定性、相关特征说明、特征组消融 | 已实现内部五折分析和测点分层汇总；仅运动学组在两个任务均为 5/5 折一致正增量，外部确认未实现 |

## 4. 改进优先级

| 优先级 | 改进项 | 完成判据 |
| --- | --- | --- |
| 已完成 | 实现未来 1/3/7 日 onset 标签和事件提取 | 3/9/21 个正日期，均来自 3 个可预测事件 |
| 已完成 | 完成 V0 与切线角预设参数敏感性 | 输出 4 张审计表；已明确 V0 敏感范围和自动 `v_eq` 不稳健性 |
| 已完成 | ConvLSTM 启用日历后置校准 | 输出校准前后覆盖率、宽度、pinball 和 interval score；只证明仓库代码切分，且仍未达到目标覆盖率 |
| 已完成 | ConvLSTM 日期块 95% 置信区间 | 7/14/30 日预设块长、1000 次配对重采样及完整来源字段已输出 |
| 已完成 | ConvLSTM 扩展窗口滚动验证 | 历史 6 通道和当前 7 通道 fixed-120 均完成三个非重叠 287 日测试折；当前版本使用隔离目录并重新绑定全部输入和源码哈希 |
| 已完成 | ConvLSTM 五种子训练稳定性诊断 | 当前 7 通道已完成预设种子 0-4 的 15 次训练、逐种子/测点指标、汇总和逐 epoch 轨迹；未选择最佳种子，历史 6 通道结果另存 |
| 条件 P0 | 若最终论文使用藕塘，补足确认性来源；若更换数据集，在新案例按折生成派生序列 | 数据来源、处理链和点位映射可追溯；先切分原始观测，再在每折内部生成派生量 |
| P0 | 增加互不相连的 onset 标签事件数据 | 使内层选参和外层评价不由单个标签事件决定 |
| 历史 6 通道已完成；7 通道未运行 | 建立 ConvLSTM 拟合期内部验证与停止规则 | 历史版本的五种子三折运行和固定 120 轮配对比较保留；不得据此选择当前 7 通道 epoch |
| 历史 6 通道已完成；7 通道未运行 | ConvLSTM 有限容量/正则化敏感性 | 历史版本保留 2x2 候选与折内选择；当前 fixed-120 协议不启动容量扩搜 |
| P0 | 增加预警分类与事件级指标的不确定性 | 事件充分后输出可追溯 CSV，不以逐行独立 bootstrap |
| P0 | 按有效协议重新调节 NGBoost | 在事件充分前暂停；之后内层选参、外层只评价一次 |
| 已完成 | 特征组消融与 SHAP 跨折/测点稳定性 | 五折协议和 11 个产物可复算；回归组排名稳定、分类组排名随时期变化，只有运动学组显示一致增量贡献 |
| P1 | 复核切线角等速阶段并确认 `v_eq` | 8 个测点复核图和含等级/融合影响的审计表已生成，可选人工阶段表已接入 `code/features/build_features.py` 并统一执行训练期校验；待导师或现场资料确认相应行并填写依据 |
| P2 | 滚动 V0、漂移检测与实时数据接入 | 在新数据上按时间逐步更新且不访问未来数据 |
| P2 | 新时段或跨滑坡确认性验证 | 数据不参与模型和阈值选择 |

在 P0 完成前不更换主模型。任何新增模型对照也必须先获得用户批准。
