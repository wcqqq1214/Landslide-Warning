# 藕塘 PINN v1.8：真实子步接口核验结果

## Material Passport

- 日期：2026-09-11；Origin Skill/Mode：academic-research-suite / experiment-agent / validate。
- Verification Status：ANALYZED。真实轨迹接口核验完成；尚无本版 PINN 训练或精度结论。
- 依据：[执行前方案](ootang_bplus_pinn_substep_audit_plan.v1.8.md)，提交 `d4c575b`；
  实现与相关测试 `5e39c57`；封存产物 `cb3f8de`。

## 1. 执行及核验结果

按已固定方案调用原算法的记录副本三次，分别使用 342A、432B、612B 的原参数/载荷。
共 **123,072 子步**，科学进程含只读回放 3.496 秒，启动器总墙钟 4.433 秒；预算内结束。
0 新物理参数拟合、0 神经更新、0 未修改版原前向、0 独立轨迹重新积分，无重试或改门限。
“三次积分”是真实新计算，不能因复用参数而称为 0 物理计算。

| 来源 | 子步数 | 日末四点均值最大差（mm） | 运动等式最大残差（mm） | 三类反力等式最大残差（原反力量纲） | gap 与原活动集最大差（原反力量纲） |
| --- | ---: | ---: | ---: | ---: | ---: |
| 342A | 33,344 | 0 | 1.4211e-14 | 1.3074e-12 | 5.6331e-12 |
| 432B | 39,104 | 0 | 1.4211e-14 | 4.6896e-13 | 1.3697e-11 |
| 612B | 50,624 | 0 | 2.8422e-14 | 1.2164e-11 | 2.1999e-11 |

每条轨迹的 48 项检查均通过：原件与派生函数一致性、首日零状态、连续子步、载荷日程、
日内背景插值、原 A/rhs/g 的 NumPy 重建、原 dx/du 状态更新、PyTorch 方程残差、
活动集/互补、日末状态及四点观测映射。背景等式最大残差均为 0。

原 dx 最小值为 −8.7991e-11 mm（432B），处于原 −1e-8 容差内；原 gap 最小值约
−1.3874e-13，处于原 −1e-7 容差内。归一化互补量最大 8.2978e-17，低于登记的 1e-8。
接口 gap 最小约 −2.1885e-11；不能把这些有限精度结果写成所有量数学上严格非负。
原始值、舍入界和差/界比值均已保存，不裁剪负值或把混合量纲最大值当作 mm 精度。

真实轨迹还显示活动集并非始终同一状态：例如 342A 的 O2 在全部 33,344 个子步处于
固定滑移坐标的活动状态，原累积塑性滑移为 0。该域与某个观测点不是同一轴；这只是
给定参数轨迹上的模型行为，不是对真实滑坡局部不运动或参数全局不可辨识的证明。

## 2. 工程验证与可复算性

- 原代码完整保留；记录函数移除新增语句并恢复声明后与原函数全文相同。短合成载荷中
  原/记录函数五类日末输出完全一致，覆盖活动集变化。合成状态、载荷、mask 污染均能检出。
- 新增 5 项测试与已有 5 项方程/梯度测试共 10 项通过（本次 1.406 秒）；Ruff 和格式检查
  通过。测试包含未来位移列为不可解析字符串时仍只读取驱动和首日 y0 的信息边界检查。
- 封存后独立命令从 NPZ 重算全部报告，结果逐值一致，0 原生积分调用。
  1,209 个保护文件、12 份科学/测试源码及 36 文件索引均核对通过，原日志完整保留。
- NumPy 在原 Context 和部分矩阵乘法处仍报告 divide/overflow/invalid 警告；实际参与判定
  的状态、残差和误差界均通过有限值检查，数值复核通过。没有修改原算法或屏蔽这些日志。

原始证据：[运行归档](../results/ootang_bplus_v1_8/20260911_substep_audit/)。只读复核命令：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 VECLIB_MAXIMUM_THREADS=1 \
PYTHONPATH=code .venv/bin/python -m physics_guided_pinn.run_substep_audit \
  --run-id 20260911_substep_audit --verify
```

## 3. 结论与下一步

原 64 子步轨迹与降阶 PINN 残差、四点观测接口一致；可以据此构造状态网络与训练损失。
本次没有用预测期位移标签训练或评分，也没有新预测曲线、概率区间或模型有效性证据。
v1.1–v1.7 的负结果不变，**稳定四点提升与可靠概率覆盖仍未实现**。

下一阶段必须具体确定可学习物理偏差与其范围、状态网络/未来推演、各量纲缩放、物理
残差容许程度、时序隔离、概率校准和有限训练预算。活动集下的零塑性方向提醒我们，不能
默认任意力学参数都有可学习信号；也不能仅靠换用 PINN 求解同一固定系统宣称精度会提高。
实现和数值验证已完成，训练效果及用户/导师接受另计。
