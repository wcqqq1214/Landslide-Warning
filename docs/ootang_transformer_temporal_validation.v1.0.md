# 跨起点 α / λ 验证记录 v1.0

## Material Passport

- 状态：固定训练、完整预测、独立复算和五张图件核验已完成。执行/效果/验收分别记录。
- 计划 `4011772`；来源156项哈希保持。

## 训练前核验

- 六组标准库 unittest 全过，包含四个 λ 的解析损失/梯度子检查；ruff 通过。首次 pytest 命令因环境未安装该库退出，未执行训练，随后使用标准库完成同组检查，未新增依赖。
- 五组教师日期/拟合前缀/来源核对通过；432/612 训练标签及驱动哈希一致；972 仅沿用792教师，无未来参数读取。
- 11次物理前向，71项完整/截断状态及特征比较、非零神经因果性、零输出回退、重载通过；18组既有初始化一致。
- 参数、状态、原优化未收敛标志与日志保存在 implementation_verification/receipt.json。来源锁和新实现锁已保存。
- 未调整冻结损失、参数网格、日期或效果门。下一步运行全部固定 α 与 λ，然后统一解锁完整外层评分。

## 旧检查点元数据兼容修订

α运行完成432起点三拟合1200更新后，在612旧权重读取的 `step` 字段报 KeyError。旧L0使用 `updates`，旧REG1使用 `step`。仅统一读取并拒绝冲突字段，新增回归检查；七组检查全过。原错误、运行日志和v1实现锁保留，模型权重/冻结配置不改；空的失败复用目录归档。后续跳过已锁定432结果，不重复训练。更新后的实现锁附变更哈希和继承记录。

## 训练后独立复算

- 42新拟合/16800更新、18旧拟合复用、5岭回归拟合；四条293日窗口和全部56组概率分布均完成。旧字段兼容异常未重复已完成训练。
- 228检查点逐一精确重载；独立NumPy因果注意力完整前向、5个岭回归SVD重建通过。共3339568数值核对，最大绝对差1.8189894035458565e-11；156来源保持，原物理参数未改。
- 141条事件核对：10次alpha/lambda轨迹发出都在唯一1461行完整评分标签读取前完成；各次选择/校准严格使用上一预测的前90/后90日。每条路径无位移反馈，旧已发预测未覆盖。
- 56汇总行、224逐点行、168种子汇总、65632点日行、原门槛布尔值与配对种子方向独立复算通过。该顺序核验不消除既有日期/超参数曾经暴露的限制。
- 选择alpha在612/792/972/1168为0/0/0/1；选择lambda为3/3/3/0。三个历史均值门均0/3，最终也失败。972平均改善仍有MJ3退步；最终两选择输出相同。

## 图件核验与修订

五张最终PNG/SVG、20面板已逐图目视。实际SVG曲线/区间88936值核对，最大量化差5.154641257831827e-6mm；240×170mm、300dpi、最小7.6pt、面板1.5pt对齐与文本边界检查通过。导师版/完整范围版均保留全部日期；图中各点RMSE和校准区间与原数组一致。跨起点初稿图例遮挡部分数据，经目视发现后只调整图例/留白，原预览留档；数据和评分未改，新图重做数值和对齐检查。

静态源码检查保留15 PASS/3 WARN/3 FAIL：导入的canvas/save未被识别导致尺寸/PNG/SVG导出项提示，检查器要求PDF及pdf.fonttype，而用户明确本轮无PDF；对数轴在代码中有正值断言，但静态规则未识别。实际SVG/PNG尺寸、字体、数据与渲染核验分别记录，不宣称静态或PDF专用检查通过。

## 统计解释检查（11/11）

| 类型 | 本轮审查结论 |
| --- | --- |
| Simpson汇总反转 | 保留逐点：972平均改善而MJ3退步，不以平均代替四点保护。 |
| 生态谬误 | 四点同一剖面，限制在现有案例，不推断其他滑坡。 |
| Berkson选择偏差 | 原四点和全部日期固定，未按胜负筛选样本；既有案例选择限制保留。 |
| 碰撞变量偏差 | 未估计因果调整效应，不从模型输入相关性推出机制。 |
| 基础率忽略 | 只评价连续位移，不报告事件诊断或预警准确率。 |
| 向均值回归 | 与同窗B+/简单对照及各固定候选配对，不把某窗较差后的改善当机制证据。 |
| 幸存者偏差 | 全42新拟合、18复用与全部窗口保存；异常及修复记录保留。 |
| 多重寻找效应 | 公开完整五点alpha/四点lambda和所有种子；不报告未经校正的显著性。 |
| 分析分岔 | 先提交计划；只修元数据读取/图件展示，网格/日期/损失/阈值不改；旧暴露使结果仍为探索性。 |
| 相关当因果 | 拟合及预测误差不是B+机制因果验证，RL未运行。 |
| 反向因果 | 预测器有因果掩码和明确前缀读取；给定未来驱动及原始as-of限制保持，不作因果推断。 |

重叠窗口不视作独立重复；90条连续误差不视作90次独立实验。区间为高斯逐日边际经验校准，不宣称可靠覆盖或整条路径同时覆盖。研究执行、效果及用户/导师验收分别记录。
