# 夜间固定小图与残差尺度验证 v1.0

## Material Passport与授权

2026-09-15（北京时间）；academic-research-suite / experiment-agent / plan→run→validate，角色在当前任务内执行。用户授权小试失败后借鉴Wang等原论文继续尝试其他模型、每步文档与commit、简要汇总和PDF图，并允许提前结束。起点条件化小试已完成且失败，上一goal turn为实质进展（提交0bfa674），不重跑它。

新分支codex/overnight-fixed-graph。新授权窗口从2026-09-14 18:23:23 UTC核对目标开始，到2026-09-15 01:00:00 UTC（北京时间9月15日09:00）截止，含准备/核验/交付。把“明早”按即将到来的早晨解释，采用较早截止。旧预算不恢复或转用；新授权明确包含PDF，旧“不PDF/停止新模型”不阻止本轮。只本地commit，不push。

## 1. 原文依据与独立假设

Wang等（2025），[DOI 10.1029/2025JH000592](https://agupubs.onlinelibrary.wiley.com/doi/full/10.1029/2025JH000592)。核读本地19页PDF相关方法/结果/讨论并对照出版商原文：§2.2/§3分解位移；§2.4使用GCN与GRU；§5.2分区内连边；§6.2报告移除注意力后精度接近。借鉴信息组织，不复制原文成绩或全文处理流程。

本轮四点ATU1/ATU5/MJ3/MJ1，按分区仅为MJ3—MJ1加边，其他点只自环。固定等权，不以全序列相关系数决定图；不声称复现原文八点距离/相关加权图。先问相同时间网络中的神经点间传递是否有效，再问残差是否支持慢/快分量监督。

创新定位为B+连续状态/合法教师引导下的四点空间与残差尺度验证。GCN、GRU、残差学习、EMA及其组合本身不宣称原创；新颖性尚未由系统检索确立。保留物理主干和概率目标，不扩为新案例或真实预警。

## 2. 任务、合法教师与对照

原四点1461日日序列保持。训练前缀612/792/972/1168分别一次发出293日；612为校准启动，792/972/1168完整窗主要评价。给定未来逐日降雨/库水位，不反馈路径内位移，不在路径中重新定标或更新区间。B+从首日连续递推，重拟合0、新增物理前向0，复用301项冻结来源。

训练伪起点m∈[432,n)，h≤min(293,n−m)，目标m+h−1<n。教师取432/612/792中拟合边界≤m的最新冻结组，历史/目标物理特征使用同一教师。实际起点教师依次612/792/792/1168。归一化只用当前n日训练前缀，episode是当前训练样本，不冒称历史独立发出预测。612最长180日训练距离、教师变化、972/最终重叠97日、日期反复暴露、as-of/预处理/旧物理优化未收敛限制保持。

九个旧对照原样复用：BPLUS_CONTINUOUS、DRIFT1、RR_COND、OLD_TRANSFORMER、OLD_HALF、OLD_REG1、COND_ATTN、NO_OBS_ATTN、POOL_MLP。完整点/日期/种子保留。

## 3. 固定GRU空间配对

每节点10个物理/驱动通道：自身B+相对位移/速度、含水量、雨水头，共享降雨/水位/水位差/7日雨量/30日雨量/库水头；历史另加自身相对实测位移、日增量、实测−B+残差及残差日增量，合计14维。逐点用当前训练段mean/std，不作其他点神经混合。B+本身有物理耦合，本点对照不等于没有任何空间信息。

共享一层GRU(14,8)，零初态，处理起点前全部历史日。末状态h为4×8；消息tanh(Linear(8,8)(A h))。两臂除固定邻接矩阵A外完全相同：

- GRU_LOCAL：A=I。
- GRU_GRAPH：自环＋MJ3↔MJ1双向边，逐行归一化，该两行各本点/邻点1/2；ATU点保持自环。

每个未来距离/点的解码输入为本点h8、图消息8、本点未来10、起点最后实测速度/残差2、h/293与log(1+h)/log294两个距离量、可学习点标识4；Linear34→16、GELU、零头16→1。输出B+连续轨迹＋残差（mm）；零头回退B+，不强制起点实测锚定。无Transformer/Mamba/注意力。

三种子0/1/2、float64 CPU单线程、Adam .001、固定200更新，batch4伪起点×16成熟距离。保存0/50/100/200，主输出固定e200等权三种子。按训练段各点位移std（下限1mm）归一化MSE＋λ=1残差平方惩罚。抽样表/初始化/单位/优化器完全配对。固定24新拟合4800更新，无论成绩均完成所有窗口，不择种子或检查点。

两臂共享参数的训练可能也改变ATU输出，不把它解释成ATU增加了邻边。以非零网络输入扰动/导数核对实际连通性。

## 4. 残差尺度诊断和唯一条件候选

两臂完成后仅使用前1152日成熟标签与保存教师，四段各180日：[432,612)/教师432、[612,792)/612、[792,972)/792、[972,1152)/792。每段教师固定；r=y−B，EMA30由段首r初始化，slow_t=(2/31)r_t+(29/31)slow_(t−1)，fast=r−slow，严格单边因果、不跨教师接缝平滑。各段前30日作预定初始化舍弃；不按胜负选日期。报告slow/fast方差、协方差与精确重构，分量方差和不冒充总方差。

仅检查7日雨量、日水位差在0/7/30日滞后的fast相关，完整报告全部点/段/滞后。描述性触发条件：至少两个点，对同一驱动/滞后/符号，在四段中的至少三段同时有|Pearson相关|≥0.3且fast方差/总残差方差≥0.1。零方差相关记不可用，不当通过。此条件只决定是否值得做有限双头尝试，不代表显著性、因果或293日预测有效性，不读最终293日成绩来触发。

仅触发通过时启动GRU_GRAPH_DUAL：同一图/GRU/解码体，末头16→2预测slow/fast，相加为总残差。训练分量标签按每个合法教师的完整前缀残差作同一因果EMA30，不跨教师拼接；目标只用≤目标日且<n的成熟标签。损失为总残差MSE＋0.5×(slow MSE＋fast MSE)＋λ=1总残差平方惩罚。其他设置相同、从头同种子训练，最多12拟合2400更新，四起点完整执行。相对GRU_GRAPH检验分量监督及额外头的作用，不单独归因输入多尺度。未触发则新增双头拟合0，保留诊断负结果。

总上限36新拟合7200更新，物理前向/拟合0。不追加其他网络、图权、宽度、轮数或RL搜索。双头如运行另存输出，不回写已锁定两臂。

## 5. 概率层与判断

沿用上一发出路径第91—180日90条成熟误差逐点RMS（下限1e-6mm），固定高斯80/90/95%边际区间。最终[1062,1152)校准及16日间隔不改；种子共用该方法集成尺度。各阶段先锁定所有发出分布，再完整评分。误差相关、距离迁移及边际区间非整条轨迹覆盖的限制保持。

平均MAE/RMSE各改善B+至少1%，逐点不退步（1e-6mm）；平均CRPS/90%区间评分各改善至少1%，逐点最多退步5%，90%平均覆盖≥85%、逐点≥80%。另外报告≥2/3种子平均MAE/RMSE同向。超过B+、超过DRIFT/RR、图相对本点、可选双头相对单头分别结论；不把局部配对胜出当整体达标。所有结果探索性，不按后期答案改主方法。

## 6. 核验、提交与最终交付

按序完成并commit：本计划/配置/来源；实现和训练前日期/成熟目标/未来信息隔离/连通性/同初始化/零头/重载/单查询批次/梯度核验；固定两臂完整训练评分；因果残差诊断和触发记录；若触发则固定双头完整训练；独立NumPy的GRU/图/解码前向、保存数组/校准/评分/事件复算；PDF和简短文档收尾。

Python/nature-figure沿用导师四点风格。交付一份简要Markdown，链接紧凑多页PDF，包含原小试失败概况、空间配对、两臂最终四点及残差诊断；如触发增加双头页，必要时补完整范围。PNG/CSV/全部种子和检查点保留。历史段只画实测+B+，不伪造起点网络训练拟合；真实区间不美化压窄，固定轴裁剪计数并配完整范围图，评分不裁剪。

PDF字体/碰撞、1.5pt对齐、实际图元和逐页面视通过后结束。每步更新progress并本地commit，无关未跟踪导师制图规则不纳入。用户整体执行授权覆盖ARS逐命令确认建议；在授权范围内实现并修复经确认的故障，记录失败/修复/实际预算，不重训已完成模型掩盖异常。无需等到09:00才结束；所有预定工作完成且无具体新假设时提前收尾。
