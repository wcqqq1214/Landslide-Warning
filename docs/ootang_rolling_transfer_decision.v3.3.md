# C4 后期迁移选择锁定

2026-09-13；开发效果与独立复算均已完成，依据 [C4 方案](ootang_rolling_probability_candidate.c4.md)进入本轮唯一一次后期迁移。冻结机器可读记录：[transfer lock](../config/ootang_rolling_transfer_lock.v3_3.json)。

选择 C4_RIDGE_FULL 为主候选，C4_RIDGE_DATA 保持消融，固定最强简单对照 DRIFT1。两臂 alpha 均为内部选中的 0.001；原尺度拟合与反馈速率 0.02 等完整数值沿用 v3.3 配置，不重选。没有按点组合方法。

使用前 1168 日位移训练，逐日回放 1168—1460 起点；30 日合法端点每点 264 个，报告全部 1—30 日步长及完整日期。先准备六个前缀物理教师至 1461 日已知驱动，仍不重新标定 B+；随后只拟合两臂的新前缀模型。反馈在后期阶段重新从初始状态开始，不借开发评分初始化。

此次选择不忽略开发限制：FULL 合并 RMSE 不如 DRIFT1，物理消融明显更好，覆盖反馈也牺牲了一部分 CRPS/区间评分。迁移评价用于判断这些有限开发收益能否保留，不预先称适合最终使用。事前 27 项标准不变，平均与合并 RMSE 继续分列。

按冻结总体方案，本次后期结果不得用于再选候选、逐点换模型或重新调反馈参数后声称独立迁移成功。成块重采样与非重叠起点报告只说明证据强度，不改变主评分和样本。拟采用长度 30 的循环移动块，同一次成块抽样在模型/点之间配对，2,000 次、种子 20260913，给出差值百分位 95% 区间；它不把历史重复暴露的数据恢复成盲测。

总截止仍为 2026-09-13 05:27:52 UTC。此锁定完成时没有读取本轮后期位移标签；后期评估一经启动即视为已经使用，不因启动失败或效果差重置选择次数。所有实现、输入、参数、输出和负结果保留。
