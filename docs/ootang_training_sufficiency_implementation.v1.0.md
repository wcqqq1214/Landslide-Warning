# 训练充分性诊断实现核验 v1.0

## Material Passport

本地实现核验，正式诊断尚未执行；新增拟合/更新0。冻结计划提交8d07a120；独立窗口10:08:15—11:08:15 UTC保持。

实现为独立`code/training_sufficiency/`入口；旧模型、教师、输入接口与结果未改。128×32面板使用精确整数分层下标；每检查点固定面板梯度只反向求值，不实例化优化器，逐状态张量检查未改变。训练目标另用代数式构造，完整未来路径只重载核对，不读取最终位移标签。诊断输出目录不可覆盖；完整触发明细先锁定再进入独立复算。

预检：1329来源、89记录、538340值，最大差1.11e−16。包含四前缀日期/教师/成熟目标、原200步抽样与第二块扩展、跨骨干批次一致性、未来位移污染隔离、未成熟目标拒绝、损失代数和合成触发正负例。最终窗评分行的增补不能改变触发结果；只降低正则目标但拟合误差变差不会通过。

旧权重不含Adam状态已实际核对；仅触发后才允许按原初始化/原200步抽样精确重放，恢复状态后固定追加200步。当前没有进行这种训练重放。预检通过只证明实现条件，不代表模型有效或已收敛。

[预检回执](../results/ootang_training_sufficiency_v1/20260915/preflight/receipt.json) · [冻结计划](ootang_training_sufficiency_plan.v1.0.md)
