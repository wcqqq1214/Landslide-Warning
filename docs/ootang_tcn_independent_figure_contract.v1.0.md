# 独立TCN四点图件合同

核心问题：已训练短窗TCN在8:2切分、293天无位移/驱动观测反馈的独立递推中，能否保持接近实测的预测。当前保存结果显示两臂均明显落后B+，须完整展示偏离与宽区间，不据此重训或改范围。

采用Python、定量2×2网格；四个面板分别保留ATU1/ATU5/MJ3/MJ1的异质性，不用平均曲线代替某点。两臂各一张，固定相同版式。参照所给图的构图和编码，数值全部来自本轮保存数组/CSV，原图数值和QRF名称不复用。

183×128 mm，中文Arial Unicode MS，正文/刻度/图例至少6.5pt；黑实测、灰虚线B_ANCHOR、蓝TCN，预测段80/95%经验高斯边际区间，蓝底训练回代、橙底独立预测。图中文字说明训练为一步回代、预测为七天块递推；前252日只画实测。各点所有量统一减原首日位移以匹配累计位移零点，原数组及误差保持。

主均值为3种子独立完整轨迹的平均；预测区间来自每点/距离的90条已兑现历史独立预测误差RMS，不是种子置信区间。训练回代916点，独立预测293点/测点，4点均保留所有日期；回放重叠和既有超参/标签暴露见结果说明。不作显著性/因果/现场预警主张。

仅PNG(300dpi)/SVG，沿用用户无PDF交付要求；PDF专用检查不适用，不声称通过。逐图运行1.5pt绘图区对齐、实际Matplotlib文字框与可见刻度检查，核查全部曲线与区间顶点，最终目视各面板。RMSE注释按文字框、局部数据范围及实测/模型/区间/预测起点线选择空白角落，不盖住数据、不截取纵轴范围。保留所有数据及区间范围；导师验收未提供。
