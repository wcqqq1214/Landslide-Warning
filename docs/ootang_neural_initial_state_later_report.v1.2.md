# 初态耦合模型：完整后期报告补充

Material Passport：academic-research-suite / experiment-agent；执行条件修订，单候选保持。

用户在开发结果已知后明确要求：“不要不进入后期，参考pdf的其他模型，跑通流程”。因此，本补充授权完整后期探索性评价，即使开发效果门失败也执行；开发失败、v1.1 原条件和旧停止回执均保留，不改写成通过。

仅用内部已锁定的 200 次更新、种子 0/1/2，在原 1168 日训练前缀各训练一次，完整评价后期 293 日的第 1—7 天。全部模型、54 参数、四维初态范围、数值物理门、原严格诊断、驱动、日期与共同概率校准不变。开发门继续作为效果判据，后期仅用于完整同任务报告；不重选模型、步长或种子，不称盲测。

最多增加三次后期拟合、600 次更新；开发已完成三次、600 次，本次续评累计六次、1200 次，低于原上限。仍在 **2026-09-13 21:05:18—22:05:18 UTC** 同一窗口内，训练截止 21:40:18，不恢复或新增时间预算。真实物理失败或超时仍停止并如实报告。

使用独立后期入口，冻结本补充、原配置、内部选择和开发决定的哈希。原 v1.1 配置/入口及开发决定不改。完成后核验全部实际阶段的模型重载、物理轨迹、概率尺度、日期锁和评分，再更新结果页及现有四页 PDF 对应结果。只在 test 分步本地提交，不 push。
