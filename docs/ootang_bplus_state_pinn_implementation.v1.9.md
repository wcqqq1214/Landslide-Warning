# 藕塘状态 PINN v1.9：核心实现与当前验证边界

## Material Passport

- 日期：2026-09-11；Origin Skill/Mode：academic-research-suite / experiment-agent / validate。
- Verification Status：ANALYZED，网络核心与局部工程验证完成，正式有限训练尚未开始。
- [训练前方案](ootang_bplus_state_pinn_plan.v1.9.md)及配置提交 `77e4f90`；
  核心实现/相关测试提交 `13e3b5a`。不修改既有物理代码、参数和负结果。

## 1. 已实现

`code/physics_guided_state_pinn/core.py` 提供：

- 只依赖已知驱动、水文状态和冻结 B+ 状态的 Case；不保存实测位移标签。
- 训练前缀独立拟合的 12 维特征标准化、状态标尺和七类物理残差标尺。
- 548 参数速率网络和 3,028 参数状态网络，共 3,576 参数；零输出层初值。
- 0.5–2 倍蠕变修正、保持跨日 autograd 的累计背景、每日至 64 子步的连续插值。
- 从首日为零的 20 维状态修正及原四点观测映射，背景始终由速率积分约束。
- 数据/七项子步物理/速率先验损失及固定 20 更新权重渐增；只接受精确训练标签前缀。

尚未实现本版完整训练器、9 次 R 力学回放调度、checkpoint/概率产物封存与完整独立复核。
现有 P 前向及损失可用，不等同于已完成本版主输出 R 或整套实验。

## 2. 实际验证

以下命令完成 6 项 unittest，约 1.050 秒，Ruff 与格式检查通过：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 VECLIB_MAXIMUM_THREADS=1 \
PYTHONPATH=code:tests .venv/bin/python -m unittest -v test_physics_guided_state_pinn
```

| 检查 | 实际覆盖 | 结果 |
| --- | --- | --- |
| 参数数目与零修正 | 3,576 参数，完整子步状态返回 B+，日末观测误差≤1e-8 mm | 通过 |
| 非零权重、初值与范围 | 首日零状态、gamma∈[.5,2]；不同长度/73 子步分块的前向一致 | 通过 |
| 跨日积分梯度 | 最后一日背景对各早日 gamma 的导数等于相应原速率，首日导数为零 | 通过 |
| 前缀隔离 | 大幅改变未来模板/驱动不改训练统计/前缀状态；训练损失对未来特征梯度为零 | 通过 |
| 损失组成 | 七项标准化 MSE、权重和先验正确；G/H 首末层均有有限非零梯度 | 通过 |
| 多日总损失方向导数 | 小幅非零输出层，固定种子 31415 的四个方向，步长 1e-5/1e-6 | 通过 |

方向导数门限 `abs(AD−FD) <= 1e-5+1e-3*max(abs(AD),abs(FD))`。
测试复用已存 342A 的 48 日工程截片，以 36 日网络前缀检查导数；人工标签为固定物理均值
加常数。没有原生求解器调用、优化器更新或实测标签拟合，不是新的 36 日标定/效果实验。
该测试只证明固定教师下网络/缩放的前缀隔离，不证明物理参数在 36 日前已经可用。

## 3. 下一步与未完成目标

继续实现训练与封存流程，按 342→432→612 锁定预测后再读取下一段标签，固定三种子、
200 轮、合计 1,800 更新；每个最终速率网络只回放一次原力学，共 9 次。
R 是主输出，P 是状态网络诊断，必须同时报告 P/R 差异与约束违反，不能事后选较好者。
概率先按已登记的滚动常尺度对照执行，尺度迁移风险及历史窗口暴露限制保持。

当前新 PINN **尚未训练**，没有新的均值、区间或效果结论；整体四点精度和概率目标仍未完成。
核心实现及本次工程验证完成，完整实验验证和用户/导师接受另计。
