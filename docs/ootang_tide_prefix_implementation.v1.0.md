# TiDE_KIN_PREFIX 实现及预检 v1.0

## Material Passport

按已提交的[固定计划](ootang_tide_prefix_plan.v1.0.md)实现，参数/损失/样本/种子/更新数不变。仅改变每个预测距离可读取的未来协变量范围，独立分支与输出目录保留原结果。

`code/tide_prefix/core.py`继承原TiDE，参数名称及110392参数完全相同。未来全部动态值未知时的编码作为基值，逐槽位实际值与未知值的特征差经编码器仿射权重相乘、前缀累加，得到各h可用的编码。h与0解码在同一h上下文内计算，再作原起点扣减；输出0固定为零。没有新的网络参数、图边、B+基线、损失项或概率头；等更新预算不代表等FLOPs。

基础输入直接复用原KIN接口，含180日位移历史、合法K与共同驱动；H关闭。训练抽样由原配置产生并与各原种子保存的400×8日程精确核对。尺度从当前合法前缀计算并与原scaling.json精确比较；目标/成熟掩码、每起点损失均保持。

预检926项/3781808个数值通过，最大差2.19824158875781e-14。所有294个输出与完整参数梯度均对照“显式逐h屏蔽、直接运行原F”的实现；另用独立NumPy前向核对。对四前缀×三种子的非零原e400权重，各预设截断h后的驱动/K/known任意扰动均不改变h及之前输出，未来动态输入Jacobian为0；允许范围输入正控有响应，原未隔离网络的早期输出也确实响应晚期信息，避免零初始化假阳性。

相同权重下h293与原网络一致（浮点容差内），h0和零初始化精确为0；重载一致、各组初值与旧e0参数精确相同、未来未成熟驱动隔离、独立历史/标签/成熟掩码均核对通过。无优化预检仅进行前向/梯度计算，新拟合和优化更新均0；五次前反向耗时估计另存，不当正式训练耗时。

入口：`python -m tide_prefix.run train`，成功后`python -m tide_prefix.run score`。运行环境为`/tmp/ootang-tide-env-20260915/bin/python`，设`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=code`，项目根目录启动；原环境不改。训练逐前缀合法读取位移，完整发报和90条成熟误差池写锁后才进入后期全标签评分。共12新拟合/4800更新、60新检查点与60原KIN检查点，固定e400主结果，不因成绩差跳过窗口。

准备阶段两次只读路径查找纠正至core.py/run.py，另删除一个未使用导入；正式预检失败0，无训练重试。预检回执、正控、临时重载权重和运行探针均保留于[preflight](../results/ootang_tide_prefix_v1/20260915/preflight/receipt.json)。正式训练与独立效果核验在此提交之后执行。
