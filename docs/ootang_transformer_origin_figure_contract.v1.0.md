# 起点条件化小试图件合同 v1.0

在18:05 UTC独立复算通过后、编写本次绘图代码之前固定。只呈现保存的e200三种子等权集成、同窗基线及原固定高斯边际区间；不选点、种子、日期或新模型。

核心结论：三臂在972历史窗改善B+，但792与最终窗退步，完整历史和注意力的配对收益尚不稳定。

1. `COND_ATTN_mentor`：最终1168/293的四点轨迹，导师固定纵轴、蓝/橙背景、实测黑/B+灰虚线/候选蓝、80%与95%真实预测区间。每点单独承担空间异质性证据。过去仅显示实测与B+；候选从预测起点开始，不伪造起点条件模型的训练段拟合曲线。
2. `COND_ATTN_full`：同一组数据完整纵轴，作为固定轴图的显示核查副本；任何裁剪日数在CSV中记录。不是独立效果证据。
3. `comparison`：a跨时段RMSE/B+展示改善方向；b完整历史相对NO_OBS与POOL的成对RMSE差展示新增信息/注意力的归因边界；c最终四点RMSE展示空间退步；d三时段90%覆盖率展示概率覆盖变化。实线/符号为集成预测评分，浅色小点是全部三个种子的评分，不能解释为独立重复置信区间。b纵轴负值表示完整历史更好。统计单位是既有时间窗/随机种子，窗口重叠，未做显著性推断。

统一Python/Matplotlib，240×170mm四面板、PNG300dpi/SVG可编辑文字；沿用已存在导师制图规则。颜色与线型/符号冗余区分方法。无PDF。每张保存数值来源、实际SVG路径数值核对、文字边界、数据线碰撞和1.5pt面板对齐检查，再逐面板目视。图注说明给定未来降雨/水位、无位移反馈、探索性日期与固定区间。报告同时给宽度和CRPS/区间评分，不能仅以覆盖高称概率效果好。
