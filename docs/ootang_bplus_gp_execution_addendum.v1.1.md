# B+–GP 执行补充 v1.1（已确认执行补充）

2026-09-13。承接用户“根据这个 plan，一步步进行”；原 v1 计划和历史结果原样保留。

本补充将原贴缺失、又会影响优化和验收的数值列为一套具体提议，**不是声称恢复了未收到的完整附件**。
数据、核结构、四点各拟合一次、792/376 切分与原停止条件保持；用户已回复“采用这套事前固定的补充方案”；以下数值据此冻结。

| 项目 | 提议固定值 |
| --- | --- |
| Constant 方差初值 / 边界 | 1 / [1e-3, 1e3]，针对已按 RMS 缩放的残差 |
| 五个长度尺度初值 / 边界 | 各为 1 / [1e-2, 1e3]，针对标准化输入 |
| WhiteKernel 噪声方差初值 / 边界 | 0.01 / [1e-6, 10]，单位为缩放后残差的平方 |
| Matérn、重启 | ν=1.5，ARD 五长度尺度，0 次重启 |
| jitter | alpha=1e-10，仅加训练矩阵对角，不作为预测观测噪声 |
| L-BFGS-B | maxiter=200，目标调用硬上限 400；ftol=1e-9，gtol=1e-5，maxls=20，maxcor=10；解析梯度 |
| 优化结束 | 仅接受 success=True；达到调用/迭代上限或异常则保存并停止，不重启、不补点 |
| 特征标准化 | 用 [30,792) 的均值和总体标准差（ddof=0）；标准差≤1e-12 时分母取 1 并记录，不删特征 |
| 残差缩放 | 每点训练残差 RMS，不中心化；RMS 分母下限 1e-6 mm 并记录是否触发 |
| 覆盖偏差 | 每点开发 90% 覆盖率距 0.9 的绝对偏差，≤概率对照偏差+1/376+1e-12 |
| 加宽保护 | 宽度比对照增加超过 1e-6 mm 时，该点 90% 区间评分必须降低超过 1e-6 mm |
| 均值/评分效果比较 | 原计划 1e-6 mm；所有平均改善与逐点不退步条件保持 |
| 模型重载 | 均值、潜在/观测方差绝对差≤1e-10（分别 mm、mm²） |
| 独立矩阵核验 | 均值绝对容差1e-6 mm、方差1e-6 mm²，均另加相对容差1e-8 |
| 分数复算 | 绝对容差1e-9 mm，覆盖率1e-12 |
| 微小负潜在方差 | 仅允许≥−1e-10×max(1,核幅度)的归一化舍入负值归零；越界失败，原最小值保留 |

## 已定位的五特征

实际保存数组具有 mean[1168,4]、moisture[1168,4]、rain_head[1168,4]、
reservoir_head[1168]、forcing[1168,2]、dates[1168] 和 theta[54]。
点序按原物理包及已有评分源码固定为 ATU1、ATU5、MJ3、MJ1，不用文字列举次序推断数组列。

1. 时间：从 2016-07-01 起的零基日序号 0…1167。
2. moisture 四代表域算术平均。
3. rain_head 四代表域算术平均。
4. reservoir_head − forcing[:,1]，即保存的滞后水头减当日库水位。
5. forcing[:,1] 当日减前一日；第0日置0，之后不填充、不截断。

这些字段语义与原 creep_workflow.py 中保存路径及水文特征接口相符；实施继续核对来源哈希、参数和 e0 均值。
不调用原 prepare_reference，因为它会运行 C 求解；新接口只读取冻结数组。

## 预算与顺序

本轮从 **2026-09-12 17:32:19 UTC** 起计，总截止 **19:32:19 UTC**。
准备与必要检查最多75分钟，正式启动最迟18:47:19 UTC；正式运行至多30分钟且最迟19:17:19 UTC结束，
保留至少15分钟完成复算、报告与提交。等待缺失参数的回复同样计入原时限，不重置额度。

本表已获确认，先冻结配置并 commit，再完成合成检查和一次运行；
全部四点预测锁定后才读取开发标签，不读取最终293日标签。既有 v2.3/v2.4 停止状态保持。

## API 核对边界

已阅读 scikit-learn 1.7 GPR 文档关于零先验均值、核超参数、WhiteKernel/alpha 的相关段落，
以及 SciPy L-BFGS-B 文档的迭代与目标调用说明。当前环境实测 NumPy 2.2.6、SciPy 1.15.3、scikit-learn 1.7.2。
这些文档支持 API 用法，不为上述初值、边界或效果门槛提供最优性证明。

- https://scikit-learn.org/1.7/modules/gaussian_process.html
- https://scikit-learn.org/1.7/modules/generated/sklearn.gaussian_process.GaussianProcessRegressor.html
- https://docs.scipy.org/doc/scipy-1.15.3/reference/optimize.minimize-lbfgsb.html
