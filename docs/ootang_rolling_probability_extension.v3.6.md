# 同一自主预算内的追加概率研究：C7

## Material Passport

2026-09-13；承接用户“约 8 小时自主跑任务，找到适合的方法，每三四次迭代纠偏、每步 docs 和 commit”的持续授权。原总起点 **2026-09-12 21:27:52 UTC**、截止 **2026-09-13 05:27:52 UTC** 不变。本文件是新的探索性方案，不改写 v3.0—v3.5 的冻结配置、预测、评价次数、阈值或结论。

## 对停止安排的修正

六候选上限是助手制定的资源安排，并非用户把自主工作限定为六次。六轮后已按约定复盘；但完整目标未达到、总时间未耗尽，不能把这一自定上限当作授权结束或任务成功。本次在同一总预算内将累计主要候选上限增加到 **9 个**，已完成的六个仍计数，第九个后再次书面复盘。每个新增候选仍须有具体证据、事前方案和一次运行，不自动用满九个。

原 v3.0 的唯一后期选择已经用于 C4，该阶段的 24/27、覆盖不足结论永久保留。C7 的后期结果属于**已暴露数据上的探索性复核**，不具有新的独立迁移或盲测地位；不得改称原方案第二次“通过”，也不重置原 293 日固定起点任务。这项解释降级在运行前确定，不因结果优劣改变。

## 唯一假设与已有依据

C4 的均值已有收益，但后期 90% 区间只覆盖 80.40%；其校准把误差历史压缩成二阶矩，再通过延迟反馈调一个高斯尺度。C5/C6 仍使用高斯分布和两个尺度参数，没有检验误差分布形状。因此，本候选只检验：**相同已经兑现的标准化误差历史，保留经验分布形状是否比压缩成高斯二阶矩更适合概率预测。** 不预先断言误差非正态，也不把覆盖不足自动归因于分布形状。

[Forecasting: Principles and Practice 第 5.5 节](https://otexts.com/fpp3/prediction-intervals.html)给出基于历史误差产生预测分布和分位区间的思路，并强调未来与历史误差可比等假设。本候选直接使用各步长已兑现误差的经验分布，不复现该书递归路径模拟；时序相关、分布变化和对称性限制仍存在，不引用独立同分布或 conformal 的无条件覆盖保证。

## 固定算法及配对对照

均值、原始标准差和物理教师全部复用 C4 冻结预测；FULL、DATA、B_ANCHOR 及全部固定简单对照使用同一校准算法。B_RAW 继续只报告均值指标，概率不适用。没有新均值拟合、神经更新、alpha 搜索、尺度优化或物理求解。

对每点、每步长分别维护最近 **90 个**已兑现预测的标准化绝对误差 `r=abs(y-mu)/raw_sigma`。本阶段开始时，用最近完整历史评价段同一模型身份的最近 90 个合格误差初始化：开发用 C3 的 612→792 日内部预测，后期探索用 C4 的 792→1168 日开发预测。初始化标签全部不晚于本阶段训练截止；校准历史的模型训练前缀、alpha 条件选择限制明确保留。后期初始池不会使用本阶段任何标签。

每个新预测使用当时的均值 `mu`、原始标准差 `s` 和池中 90 个数，形成 180 个等概率支撑点：`mu+s*r_i` 与 `mu-s*r_i`。对称构造严格保持分布均值为原机器学习均值。CRPS 对完整离散分布解析计算；80/90/95% 区间取对应中心分位数，概率恰落在经验 CDF 跳点之间的平段时取两相邻支撑点中点，数值规则固定。不添加极端点、放大倍率、尾部参数或人工截断。

同时构造 **MOMENT_G** 配对对照：使用完全相同的原始预测、历史池、更新时间和初值，仅把分布替换成均值 `mu`、标准差 `s*sqrt(mean(r_i^2))` 的高斯；零方差时使用确定点分布的极限评分。它用于区分经验分布形状的作用。旧 C4 另作原始参照，不能把新历史池初始化带来的变化全部归功于非高斯分布。

每次预测发出并锁定全部模型后才释放下一条位移；仅把此时到期的误差放入后续预测池。过去残差使用当时实际发出的均值和 raw_sigma，不以当前模型回算。保存完整分布参数、初始误差池、预测锁定记录和最终池状态，独立复算时不用保存的评分作输入。

## 评价与边界

- 数据、四点、每日起点、已知未来驱动和 30 日主步长不变，完整报告 1—30 日；不改主日期掩码、不删尾段。
- 主候选固定为经验分布 FULL，DATA 保持消融；MOMENT_G 是配对诊断，不能在结果出来后互换主候选。最强固定均值对照身份继续是 DRIFT1。
- 开发与后期探索都按运行前固定方案执行，不根据开发结果选新参数、换方法或择优决定展示哪个阶段。各自 347/264 个主步长起点，原 27 项数值标准原样复算；原 C4 各项结果另列。
- 即使新增分布满足数值标准，也只能称探索性数值合格候选。原 v3.0 迁移失败、后期已暴露、完整物理增益和空间联合分布未证实的事实不改变。没有把“实现完成”或“多次查看后通过”改称独立效果证明。
- 全部模型相同的校准历史有利于算法对照，但历史误差来自较早训练量的模型、分布对称和仅使用过去已兑现误差仍可能限制效果；本次不为这些限制追加参数。

## 预算、核验与停止

本候选运行局部上限 **20 分钟**，同时受原总截止约束。实现、合成检查、一次运行原件、独立核验和结论分别留下 docs 与 commit。不复用 C5/C6 被禁止的迁移入口，新增入口明确为 C7 探索性复核，原 guard 不修改。

核验重点为经验 CRPS 与独立两两距离公式一致、分位数/零方差/对称均值正确、初始池与成熟误差的日期边界、未观测位移扰动不改变当前分布、全部旧均值/raw_sigma 不变、原始对照与完整评分可复算。实现测试不代替效果判断。

没有提高覆盖或未优于同历史池 MOMENT_G 时，不能声称形状假设得到支持。下一候选只在本次产生可具体检验的新证据时另写方案；不能因剩余时间而自动扩大模型、分布或参数搜索。此次无需等待新的逐命令批准，依据是用户仍有效的自主实验授权，不是新的预算批准。
