# 十五轮结果归并与 C8 统计复核

2026-09-13，第五次复盘 `8031656` 后的只读分析，不是 C16 或新选模。原八小时截止 05:27:52 UTC 保持，最多 20 分钟用于本次程序运行，准备、图件核验和提交另计入总时间。零训练、参数优化或物理调用。

## 交付与边界

从十五候选的冻结产物生成逐候选、逐阶段的完整 h30 主输出表，保存每行原模型名、各自 B+ 概率对照、原强简单对照、原 27 项判断与来源哈希。C1 的固定速度对照是 DRIFT14，C2 起为 DRIFT1；早期概率校准不同，不能仅按跨版本 CRPS 排名。C4 原一次迁移、C5/C6 未迁移与 C7 起已暴露探索明确分列，不制造缺失结果。另保存全部步长、对照和已存种子行，主表不选种子或拼点。

对已核验的 C8 原 NPZ 只读复算 FULL、DATA、静态 FULL/DATA、B_ANCHOR 和 DRIFT1 的完整步长评分及 h30 逐点误差。复用原 C4 分析公式与冻结统计配置：相同起点及四点配对、循环成块长度 30、2,000 次、seed=20260913、95% 百分位区间；再保留从首起点开始每隔 30 日的非重叠统计。固定比较 FULL/DATA 各对 B_ANCHOR、DRIFT1，FULL 对 DATA，在线各对对应静态模型。平均点 RMSE 与合并 RMSE 分开。

置信区间描述这些已暴露轨迹内的不确定性，不消除候选选择偏差，不是新盲测；不计算或宣称多重比较校正后的显著性。C8 只看已有有效候选，不改判原 FULL 主模型或 C4 唯一迁移。是否满足数值门槛继续按原保存决定判断，不能靠成块结果豁免覆盖。

## 图件约定

沿用本任务既有 Python/matplotlib。内部研究报告用途，无期刊投稿合规声明；输出可编辑 PDF/SVG 和 PNG 预览。定量网格风格，7 pt 以上文字，固定物理尺寸约 183×120 mm，保持全部可评分日期和四点。

证据问题：在线 Ridge 的平均收益在完整预测轨迹上如何表现，失败发生在哪里，是否随步长改变？每阶段三图：四点 h30 完整位移与 FULL 90% 概率区间；四点完整有符号误差（含 DATA 对照）；平均 RMSE、CRPS 和覆盖随 h1—30 的曲线。四点面板用于检验逐点保护，不能只画最好点；步长面板用于完整展示预定输出，不按成绩挑新的主步长。阴影是模型预测区间，不是置信区间或种子方差。

字段对应：`origins+h-1` 为目标日期；`mean`、`sigma` 为已发边际高斯参数，单位 mm；Date 来自固定原监测表。唯一日期掩码为目标索引小于阶段终点，h30 每点开发/后期 347/264 行；非重叠分析另有明确抽样规则，不能替代主图或主评分。图件不插值、不平滑、不裁尾或变换误差轴。

统计帮助函数沿用既有分析脚本，绘图结构按新的 C8 标签适配，原 C4 文件不改。导出前检查等网格面板对齐，导出后逐份核对 PDF 字号、碰撞及源数据一致性，并目视全部面板；不把几何检查当成统计或效果验证。运行前固定来源清单，运行后保留源码及依赖快照与结果哈希。
