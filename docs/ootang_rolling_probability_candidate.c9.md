# C9：按预测步长学习概率尺度

2026-09-13；第九个主要候选，原总截止 **05:27:52 UTC** 不变，完成后作第三次路线复盘。沿用追加方案的已暴露后期探索地位，旧 C4 唯一迁移结果不改判。

## 事前证据

C8 的独立批量解/时序核验通过，在线均值只有小幅收益，后期覆盖未改善。进一步从已保存的原初始化样本和 C4 均值/尺度参数按原公式重算，发现尺度失配已经存在于拟合段，不需要以预测失败猜测。

| 原模型训练前缀 | 1 日原始覆盖 | 7 日 | 14 日 | 21 日 | 30 日 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 792 日 | 100.00% | 99.85% | 97.55% | 86.59% | 69.81% |
| 1168 日 | 100.00% | 99.63% | 96.76% | 86.56% | 74.75% |

这是原始高斯尺度的拟合覆盖，**不是最终校准后预测覆盖**，也不是仪器精度。完整 30 步、四点、来源哈希与计算公式见[运行前尺度核对](../results/ootang_rolling_v3/20260913/c9_horizon_pre_run_audit.json)，新增拟合和物理调用均为 0。

源码中的原始方差为 `(a_p*target_scale_hp)^2+(b_p*q_nhp)^2+0.01^2`，四点各有两个系数，跨全部 30 步共享。虽然 target_scale 随步长变化，当前两个共享系数仍未使各步长拟合误差同时匹配。因此只检验**不同步长分别估计这两个尺度系数**；不预先保证这种拟合修正能改善预测。

## 唯一主要变化与固定对照

保持 C8 的全部在线均值预测不变，原始尺度改为 `(a_hp*target_scale_hp)^2+(b_hp*q_nhp)^2+0.01^2`。每点每步长单独用原初始化训练集的固定均值残差拟合两个尺度参数，目标仍为同一高斯 NLL；训练输入、特征/目标 RMS、原均值、查询边界与数值下限不变。

两臂、两阶段共有 **480 次**两参数优化，每次复用原初值 `[-ln(10),0]`、对数边界 `[-10,5]`、maxiter=100、maxfun=200、ftol=1e-10、gtol=1e-6。不新增初值搜索，不因警告扩边界或重启；所有收敛标志保留。没有重跑 C8 的在线系数更新、神经训练或物理计算。

初始化均值残差仍来自该阶段 C4 保存的原训练模型，阶段内均值则为已经因果生成的 C8 在线轨迹。这一固定初始尺度模型不随新的在线系数重新拟合；不把它称为完整联合概率训练或严格贝叶斯后验。

主候选 `C9_HORIZON_FULL`，DATA 为消融；原 C8 在线分布作为 `C9_SHARED_FULL/DATA` 配对对照，全部 C8 静态分布和简单对照原样保留。新的概率校准继续使用 C4 的 RMS 规则和 0.02 延迟反馈，只有已经发出且兑现的误差可更新；不加入 C7 经验分布，不调整窗口、反馈速率或覆盖门槛。

拆分尺度系数增加了自由参数，所以它不是匹配参数数量的消融；不能把拟合覆盖改善自动归因于发现了物理机制。是否值得保留必须由相同完整预测时段的评分和覆盖决定。

## 评价、核验与退出

四点、原数据、每日起点、已知未来驱动、1—30 日完整输出和 30 日主评分不变。开发/后期探索每点主目标仍为 347/264 个，原 27 项数值标准保持，最强简单对照固定为 DRIFT1。两个阶段均按此方案执行，不按开发结果改后期配置。

保存每个尺度头的优化记录、训练目标与来源、原始/校准后尺度、完整预测锁定链、逐点和汇总评分。核验所有均值和原对照精确不变、每步尺度 NLL/梯度及训练边界、仅对应步长的头生效、目标成熟顺序和独立 Gaussian CRPS/区间评分；原数值容差不变。

局部运行上限 20 分钟，同时受原八小时总截止约束。原先计算的 153,600 次 C8 在线更新不再次计入，C9 的 480 次尺度优化单独记账。没有目标同时改善则停止此候选，不再拆点、拼头、增加层数或扩大边界；第九个完成后按约定书面复盘，不凭剩余时间自动增加候选。

即使数值标准通过，后期仍是已经暴露的探索性复核，原 C4 独立于本轮追加选择的失败保留；本次不能自动证明物理输入额外价值、完整空间联合概率或原 293 日单起点效果。

## 实现准备

`code/rolling_probability/horizon_scale.py` 已实现独立入口：读取 C8 冻结数组和原初始化模型，先核对来源及训练标签边界，再逐步长拟合尺度；全部均值与旧对照直接复用，保存每个优化头、预测锁和成熟误差反馈。没有改动原 Ridge、C8 在线更新或评分核心。

两项新增检查通过：单个尺度头只影响对应步长/测点，训练残差保持原标准化与固定均值。合成检查首次把 `exp(log(3))` 与整数 3 当成逐位相等而失败，差 8.88e-16；改为未变化单元仍精确相等、理论值比较 1e-14 绝对容差。仅修正新增检查的浮点预期，未修改原实验容差或生产计算。新增 lint 通过，尚未运行本轮优化。

## 一次执行结果

实现提交 `8c3819a`。运行原件：`results/ootang_rolling_v3/20260913/c9_horizon/`；2026-09-13 01:15:31.701333—01:15:34.231984 UTC，共 2.5305 秒。480 次优化全部按保存标志收敛，共 7,922 次目标调用、6,506 次迭代；新增均值/神经更新和物理调用均为 0。

以下为完整 30 日主预测端点、四点平均，误差和评分单位 mm；HORIZON 是 C9，SHARED 是原 C8 在线分布。

| 阶段 | FULL 分布 | 平均 RMSE | CRPS | 90% 覆盖 | 90% 宽度 | 90% 区间评分 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 开发 | SHARED | 1.078066 | 0.573748 | 87.25% | 3.984749 | 6.467871 |
| 开发 | HORIZON | 1.078066 | 0.586071 | 88.04% | 4.342857 | 6.634949 |
| 后期探索 | SHARED | 1.751943 | 0.819849 | 80.40% | 4.036937 | 8.489995 |
| 后期探索 | HORIZON | 1.751943 | 0.867595 | 82.58% | 5.981258 | 8.947460 |

C9 开发 27/27 项通过，后期 25/27 项通过；后期平均覆盖 82.58% 和 ATU5 的 75.76% 未过。其余后期三点覆盖 ATU1 81.44%、MJ3 85.61%、MJ1 87.50%。相对 SHARED 覆盖有所增加，但 CRPS 和区间评分均退步，不能把区间变宽当作完整改善。均值保持 C8，开发/后期合并 RMSE 分别为 1.345994/2.142781 mm。

DATA 消融开发/后期平均 RMSE 为 0.551379/1.729787，CRPS 为 0.364746/0.855832，覆盖 88.26%/82.95%；仍不支持 FULL 的物理输入额外价值。以上是运行保存值，接着进行独立 NLL/梯度、尺度/因果时序与评分核验；没有宣布整体目标通过或继续调参。

原件提交 `bd3232a`。只读复算器 `scripts/verify_ootang_horizon_scale.py` 已实现：独立标量 NLL 与复数步长梯度、全步长原始尺度、已成熟误差反馈、所有原对照/均值不变、完整事件链和 27 项判据核对。复用之前独立核验器的高斯评分和时序算法，不导入训练入口或重跑优化器；脚本及其依赖随核验结果保存。格式、lint 与语法检查通过，下一步运行实际数组复核。
