# 独立TCN预测：核验与解释边界

## Material Passport

用户明确要求参照导师图开展8:2无位移反馈的新实验。执行前冻结 `35a33c3`，推断实现 `44654bb`；历史校准锁 `0ce55a0`、完整293日分布锁 `53237e8`，之后才读取本轮评分标签。只重用已有18份三前缀/两臂/三种子固定权重及3份岭回归模型；新增拟合和优化器更新均为0。此处采用 academic-research-suite 的验证与解释风险核对，研究结论来自项目保存数组，不来自文献类比。

预测实现、来源、保存数组复现和评分一致性为 VERIFIED；这个状态不代表预测有效、原始数据as-of得到验证，或通过导师验收。关键数值见[核验回执](../results/ootang_tcn_independent_v1/20260914/verification/receipt.json)与[交付核对](../results/ootang_tcn_independent_v1/20260914/verification/delivery_receipt.json)。

## 11项解释风险核对

| 风险 | 本次证据与边界 |
| --- | --- |
| Simpson/聚合反转 | 两版TCN相对B+与DRIFT1的MAE/RMSE/CRPS/90%区间评分在四点都退步；完整逐点表保留。直接版对RR的区间评分只在两点较低，不把平均优势推广到所有点。 |
| 生态谬误 | 四点平均不代表某点或其他滑坡；合并RMSE与平均逐点RMSE分列。 |
| Berkson/选择偏差 | 固定同一藕塘案例及四点，日期已参与历史研究，属于探索性迁移评价。 |
| Collider/调整偏差 | 没有开展控制变量因果分析；相同输入的输出残差配对不能识别物理机理贡献。 |
| 基率忽略 | 没有失稳事件/预警标签，不推算灵敏度、误报率或工程可靠性。 |
| 向均值回归 | 完整293天和同期对照全部评分，没有选取最坏时期作前后改善比较。 |
| 幸存者偏差 | 五种方法、三个种子及完整日期保留；即使效果差也完成预定比较，没有以有限前缀替代完整评分。 |
| 多重搜寻 | 本轮固定既有400次检查点，没有后期选模或显著性检验；旧窗口和超参选择暴露仍属限制。 |
| 分析路径自由度 | 7天块、293日长度、固定未来驱动、90条同距离误差池均事前冻结；绘图重排与审计计数修正不改变预测或分数。 |
| 相关当因果 | 误差累积是当前递推结果；未分离模型权重、驱动场景和输入分布变化的贡献，不断言唯一失败成因或整个TCN家族无效。 |
| 反向因果 | 回放只读起点前历史，所有校准目标在1168之前；预测分布先锁定再评分，但不能消除原始数据实际可用时间和既有研究暴露问题。 |

长期区间按距离的90条独立无反馈回放误差校准，但这些回放彼此重叠，并非90个独立样本。部分早期回放使用的400次选择在历史上涉及其后的内部记录，权重前缀合法和整个选择过程独立是两件事；方案与结果均保留这一限制。没有将短期90条误差直接当作293日误差，也没有在预测期用兑现误差更新尺度。

三种子各自完成独立轨迹后平均，未拼接种子；两臂的配对种子四点平均RMSE均显示残差版更差，但MJ1种子0有局部反向，完整种子表保留。概率带是经验高斯边际预测区间，并非种子均值置信带、多点多日联合概率或覆盖保证。

8项针对性实现检查、382起点复现、294个最终递推块独立重放、240项点级评分及71197项后续数值核对已完成，新增训练0。生产物理前向388次（校准384、最终4），复算前向386次；实现阶段调用另见原检查回执。矩阵运算警告保存，有限值和数值一致不等于已修复底层库。

两张最终PNG已目视全部8个面板。第一次排版注释跨预测分界线；第二次将浅色网格也全部作为禁放区，因无可用位置中止；第三次使用真实数据/区间/分界线的显示坐标碰撞检查，移至左上空白数据区域。所有版本和日志保留。后续SVG核验首次忽略了多行文字拆成多个text节点，已按实际结构修正审计计数；曲线和指标没有重生成。

通用源代码静态审计17项PASS，两项PDF专用FAIL不适用于用户无PDF要求，两项TIFF/600dpi偏好WARN保留。本次实际PNG为300dpi，SVG含66/65个可编辑文字节点，中文字符覆盖、实际文字框、完整曲线/区间与1.5pt绘图区对齐通过；不声称完成PDF或期刊投稿成套验收。

本轮结果和图文交付完成后停止。后续若研究新的长窗训练目标，须单独提出并获新任务授权；不把本次负结果当作自动追加训练的理由。
