# Transformer 残差正则化核验 v1.0

## Material Passport

状态：EXECUTED / VERIFIED。固定9次训练、完整开发及最终评价、独立模型/数值/图文核验已完成；完整效果条件未通过，用户/导师验收未提供。依据冻结计划及配置，所有核验本地执行。下方训练前记录保留形成时状态。

## 训练前核验

- 779项冻结来源哈希一致；三套合法前缀教师缓存的完整日期、给定驱动、22维输入及前缀输入尺度核对通过。
- 5项聚焦合同检查通过：λ=0数据损失/梯度精确相同；λ=1平方完成恒等式与解析梯度；未来未释放标签/未来特征扰动对训练输入、目标及尺度无影响；非零网络因果性、零输出回退B+、保存重载及独立NumPy注意力一致；非法/非有限目标拒绝。
- 原版内部三个种子的e0与新构造的4916参数模型完全一致；每阶段实际训练前还会重新核对相应原版初始化。
- 三个既有非零内部e400检查点完整792日前向：NumPy与PyTorch最大差2.27e-13 mm，低于冻结1e-7 mm容差。
- 预核验仅解析前612日训练标签；不进行优化器更新、正式拟合或物理前向。
- ruff检查通过；训练核心和合同测试已冻结在implementation_lock中。后续核验/绘图入口可新增，不能改动已冻结训练核心来适应成绩。

凭据：[预核验回执](../results/ootang_transformer_regularization_v1/20260914/implementation_verification/receipt.json)、[合同输出](../results/ootang_transformer_regularization_v1/20260914/implementation_verification/unit_tests.txt)、[实现锁](../results/ootang_transformer_regularization_v1/20260914/implementation_lock.json)。

## 正式训练后核验

9拟合、3600更新和45检查点已完成，训练异常0，训练循环累计73.1539秒。每个检查点使用PyTorch逐数组精确重载及独立NumPy因果注意力全前向；归一化、零输出回退、三阶段全部9个初始化与原版e0相同、损失分解及所有逐点拟合指标已核对。

来源779项；独立核对696320项数值，最大绝对差5.46e-12，原模型前向容差1e-7 mm保持。40条事件确认内部→开发锁定→最终顺序：训练只读取对应前缀，均值/尺度先发出，评分后读取该阶段标签。三个阶段各3拟合1200更新；所有成熟校准池、单位、预测/概率指标、完整13380行逐日表、开发名单和门槛通过。原版/B+/DRIFT1/RR按原字节复用，旧模型拟合及物理前向0。

预定18组均值/尺度交叉、72点组合及2160条点日校准误差保留；用原评分实现再核864个交叉指标，和独立显式公式相差在1e-8容差内。原始分析CSV复制回读13470单元格；报告7表30行105个指标值、逐点方向、概率对照方向和16个本地链接核对通过。所有数值是同窗探索性描述，没有显著性检验或盲测声明。

凭据：[独立回执](../results/ootang_transformer_regularization_v1/20260914/verification_v1/receipt.json)、[图文回读](../results/ootang_transformer_regularization_v1/20260914/analysis/delivery_checks.json)。

## 图件核验与边界

两张图、8面板，完整1461日训练/预测曲线和293日比较均保留。23408个实际SVG曲线/区间值逆变换核对，最大量化差5.42e-6 mm，SVG专属序列化容差1e-4 mm；没有放宽原始模型容差。240×170mm、300dpi PNG、可编辑SVG文字及中文字符、坐标单位、RMSE注释、1.5pt面板对齐、实际文本碰撞和出界检查通过。两张PNG已由本轮助手打开并逐面板目视；未声称用户或导师已看过。

静态源码检查为16 PASS、3 WARN、2 FAIL，保留原输出。两个FAIL均只针对未导出PDF及未设置PDF字体；用户明确不制作PDF，本轮不适用。三个WARN为未出TIFF、300而非600dpi，以及静态解析器把`240/25.4`中的240误解为英寸，误报6096mm；实际SVG/PNG尺寸已复核240mm。本轮是导师报告图，不声称满足特定期刊最终投稿规格，也不声称全部静态检查通过。

目视说明：正则版训练曲线没有重新锚定；最终宽区间完整显示。比较图保留五种方法、困难尾段和原版略优的局部；原版红线与正则蓝线局部重合属于实际近似预测，两者图例/CSV均保留。图件合同与来源数组随交付保存。

## 11项统计解释检查

| 检查 | 本轮处理 |
|---|---|
| Simpson悖论 | 同时报两阶段和四点；最终平均提升不覆盖MJ3失败。 |
| 生态谬误 | 四点来自一个案例，不推广到滑坡总体。 |
| Berkson偏差 | 点和日期由原协议固定，不因预测表现入选或剔除。 |
| 碰撞变量偏差 | 不按效果门筛选进入最终的方法，固定候选完整运行。 |
| 基率忽略 | 当前未做预警分类/事件概率，不推断预警有效性。 |
| 均值回归 | 原版因结果差被继续检查；本轮正信号受历史暴露限制，不当作独立重复证据。 |
| 幸存者偏差 | 三种子和全部日期保留，种子1配对退步及MJ3失败均列出。 |
| 多重寻找效应 | 当前只一固定λ，但历史多候选和已看过窗口使结论保持探索性；无未经调整的显著性声明。 |
| 分析路径分叉 | λ=1、400更新、评价和交叉诊断事前提交，不据最终成绩改规则。 |
| 相关与因果混淆 | 受控目标变化支持本次数值配对描述，不证明滑坡真实物理因果或过拟合的唯一成因。 |
| 反向因果 | 模型因果掩码/前缀标签边界已核验；不把拟合出的时序依赖当成机理方向证据。 |

本轮执行和核验完成，正则版相对原版两阶段配对改善；相对B+的完整效果目标未完成。按计划停止，不添加候选、λ或更新次数，旧结果保持。
