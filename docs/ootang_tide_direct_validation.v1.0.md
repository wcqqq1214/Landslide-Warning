# TiDE小试独立核验

数值核验完成：261项冻结来源保持，24次正式拟合/9600更新/120检查点、四条完整293日路径全部保存。正式拟合失败及重试0；本轮B+重拟合和物理前向均0。旧B+、起点校正B+、DRIFT1、RR_COND、G_CACHED均值/种子及校准尺度保持原值。

最终audit_v2执行20001项检查、93991564个数值比较，最大绝对差2.1600499167107046e−12。独立重建两臂全部3632个起点输入、目标和成熟掩码；1520个“起点×方法”具完整293日监督，不能当作独立样本。每个检查点独立NumPy前向与PyTorch重载一致，h0、初始参数、抽样、首步目标、Adam步数、种子集成均核对。

84逐点/21平均/63种子汇总、24612点日、1008边界误差、360配对差和33项比较门独立复算；上一已发报路径第91—180日误差校准及最终16日间隔符合合同。62个事件记录确认全部四条发报锁定后才解析完整1461行评分标签。训练中不读取前缀后位移或协变量，DATA在教师为空时仍可构造全部输入。所有标准化只用当前拟合前缀。

预检的87项/399342数值通过。原SciPy二进制兼容异常、初版非实质NumPy浮点状态告警与独立重验已留档；原环境不改。audit_v1已通过，随后仅将CSV读取lambda按lint改为函数并增加源码哈希记录，audit_v2完整复算保持同样数值，无新训练。

图件核验另见本轮figure_qa回执及visual_review，只有完成其数值/渲染/目视核对后才认定图件可交付。数值实现正确不等于模型效果达标，也不代表用户或导师验收。逐点保护、已暴露且重叠的窗口、612长距监督不足、训练尾部协变量掩码、原物理与数据限制全部保留。
