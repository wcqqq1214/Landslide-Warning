# 历史教师更新配对：实现与训练前核验

## Material Passport

固定方案实现；尚未进行正式物理拟合/神经训练。本文件随实现锁在运行前冻结。来源计划提交bafd5a0d；203项旧代码、数据、教师、物理求解器及对照数组哈希保持。用户整体执行授权覆盖本轮实现与运行，旧停止记录不阻止本次独立实验。

## 实现

独立入口code/teacher_refresh/：core.py显式区分旧缓存/起点更新教师；两臂共用模型创建、14维物理/历史特征和评分接口，不修改旧入口。teachers.py只接收注册前缀的位移与驱动，原合法教师作初值，单阶段800次上限；每次保存全状态与来源。run.py按612/792/972/1168顺序准备需要的教师、训练两臂并锁定已发出预测；全部完成后另行score才读1461日位移。

G_REFRESH的教师变化会同时改变物理派生输入和残差目标；当前外层教师归一化、网络和采样保持共同。新历史教师只用于m<n的训练查询；外层972继续使用原792教师，最终1168保持原1168教师。完整历史物理状态从首日延续，无路径位移反馈。

每个正式神经检查点保存模型和Adam状态。24拟合×200更新，96检查点；固定e200，不用历史/最终成绩改选更新次数。预检模型只有零头/人为设定头的前向和梯度检查，未执行optimizer.step，不计作正式拟合。

## 预检结果

preflight_v2/receipt.json：149项、283481个数值比较通过，最大差7.15e−11，均在各自原定容差内。包括12份共同采样表、0/2/5/8完整训练窗计数、教师拟合边界、未成熟标签拒绝、输入未来位移污染、相同初始参数、旧缓存适配一致、1241参数、零头回退、独立NumPy前向、权重精确重载及一项头参数中心差分梯度。

两次固定物理前向检查历史前缀不受未来驱动影响、目标向量/终点权重、上下界差分方向。首次预检147项通过，但原程序输出矩阵乘法运行时警告；v2新增液压力矩阵和位移投影的显式逐项求和核对，并保存完整日志。149项检查通过不等于修复底层库警告；正式物理阶段也会保存警告、检查全状态有限值与显式乘法一致性。原首次通过记录保留。

合成刷新教师仅用于证明路由和未来信息隔离，不保存到正式教师库，不用于训练或结果评分。预检新增B+拟合0、神经拟合0、优化器更新0；每次预检物理前向2次，共4次。

## 固定运行与限制

在仓库根目录使用PYTHONDONTWRITEBYTECODE=1、PYTHONPATH=code和.venv/bin/python，依次运行模块teacher_refresh.run的train、score命令。

只有完整检查点/发报锁通过才能复用已完成步骤；不覆盖半成品或自动重试数值失败。单纯触及最小二乘800次上限，若原数值条件仍合法，按计划保留输出并继续；收敛和效果单列。进程/日志由当前任务持续监控，每次目标调用和训练更新检查13:01:47 UTC硬截止。窗口已暴露、重叠、教师工况少及旧预处理限制保持；全部结果仍为探索性。
