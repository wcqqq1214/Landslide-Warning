# 训练充分性核验与固定预算对照：验证记录 v1.0

## Material Passport

Verification Status：**VERIFIED（实现、保存权重/预测与数值复算范围）**；效果判断为探索性。24次条件性拟合全部完成，未按成绩停止或挑选种子；导师/用户尚未验收。核验程序自身新增拟合/优化器更新0。

## 1. 核验范围

| 阶段 | 检查记录 | 数值个数 | 最大差 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 实现预检 | 89 | 538340 | 1.11e−16 | 通过 |
| 原96检查点诊断复算 | 2330 | 401128 | 4.55e−13 | 通过，触发固定追加 |
| 72新检查点及完整评价 | 3968 | 1265488 | 2.16e−12 | 通过，效果不稳定 |
| 四图16面板 | 481 | 232804 | 数据2.14e−14；SVG7.21e−7pt | 通过，含实际导出坐标 |

- 1329冻结来源在诊断前及交付时核对；旧计划/配置/教师/数据/结果保持。96旧检查点包括两起点臂×四前缀×三种子×四训练次数；72新检查点包括两臂×四前缀×三种子×0/200/400。
- 128×32面板日期、全部目标、损失MSE/正则项/J及其代数分解完整核对。每个检查点固定4个面板起点作独立NumPy非线性前向，全部168条保存的完整预测路径独立重放。未把抽样前向描述成全128个起点均由独立网络实现重算。
- 固定面板梯度范数经PyTorch求值、有限值及权重逐项未变检查；未做独立数值差分梯度。触发不使用梯度范数。
- 训练教师拟合日≤伪起点、标签在训练前缀内成熟；每个起点的输入不读未来位移，污染测试通过。当前前缀归一化没有冒称当时伪起点的独立在线归一化。
- 触发规则用e100/e200及完整成熟的612/792路径，未使用最终标签或截短972窗口。合成正负例、降低惩罚但拟合变差、加入最终窗行均按预期处理；独立评分和独立条件表达重现触发true。
- 新24次训练的前200步抽样、损失/梯度日志、参数、预测均与原件精确一致。Adam状态从原初始化重建并在新0/200/400检查点保存；9600次实际更新包括4800重放及4800追加，所有优化器状态步数/有限值核对。
- 新四起点完整293日路径及误差池锁定后，执行进程才解析全1461日位移评分；既有历史与最终结果此前已暴露，不因此称为盲测。全部24汇总、96逐点、72种子、288种子逐点、28128点日及57配对门复算。
- 四张最终v3图均检查源数据、实际SVG路径、所有科学标记在轴范围内、可编辑字体/字号、1.5pt对齐、文本碰撞及逐图目视。四图碰撞FAIL/WARN均0；最终PNG300dpi与SVG按报告合同交付，临时PDF只作QA载体。

## 2. 配置与异常解释

本轮冻结JSON从上轮配置继承接口参数。本轮执行范围由`arms`、`diagnostic`、`trigger`和`conditional_extension`明确指定；继承的上轮`new_arms`、`reuse_arms`、`historical_controls`、`reporting.paired_edges`等字段未驱动本轮入口。未修改冻结JSON掩去继承项；实际目录仅G10/T10两起点臂，旧raw两臂没有训练。根`updates=200`代表旧检查点基准，新阶段固定`conditional_extension.updates=400`；预算上限为24拟合/9600实际更新。入口调用及全部训练日志支持这一解释。

1. 第一次只读诊断审计的CSV精确比较发现5.68e−14mm差异；默认pandas解析未保持17位文本往返。改为`float_precision='round_trip'`后通过，目标/预测/容差/触发不变。原错误JSON和日志留存。
2. 图v1预算面板a在添加数据前设定纵轴下界，关闭自动范围后成为空面板。目视发现后，v2改为数据添加后设置下界，并增加所有科学标记必须在轴内的核验。
3. 图v2预测区内注释触发绘制碰撞门。v3把注释移到绘图区下方，保留完整共同轴范围；旧初稿和失败回执留存，训练/评分没有重跑或改变。

正式24次拟合均一次完成，没有失败拟合重试；核验与制图的修订不是模型调参。旧物理优化未收敛/as-of/预处理记录保持。

## 3. 统计解释检查：11/11

| 项目 | 本轮处理 |
| --- | --- |
| Simpson/聚合反转 | 同时保留窗口、点、种子；明确最终MAE略降而RMSE升高、2/3种子改善不等于集成改善。 |
| 生态谬误 | 结论限于单案例四点及固定小模型，不推广网络家族或总体。 |
| 选择偏差 | 固定公开案例、既有窗已暴露；不称具有总体代表性。 |
| Collider偏差 | 不做观察性协变量调整或因果效应估计。 |
| 基率忽视 | 不做分类/预警，没有用位移区间推导真实预警准确率。 |
| 回归均值 | 固定同种子预算配对、完整窗口，不按最差点或极端种子筛选。 |
| 幸存者偏差 | 全部24拟合、三种子、完整293日保留；负结果未删除。 |
| 多重寻找 | 事前固定唯一400次预算，不根据最终结果新增800次或选最优检查点。 |
| 分析分叉 | 先冻结规则再看固定面板；承认旧成绩已暴露，结果为探索性。 |
| 相关与因果 | 预算变化的本地计算对照不等于滑坡物理机制证明；教师迁移差异仅为限制。 |
| 反向因果 | 位移输入/成熟标签时序核验，给定未来驱动的条件预测与实用在线预测区分。 |

90个相邻误差、3个种子及重叠窗口不是独立重复，不报告p值、显著性或参数置信区间。80/90/95%为边际预测区间，不是整条293日路径同时覆盖。未证明全局最优或充分收敛；本次追加未建立稳定收益。

[预检](../results/ootang_training_sufficiency_v1/20260915/preflight/receipt.json) · [诊断复算](../results/ootang_training_sufficiency_v1/20260915/diagnostic_audit_v2/receipt.json) · [追加复算](../results/ootang_training_sufficiency_v1/20260915/extension/audit/receipt.json) · [四图核验](../results/ootang_training_sufficiency_v1/20260915/figure_qa_v3/receipt.json) · [目视记录](../figures/ootang_training_sufficiency_v1/20260915/v3/visual_review.json) · [结果](ootang_training_sufficiency_results.v1.0.md)
