# 原 TiDE_KIN 距离诊断：图形契约 v1.0

在制图代码前固定。沿用Python/matplotlib统计制图与nature-figure核验；本轮为本地研究图，240×170mm、PNG 300dpi、可编辑SVG，四图各四面板。临时同源PDF仅在/tmp执行字体/碰撞检查，不交付PDF。无生成式图片、平滑、插值、日期删除、隐去不利种子或截断数值。

| 图 | 单句问题及面板作用 | 编码及汇总 |
|---|---|---|
| supervision_weights | 完整远期监督在各训练前缀中有多少？四前缀依次为启动缺失、第一主窗、第二主窗、最终窗 | h1—293；293×距离损失系数，1代表各距离均匀。理论期望黑实线，e400实际三种子分别细线；保留全部距离与零值。不是梯度大小。 |
| error_by_distance | KIN失误是否只在远期出现？四点分别检验，避免四点平均掩盖 | 固定四距离段；三主窗颜色，KIN实线、B+虚线、DRIFT1点线，逐点集成RMSE。这里集中三种预定关键参照，全部六方法/各种子完整CSV照常交付；图上不加基于种子的置信区间，因为展示的是原发报集成预测对实测误差，种子离散另由检查点图及MSE表呈现。 |
| checkpoint_fit | 原训练拟合改善是否伴随原发报改善？四前缀完整呈现 | 固定0/50/100/200/400更新；训练与发报均先逐点RMSE平均、再取三种子均值，个别种子散点全部显示；发报集成另用黑虚线，不能将训练种子均值叫训练集成。训练为成熟单元内拟合、发报为完整293日，两者样本集合不同。无新选模。 |
| visibility_sensitivity | 晚期协变量可见性会改变共同早期输出吗？四前缀对应输入分布差异 | d=30/90/180/293；四点不同颜色，集成预测变化RMS曲线与全部三种子散点。只在共同h1…d相对完整输入计算变化，不是对实测RMSE。d293为零变化对照；不同d使用不同日期集合，不能当等距离剂量效应。 |

三主窗为792/972/1168，均探索性；612仅启动诊断。模型原180日历史、e400主结果与三种子保持。相邻日、点、重叠窗口和随机种子均非独立科学重复；不报告p值或新显著性门槛。图例写明统计单位与汇总方式，不把敏感性解释为标签泄露、有效性或因果。

图形来源为已独立核验的diagnostic_v1 CSV。每条曲线保存来源配方及坐标；独立图形核验重算配方并检查实际SVG坐标、全范围未裁切、字体至少5pt、1.5pt面板对齐、碰撞与全部PNG目视。所有WARN逐项说明或修复，修订保留前稿。绘图代码、CSV来源哈希、导出与QA记录分别锁定。图形层不新增科学结果筛选。
