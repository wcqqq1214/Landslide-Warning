# 神经初态耦合模型：独立续评补充协议

## Material Passport

- Origin Skill / Mode：academic-research-suite / experiment-agent / run。
- 用户已批准上轮提出的单候选续评及 PDF 同步，仅 `test` 本地提交，不 push。
- 起点：`c956401`；旧 v1.0 及软约束状态 PINN 的结果、配置、失败记录保持。
- 这是已知物理检查结果之后、尚未进行候选效果选模的验收修订。全部评价仍为探索性，不称新盲测。

## 1. 独立预算与唯一变化

本次窗口 **2026-09-13 21:05:18—22:05:18 UTC**，包含准备、实现、评价、条件训练、核验、报告及提交。计划最迟 21:15:18，预检最迟 21:20:18，训练最迟 21:40:18，剩余时间用于核验和 PDF。任何阶段超时即交付已完成结果和缺口，不恢复旧预算。

唯一模型仍为 `NIS_BPLUS`：原网络、四维有界 z 初态、完整记忆、原 54 参数、严格 C 日递推和原观测锚定均不改变。本轮**只修订塑性子步数值验收的单位口径，并补完原效果流程**；不学习新状态、力、参数或输出残差。

## 2. 容差换算与双重报告

原求解器子步变量满足 `delta_p_j = x_j / beta_j`，且 beta_j > 0。原数值门 `x_j >= -1e-8` 等价于逐坐标 `delta_p_j >= -1e-8 / beta_j`（mm）。本轮将这一按系数换算的条件作为续评数值可行性门，**统一用于零修正 B+ 和所有候选种子**，不依据已观察的负增量调整常数。实现直接核对每条轨迹的原 x 最小值与正 beta，并保存各坐标换算阈值；这与上述不等式代数等价。

原 `delta_p >= -1e-8 mm` 子步门继续作为独立严格诊断，保存原负增量、失败起点及计数，不覆盖或裁剪轨迹。日塑性门 `>=-1e-8 mm`、gap `>=-1e-7`、归一化互补积 `<=1e-7`、初态约束、完整状态与独立 C 重放误差 `<=1e-8` 均保持。

因此“通过”准确指**符合原求解器数值容差的物理递推**，不宣称实数意义上的绝对不可逆。三种子逐条核验；均值集成只作统计量，平均初态重放与平均预测的差另报，概率区间无物理可行性保证。

正式评分前，先对完整内部 180 起点的零修正控制核验，并用保存的独立反例核对容差换算及严格诊断保留。复用 v1.0 非零初态／梯度核验：模型、C 递推和反向实现未变，只核验来源哈希，不重新把抽检宣称为全域梯度证明。新物理门失败即停，不训练或跳过日期。

## 3. 内部复用、选择与条件训练

复用 v1.0 三种子 `[0,1,2]` 的 `[0,50,100,200,400]` 全部检查点、354 个训练查询和标准化，**不新增内部训练**。每个检查点逐种子输出锁定与物理核验完成后，使用原 v4.0 成熟误差概率层评分。内部选模在全部七目标可得的共同起点上，按原七步长平均 `Q=0.5*(RMSE/DRIFT1_RMSE+CRPS/DRIFT1_CRPS)`；`1e-8` 内并列取较早轮数，不逐点或逐 h 选检查点。

e0 最好、任一物理门失败或 Q 未严格优于 e0 `1e-8`，均停止并报告内部比较。否则按选定共同轮数，在 792 日前缀重新初始化三种子并完成开发评价。原开发门保持：MAE/RMSE 与 CRPS/90% 区间评分各平均改善至少 1%，原逐点保护和覆盖偏差规则保持；七 h 平均 RMSE 均不退步且至少 4/7 个 h 均值与概率同时过门，才进入 1168 日前缀后期一次三种子训练。

最多新增 **6 次拟合、2400 次更新**；Adam 0.001、batch 64、梯度裁剪 5、正则 0.01、单线程 CPU float64、单拟合最多 600 秒及其他原配置保持。无架构、权重、种子或轮数扫描；无训练重启。后期已暴露，完整保留探索性标签，不重选。

## 4. 日期、对照和概率

沿用 v4.0 四点、原数据、30 日历史、内部 612/180、开发 792/376、后期 1168/293 及全部合法起点。仅按 h 目标是否越界遮罩，不删尾段。未来驱动仍为过去七日平均降雨和最新库水位保持。

固定比较 B_ANCHOR、DRIFT1、C16_CORE_RULES、C16_PHYS_RULES、CL_DIRECT、CL_BRES；内部缺少同窗 C16 时如实记为不适用，不拿开发数字代替。概率层沿用 90 条已成熟预测误差的高斯校准，按原顺序先锁定当前分布再释放标签；前阶段预测用于后阶段初始化，内部以历史 DRIFT1 初始化，不使用拟合误差。

每 h 分别报告四点及平均 MAE、RMSE、CRPS、80/90/95% 覆盖率、宽度和区间评分，合并 RMSE另列。B+ 与候选采用同日期掩码和上述数值物理口径，原对照预测不重训。均值改善、概率改善、物理检查和工程预警分开结论。

## 5. 保存、核验和交付

新产物根 `results/ootang_neural_initial_state_v1_1/20260914`；旧目录只读。先冻结本计划、配置、旧输入／检查点哈希并 commit，再实现和执行。内部训练文件复制后逐字节核对，不能把复用计为新拟合。每个阶段保存全部种子轨迹、初态、双重物理检查、尺度、发出锁、指标与决定。

D 对实际完成的全部阶段重载模型，独立原 C 重放，重建成熟误差尺度与日期锁，并独立复算保存指标和去留规则。仅核验已完成阶段；因效果门停止时不自动开展后续训练。停止结果也是有效交付，但物理失败、预测失败、缺少评价三种情况分别标明。

交付结果 Markdown、完整已运行 h=1—7 比较及现有四页 PDF 的对应更新；软约束状态 PINN 与初态耦合模型分列，不覆盖旧失败。报告保留公开日序列结构、真实 as-of 未知、反复探索及真实预警未验证限制。每完成一步独立本地 commit，排除无关 Vajont Excel，不 push。
