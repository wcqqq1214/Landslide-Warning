# 起点条件化Transformer核验 v1.0

## 训练前实现说明

计划中432教师“延长至725”为覆盖长度笔误：该教师用于伪起点432—611，而不仅432单一起点，最大目标索引为611+293−1=903，因此数组实际延长至904行。此修正于正式训练前明确，原计划字节保留；伪起点、预测距离、参数、更新次数与两次物理前向预算均不改变。612教师至1084行保持。代码实现以该说明和实现锁共同约束。

检查：完整保留历史日token；NO_OBS仅屏蔽8个历史观测通道，POOL使用均匀历史池化；合法teacher、目标索引、未来位移隔离、非零历史输入正控、零残差B+回退、重载、单query/批次一致、有限梯度及旧教师重叠前缀核验。实际数值与状态见preflight/receipt.json；尚未填写执行/效果结论。

ARS作为执行和研究解释辅助；本次用户已授权实现及固定运行，不逐命令再问。图件沿用Python/nature-figure，用户无PDF要求优先，未宣称PDF专用检查通过。
