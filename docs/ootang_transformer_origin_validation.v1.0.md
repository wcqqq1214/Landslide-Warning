# 起点条件化Transformer核验 v1.0

## 训练前实现说明

计划中432教师“延长至725”为覆盖长度笔误：该教师用于伪起点432—611，而不仅432单一起点，最大目标索引为611+293−1=903，因此数组实际延长至904行。此修正于正式训练前明确，原计划字节保留；伪起点、预测距离、参数、更新次数与两次物理前向预算均不改变。612教师至1084行保持。代码实现以该说明和实现锁共同约束。

检查：完整保留历史日token；NO_OBS仅屏蔽8个历史观测通道，POOL使用均匀历史池化；合法teacher、目标索引、未来位移隔离、非零历史输入正控、零残差B+回退、重载、单query/批次一致、有限梯度及旧教师重叠前缀核验。训练前实际数值与状态见preflight/receipt.json。

ARS作为执行和研究解释辅助；本次用户已授权实现及固定运行，不逐命令再问。图件沿用Python/nature-figure，用户无PDF要求优先，未宣称PDF专用检查通过。

## 固定执行与独立复算

运行提交 `d46af99` 保存全部36拟合、7200更新、144检查点和全部预定293日比较；所有预测先锁定，再读取完整外层标签评分。无训练失败、重试或因效果提前停止。两个新增物理前向的底层矩阵警告原样保留；教师数值有限、旧前缀重放一致，不宣称底层库警告已修复。

只读 `PYTHONPATH=code .venv/bin/python -m transformer_origin.audit` 于18:05:19 UTC通过，回执为 `results/ootang_transformer_origin_v1/20260914/independent_audit/receipt.json`。核对253来源、144权重精确重载、独立NumPy交叉注意力/池化完整前向、36拟合日志、全部抽样索引及三臂一致性、训练前缀标准化、教师边界、成熟误差池、旧基线数组、83事件时序，以及全部27汇总/108逐点/81种子/31644点日记录与原门槛。共9027212个数值单元，最大差2.16e-12，零新增训练/物理调用。

NumPy复算不调用模型forward；线性层、GELU、LayerNorm、相对位置、两头注意力和池化分别重建。全部144检查点与保存预测一致，不仅检查最终权重。pandas发出一次索引排序性能提示，核验结果通过，未改训练/评分数据以消除提示。

执行与数值核验成功不等于有效：三臂只在972历史窗通过均值门，最终均值和概率门均未通过；研究结论见结果报告。用户/导师尚未验收。
