# 训练预算核验图件约定 v1.0

## Material Passport

Python/matplotlib，沿用本任务既定后端。数值诊断与固定400次对照均已独立复算；图不改变任何评分或选模。科研报告图件，不声称符合某期刊最终投稿规范；交付PNG/SVG，临时同源PDF仅作字体/碰撞QA。

1. **旧检查点诊断图**：检验启动依据。a固定训练面板MSE；b正则目标距逐样本自由输出代数下界的距离；c/d两个完整成熟历史窗RMSE。训练指标先按各前缀e0归一化，再四前缀等权；薄线为三种子，粗线为种子均值。历史粗线为先平均预测再评分，薄线为逐种子。只到200次，不将400次倒灌进原触发。
2. **预算对照图**：检验“多训练是否解决迁移”。a三个完整窗的四点平均RMSE与B+；b同种子400减200的配对RMSE；c最终逐点90%覆盖；d相同固定训练面板MSE的200→400变化。训练继续改善与外推失效并列，不将平均MAE的局部改善解释为RMSE改善。
3. **GRU与Transformer各一张四点曲线**：给导师检查最终293日走势和区间。实测黑线、完整B+灰虚线、200次均值虚线、400次均值实线、三种子400次细线及400次80/95%区间。训练段只显示实测与B+拟合，不制造神经训练段重构线。两图各点共用完整范围，包含全部观测、两预算种子和400次95%区间，无尾段删除或区间裁剪。

均为quantitative grid，240×170mm、PNG300dpi、SVG可编辑文字。8pt常规文字、最小7.4pt；每图测量最终面板矩形并过1.5pt对齐门、文本几何、实际SVG坐标与数据、同源字体/碰撞QA，再逐图逐面板目视。所有路径、种子、窗口保留；12组训练曲线汇总和重叠历史窗不是独立重复，不绘制统计显著性或参数置信区间。区间是固定高斯逐日边际区间，不是293日同时覆盖承诺。
