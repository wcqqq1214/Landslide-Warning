# 统一接口骨干×起点表达：核验 v1.0

## Material Passport

本地实现/结果重放验证；训练实现abcc7b67、完整实验d49185e0。独立指单独NumPy公式/代码实现，不是外部审稿或独立研究数据。核验新拟合/优化器更新/物理前向0。

## 数值及复用

[预检](../results/ootang_backbone_anchor_v1/20260915/preflight/receipt.json)已复现旧GRU两臂96检查点、共享输入/解码器/初始化/抽样、稠密因果注意力与输入/参数梯度、padding和未来位移隔离、成熟目标、h0/零头回退与重载。

[正式独立复算](../results/ootang_backbone_anchor_v1/20260915/independent_audit/receipt.json)通过1106来源、10340记录/3813856数值、全部192份检查点（96新TF+96复用GRU）、87事件、60组配对判定和360行因子效应，最大差2.16e−12（各量原单位）。96旧GRU检查点在新公共接口的预测差0；新TF独立实现位置编码、LayerNorm、注意力softmax、GELU、公共图/解码以及起点差分。所有正式e200未来位移毒化均不影响输出。

独立从驱动/教师状态构造14维历史及h0/未来查询；核对训练前缀尺度、成熟距离、相同uniform采样、200条损失/梯度日志、所有种子平均、12方法的旧来源/基线和90条成熟误差池。高斯CRPS/区间评分/覆盖/宽度与门槛使用独立实现，36汇总/144逐点/108种子/432种子逐点、42192点日和1728端点逐项一致。四条发报锁均早于完整1461日标签评分读取；旧模型预检读取合法前缀是单独复用核验，不冒称数据从未暴露。

正式24次新拟合累计4800更新，另24份GRU历史拟合4800更新原样复用。新训练循环含检查点保存累计14.035595秒；这个耗时不包含初读、准备、独立核验或图文，整个任务用时见最终回执。全部预定实验完成，未因效果停训。Pandas未排序索引的PerformanceWarning保留，属于核验性能提示，逐项数值均通过。

训练前原参数加总笔误及实现锁路径封装失败见[实现说明](ootang_backbone_anchor_implementation.v1.0.md)，当时无拟合/更新，未调整模型或门槛。正式24拟合无中途崩溃或重试，不以预检通过替代效果。

## 统计解释：11/11检查

| 检查 | 处理与限制 |
| --- | --- |
| Simpson/聚合遮蔽 | 三窗、四点及三种子并列；TF起点表达最终平均RMSE下降，但ATU1/ATU5的MAE反而增加，不推论每点改善。 |
| 生态谬误 | 四点平均不推广至其他点、其他滑坡或真实预警。 |
| Berkson选择偏差 | 原uniform起点/距离不依胜负变化；教师工况少、数据固定，代表性有限。 |
| 碰撞变量偏差 | 非自然系统因果识别；物理/残差特征共用不证明机制。 |
| 基率忽略 | 未做预警分类，位移区间覆盖不冒充预警准确率。 |
| 均值回归 | 已失败窗口上提出新假设，旧数据已暴露；同条件配对有助于计算比较，不构成外部验证。 |
| 幸存者偏差 | 24新拟合/96新检查点、24复用拟合/96旧检查点全部保留，三主要窗/全部日期/种子均评分。 |
| 多处寻找效应 | 四组及配对事前冻结、无选模；历史路线多版本结果保留，不宣称新显著发现。 |
| 分叉路径 | 参数计数与锁路径问题在训练前留痕，结构/损失/训练次数及完整窗口不因成绩更改。 |
| 相关与因果 | 只比较固定模型的计算干预；A联动基线/目标/正则化对象，骨干因素还包含时间结构/位置编码且差8参数，不认定单一物理失败原因。 |
| 反向因果 | 教师/输入/成熟标签/校准时间核验通过；给定未来驱动是条件假设，不证明驱动物理因果。 |

没有p值或独立重复置信区间；三种子是算法随机性，四点同剖面，历史窗与最终重叠97日。区间是固定高斯边际区间，不是整条293日路径同时覆盖或参数后验。旧教师未收敛、原as-of/预处理、已知未来驱动、612训练最长180日和最终教师不同于episode教师等限制保留。

## 当前效果与下一阶段

TF起点表达三窗平均MAE/RMSE均改善，配对种子同向2/3、3/3、2/3；并非每点/每种子有效。在A开时，GRU三窗集成MAE/RMSE/CRPS均低于本轮TF，两个固定实现的比较更清楚，但不能推广GRU家族必然更好。四组B+完整联合条件均0/3，最终所有新/复用四组RMSE仍高于B+。旧Transformer半残差8.867132mm局部正结果保持，不据最终成绩重选。

图件与文档核验分别记录；用户/导师尚未验收。

## 图件核验

九张PNG/SVG、36面板已完成逐面板及整图目视；[图形核验](../results/ootang_backbone_anchor_v1/20260915/figure_qa_v2/receipt.json)共780896个数据/图形/尺寸值、1386项检查，原数据差0、SVG量化差≤7.2e-07pt。1.5pt对齐和全部字体/渲染碰撞检查通过，源码18 PASS/3 WARN/0 FAIL，三条显示合同提示（无TIFF、300dpi、240mm源表达式识别）由实际导出补证。

导师固定轴95%区间越界日数：G00为0/0/248/1，G10为0/0/52/0，T00为0/0/271/1，T10为0/0/8/0；点序ATU1/ATU5/MJ3/MJ1。所有观测/均值/种子均未超出固定轴；各组完整范围版包含全部区间。初版QA将PNG DPI元数据0.0006量化差归入源数据字段，v2分列DPI及数据误差并新增SVG高度检查；图件本身和训练评分未修改。无PDF交付，临时同源PDF仅供自动字形/碰撞核验。

## 文档与最终交付

结果报告9张表/53行、137个数值按CSV独立核对，按mm六位小数、覆盖率两位小数显示；配对种子/保护门、日期、叙述中的改善方向、README三窗表和170个本地链接通过。最终回执另保存实际新/复用拟合数、时间、交付哈希与步骤完成状态。仅在本地分步提交，未push、未新增训练或PDF交付。
