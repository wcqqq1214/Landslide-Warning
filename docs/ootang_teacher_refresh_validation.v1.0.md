# 历史教师更新配对核验 v1.0

## Material Passport

固定实验与独立复算均完成；效果目标未完成，用户/导师尚未验收。实现、训练和验证分别留档，不以正常退出代替预测效果。预检见实现文档；图形与文档一致性另记最终回执。

## 数值与信息边界

独立复算5022项、5588338个数值比较通过，最大差2.17e−12以内：10份新物理参数/全状态、96份神经检查点完整重载与NumPy前向、24拟合/4800更新、全部合法网格查询输入和成熟目标、外层归一化、四条发报与校准、18汇总/72逐点/54种子/216种子逐点、21096点日、864端点、360配对差与27组门槛复算一致。

物理核对使用原C求解器重放，另用显式求和核对坐标到观测投影和独立表达构造特征；不冒称另一套独立物理求解器。新教师初始化均来自m之前原冻结432/612/792教师，完全相等；拟合只用m行位移和驱动，未来驱动延展不改变前缀状态。审计新增优化器更新/拟合0、固定物理前向20次。

两臂同种子全部抽样、单位、初始化和网络相同；独立重建历史/未来特征和全部合法距离目标。标准化按当前外层前缀，不能称伪起点当年真实在线标准化。当前外层教师保持，972仍用792日参数。96个完整路径由独立NumPy GRU/图/解码计算，权重重载误差0；未来位移污染不改变e200发报，零头精确回退起点校正B+。

94条事件按顺序核对：每阶段需要的新教师先完成，才启动相应神经拟合；每窗两臂三种子完成才发报；全部四窗锁定后才读全1461日标签评分。90条误差池仅来自上一已发出轨迹第91—180天，最终[1062,1152)，16日间隔。种子概率评分使用本组集成尺度，保留该限制。

## 物理拟合与来源限制

10次新拟合各达到800次函数评价上限，共8000次nfev、388325次优化物理前向、20次正式后检前向；全部数值合法，优化器成功标志0/10。目标下降/终点对齐不证明参数唯一或优化收敛，不自动增加迭代。单阶段历史更新与原导师全套标定有差异，外层972复用旧教师；不能由本次失败否定所有匹配教师策略。

矩阵乘法运行时警告在原程序中仍出现；全状态有限，液压力矩阵及坐标投影与显式求和一致。没有改底层库、物理模型或数值容差，不声称警告成因已修复。

203项冻结来源哈希保持，其中185项原已跟踪、18项为本地运行时提取/编译依赖。18项现已按原字节归档至本轮reproduction_runtime，16项与版本化ZIP对应原件完全相同；POSIX Python仅有包导入及动态库后缀改写，逐字转换核对一致；dylib为原C的主机编译结果，原二进制哈希保存。原运行时路径和导师资料不改。

## 统计解释检查（11/11）

1. 聚合反转：完整保存逐点、种子与集成；972更新组2/3种子均值改善，集成却退步，分别报告。
2. 生态推断：不把四点平均推广到每点、其他滑坡或真实预警。
3. 选择偏差：网格和完整预测窗事前冻结，未按教师/预测质量删日期。
4. 碰撞变量偏差：没有据混合输入或事后筛选相关性宣称机制。
5. 基率：当前无分类/预警评价，不适用；区间覆盖不称预警准确率。
6. 均值回归：两组同起点和标签，保留B+、起点校正B+与简单对照；不因某一坏窗口改善断言有效。
7. 幸存偏差：10次未收敛物理输出和全部24神经拟合保留，无按成绩重试。
8. 多重探索：承认历史窗口反复暴露；无p值、无新盲测和按最优版本选模。
9. 分析路径：先冻结计划/配置/样本/预算/门槛；训练与评分锁保持，不追加λ/RL。
10. 因果：效应对应整个教师更新政策，输入/基线/目标随教师一起改变，不等于真实物理因果，也不识别唯一失败原因。
11. 时间倒置：历史教师不使用未来拟合参数或标签；后期标签仅评分，已知未来驱动条件明确保留。

拟合前缀612/792/972/1168的完整293日监督起点仅0/2/5/8，大量窗口重叠；“13个教师版本”不等于13段独立293日样本。最终较晚的新教师仍只有部分成熟目标。原数据as-of/预处理和旧优化限制保持。

复算入口为模块teacher_refresh.audit，回执在results/ootang_teacher_refresh_v1/20260915/audit/receipt.json；来源、教师拟合、输入支持、原始警告和逐项比较均可追溯。审计首次运行通过，无训练后数值或门槛修订。

## 图文交付核验

三张最终PNG/SVG、12面板、88条科学图形记录，349项/222772数值核验通过；源数据差0，SVG浮点表示差7.21e−7pt以内。字体、遮挡、1.5pt对齐和逐图目视通过，所有观测/均值/种子/95%区间完整落在坐标范围内；两组四点图使用同一组纵轴。临时同源PDF仅用于渲染QA，无PDF报告交付。

结果报告6表31行114个数值单元、README九个RMSE单元按源CSV回读，配对变化独立从两组汇总重建；全部门槛、种子计数和“8/10历史拟合改善但全部未收敛”逐项核对。文档回执另记录本地链接数量。图件封装首次因父目录README无法相对于v1表示而失败，修正封装共同根目录；图像、数据、评分不变。图文步骤新增训练/物理前向均0。
