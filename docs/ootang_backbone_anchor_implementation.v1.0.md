# 统一接口骨干×起点表达：实现核验 v1.0

## Material Passport

代码实验、本地实现核验；新训练0。冻结计划da685f33，训练前计数勘误61285287（结构不变）；旧数据、旧模型和结论不修改。

独立入口`code/backbone_anchor/`继承冻结GraphGRU公共模块；新Transformer只有时间编码器不同。实际参数GRU1241、Transformer1249（多8，0.64%）。公共图/点嵌入/解码初始张量相同；两Transformer输出臂同seed全部初始参数相同。

[预检回执](../results/ootang_backbone_anchor_v1/20260915/preflight/receipt.json)：1106项冻结来源、465项记录/596805个数值，最大差1.78e-15。两旧GRU臂96份检查点从新公共接口重载，完整预测和学习变化均差0；样本表和尺度完全一致，满足复用条件。复用核验分别读取各自合法前缀，已暴露旧结果不冒称盲测；新Transformer预检只使用612日前缀。

新增结构检查包括：最后token注意力与完整一层因果注意力前向/全部参数及输入梯度等价；变长批次padding毒化隔离；公共解码给定同状态时逐项一致；未成熟训练目标拒绝；监督与独立残差计算一致；未来位移毒化不改预测；h0参考/零变化连接及两类零头回退；非零头梯度有限并能到达编码器；模型重载差0。预检未执行优化器更新，未重新拟合B+或物理前向。

制作实现锁时曾把目录相对产物锁用于仓库相对源码，路径检查失败；随后入口因缺失实现锁而被保护条件阻断。新增拟合/更新均0。已改用显式仓库相对源码清单，训练源码/配置/阈值未因该异常改变；[原异常和说明](../results/ootang_backbone_anchor_v1/20260915/preparation_anomaly.json)保留。

正式预算两臂×四前缀×三种子×200更新=24新拟合/4800更新，另复用24份旧GRU拟合。训练和解码入口源码及预检锁入[实现锁](../results/ootang_backbone_anchor_v1/20260915/implementation_lock.json)。接着按前缀发出完整293日并保存，再统一评分；成绩好坏不改变预定工作量。
