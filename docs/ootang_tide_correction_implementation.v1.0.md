# 冻结KIN校正实施与预检

新入口`code/tide_correction/run.py`将历史KIN重建、完整路径入池、校正器拟合与发报评分分开。历史模型只看其r前缀，重建预测每条只用m前历史和m+293给定情景；原612/792模型只读复用。整条成熟后才将该路径交给校正器，目标为该种子历史训练外预测的已兑现误差，绝不使用当前n模型的训练集拟合误差。

原四起点KIN均值/种子路径及检查点原样保存为基准。校正器独立模块只有13179参数，模型图和优化器内均没有KIN参数；每次保存校正检查点核对冻结基模型SHA。CAL与HCAL仅有标准化后H三列是否置零不同，结构/初始化/预算/残差来源相同。612零样本分支只保存e0，不产生优化器步骤。

训练前150项、470005数值检查通过，最大差2.2737367544323206e−13，优化更新0、基模型重拟合0。覆盖完整路径成熟0/20/200/396、基模型截止、未成熟目标拒绝、训练后段驱动/位移隔离、旧KIN独立前向与精确回放、H字段/日期、CAL遮蔽负控与HCAL正控、相同初值/抽样、零校正回退、h0/大输入有界、独立NumPy前向、重载和基模型梯度隔离。原来源2216项核对通过。

独立90分钟窗口及固定39正式拟合15600更新见冻结计划；不按效果停训练或换配置。命令在仓库根目录执行：

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=code /tmp/ootang-tide-env-20260915/bin/python -m tide_correction.run train
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=code /tmp/ootang-tide-env-20260915/bin/python -m tide_correction.run score
```

下一步为完整训练及261检查点、396历史路径、校正残差/概率池、评分/图件独立核验。实现通过不代表效果达标；用户/导师验收仍待定。
