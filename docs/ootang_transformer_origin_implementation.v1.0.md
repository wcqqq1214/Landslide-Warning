# 起点条件化Transformer实现冻结 v1.0

事前计划5727067保持。432教师覆盖长度由计划笔误725修正为904：教师适用伪起点432—611，最后目标索引611+293−1=903；612教师长度1084正确。此次只校正覆盖长度，不改变样本/结构/轮数或两次物理前向预算。原计划可追溯，核验阶段正式神经训练0。

已完成253来源哈希核验，两个延长教师与既有完整前缀逐字段最大差0。全部12个训练抽样表的目标成熟性/日期边界通过。未来位移改变对输入影响0；非零模型的历史观测正控影响COND_ATTN=0.07010641206862467、POOL_MLP=0.08835687723388053，NO_OBS_ATTN=0。三臂零头B+精确回退、非零重载、batch/query一致、未来query隔离和有限梯度通过。

模型参数3220/3220/2644；POOL没有q/k投影及query归一化参数，其他参数按种子共用初始化。训练实现、配置、计划、来源、此勘误和教师数组一起冻结。正式阶段固定36拟合7200更新、三个完整外层窗口，无选模或效果失败停跑规则。
