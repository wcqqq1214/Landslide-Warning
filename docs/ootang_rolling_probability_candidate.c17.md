# C17：用新均值自身的成熟误差做原规则校准

2026-09-13。原八小时截止 05:27:52 UTC 不变，本次准备/运行/核验最迟 **05:10:00 UTC**，单次回放最多 5 分钟。C16 原件/独立核验 `2e8c83f`/`64d86d5` 已固定；本版是零均值训练的概率配对实验，不追溯修改 C16 26/27 或原 C4 迁移失败。

## 唯一要检查的区别

C16 的新均值实际改变了误差，而 sigma 完整继承自 C8 DATA，它的历史校准误差仍来自旧核心。固定 sigma 是 C16 事前设计且构成合法分布，不是实现错误。下一项问题是：保持同一原校准算法和所有参数，用**自己已发均值**的成熟误差更新校准，是否更合适。

这与 C7—C12 在固定均值上换分布形状/宽分量/训练评分不同；本次均值已发生实际变化。不搜索参数，不设额外倍率、覆盖补偿或权重下限。若结果没有支持，不再顺着接近 85% 的覆盖成绩调宽或新增概率候选。

## 冻结配对与完整规则

原 C16 PHYS/TREND/CORE 全部均值、raw_sigma、输入/系数和所有旧对照保持。新增：

- `C17_PHYS`：复用 C16_FAST_PHYS，主候选；
- `C17_TREND`：复用 C16_FAST_TREND；
- `C17_CORE`：复用 C16_FAST_CORE；
- `C17_C8_CONTROL`：对原 C8_ONLINE_DATA 应用同一回放，须精确复现它的已存 sigma、校准因子和覆盖反馈，作为实现一致性对照。

各模型独立初始化原 CausalCalibration：每步长/点最近 90 条成熟标准化误差；先验计数和平方和各 10；覆盖反馈 rate=0.02、目标 0.9、初始 log_scale=0、边界 ±log(10)，sigma 下限 0.01 mm。先锁定当日分布，再释放 y[n]，再处理所有目标等于 n 的已有预测。RMS 和漏覆盖反馈只用该模型自己已发的 mean/raw_sigma/sigma，不用新均值去重写过去区间。

均值、原始尺度及其历史模型训练完全固定，校准流程不会反向改变 C16 误差头。三臂属于既有机器学习均值的概率模型；不新增神经网络，不称经典 PINN。PHYS 的 B+ 误差参照仍是模型输入，其额外价值须对两个预定消融分列，不根据排名拼点。

两阶段 792/1168、1168/1461，后者仍为已暴露探索。四点完整 h30 347/264 个目标，h1—30 全部保留，原 27 项门槛和 DRIFT1/B_ANCHOR 对照身份不改。三候选加一致性对照共预计 **307,200 次成熟点位校准更新**；零新增均值求解、尺度参数优化、神经训练或物理调用。与此前 Ridge/混合权重更新分开计数。

## 验证和停止

从 C16 冻结模型数组、原标签、逐起点锁和独立 NormalDist 分位数重建校准，检查未来目标拒绝、实际已发 sigma 用于反馈、全部旧分布及均值/raw_sigma 不变、C8 同一性、最终反馈状态、完整评分与原门槛。原数值容差不改，不运行旧训练入口。

如果两阶段完整门槛通过，完成原配对成块、非重叠敏感性及完整曲线再评估保留，物理消融限制单列；若不通过，停止此新均值自身校准配置，保留 C16/C17 正负结果，不调反馈速率、窗口、倍率或主步长。候选和总截止不延长，每一步 docs 和本地 commit；不把概率指标接近门槛视为已达成。
