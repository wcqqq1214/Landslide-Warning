# 藕塘历史输入审计补充 v1.16.1

## Material Passport

- 日期：2026-09-11；Origin Skill/Mode：academic-research-suite / experiment-agent / plan。
- 原 v1.16 运行在三来源比较处失败，exitcode=1，原代码/方案/日志及失败已封存。
- 本补充只完成不依赖导师 CSV 严格等值的历史输入构造，不训练或生成新预测。

## 1. 失败依据与变更

原失败索引 SHA-256 为
`e93490b465e1a2655d51e900da9fd2e32c079d9b261083bd00d003369b734b58`。
有界只读定位发现：前 612 日六列，CSV/XLSX 最大差为 0；导师 CSV 的 MJ3
有 240 个值超过原 1e-12 容差，最大差 5.002220859751105e-12 mm。
示例：2017-05-03 为 1005.100114453055 与 1005.10011445305 mm。
这定位到末位小数差异，尚未核实导师文件采用的具体序列化方法。

原三来源等值标准及失败不改写。补充程序保存全部三对比较、逐列最大差与超门限
计数，对导师比较明确记 `passed=false`。历史接口仍以原公开 CSV 为输入，并要求
CSV/XLSX 在原 1e-12 门限内一致。这个处理变化在本版登记，不伪装成原运行通过。
历史构造成功与三来源严格等值、原始 GNSS 当时可用性分开报告。

## 2. 固定计算与验证

- 沿用 v1.16 的四点顺序、c=252/342/432/612、30 日窗口、差分左端点、前 612 日
  数值读取上限、1e-10 历史值核验容差。复用原已提交的有界读取器和历史构造代码。
- 仅新增一次补充执行，120 秒硬上限；0 拟合/神经/物理/尺度调用或新预测。
- 保存 `source_checks.json` 的失败比较及 `origins.csv`、`monthly_links.csv`、
  `history.csv`、四个 `history_c.npz` 和 `availability.json`。原始可用性保持 unknown。
- 独立用另一 CSV 解析器逐格核对位移与相邻日差分，独立核对历史日期、月内计数及
  冻结指纹连接；全部日期/点位/整数/状态精确比较。保留来源快照、保护哈希、日志和索引。
- 合成测试沿用 v1.16 的 10 项；增加来源比较状态测试，确保导师差异被记录、公开
  CSV/XLSX 不一致会被拒绝，且任何来源的日期错位都不能被忽略。
- 补充独立复核只读，不改变原失败的 exitcode、源码、门限或产物。无未来值反馈，
  不改变既有模型、划分和负结果。结果仍为现有物化日表上的条件历史原型。

方案、实现/测试、补充产物、结果与入口分步 commit。后续历史编码模型及训练预算
仍须另版确定；本补充的接口核验不证明预测精度或概率覆盖已经改善。
